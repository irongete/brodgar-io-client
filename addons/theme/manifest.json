{
  "name": "Theme",
  "id": "theme",
  "version": "1.8.0",
  "author": "brodgar",
  "description": "Example addon: a whole client look as a data file. It reads theme.json with hafen.asset(), parses it with hafen.json() and hands the table to sheet:load(), mapping only the two values JSON cannot carry -- a font face and an image. ':theme' applies, drops, saves and restores; to make a different theme you edit the JSON.",
  "api_version": 1,
  "saved_variables": [
    {
      "name": "layout",
      "scope": "account"
    }
  ],
  "files": [
    "main.lua"
  ]
}
