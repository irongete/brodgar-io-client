{
  "name": "Atlas",
  "id": "atlas",
  "version": "1.8.0",
  "author": "brodgar",
  "description": "Example addon: a live minimap panel built from the map database alone -- hafen.map():grid():get(id), grid:image(lvl) and the segment's markers -- painted by the engine through the stylesheet, so the panel runs no draw callbacks at all until ':atlas pins' turns the Lua-drawn pin layer on.",
  "api_version": 1,
  "files": [
    "main.lua"
  ]
}
