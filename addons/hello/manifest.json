{
  "name": "Hello",
  "id": "hello",
  "version": "0.89.0",
  "author": "brodgar",
  "description": "Example addon and standing regression harness: it asserts the read, event, timer, saved-variable, UI, ghost, kin, marker, icon-category and container tiers of hafen.* on every login and prints a pass list. Read-only -- it declares no permissions, so it is enabled by default.",
  "api_version": 1,
  "files": [
    "main.lua"
  ],
  "saved_variables": [
    "persist",
    {
      "name": "acct",
      "scope": "account"
    }
  ]
}
