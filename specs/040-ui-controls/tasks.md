# 040-ui-controls — Tasks

- [x] 040.1 — The ownership contract, and `hafen.ui():button()` as its first consumer
- [x] 040.2 — The face setter, and `IButton`
- [x] 040.3 — Display: `:label()` / `:image()` / `:separator()` / `:progress()`
- [x] 040.4 — `:check()`, and the `:value()`/`:onChange()` spine
- [x] 040.5 — `:radio()` as ONE control
- [x] 040.6 — `:slider()` and `:scrollbar()`
- [x] 040.7 — `:entry()`
- [x] 040.8 — `:scroll()`
- [x] 040.9 — The row bridge, and `:list()`
- [x] 040.10 — `:dropdown()` and `:menu()`
- [x] 040.11 — `:grid()`
- [x] 040.12 — `:table()`
- [x] 040.13 — The close: the example addon, the docs, and the completeness sweep
