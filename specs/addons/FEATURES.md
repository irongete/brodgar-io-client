# FEATURES — index of `NNN-` folders

> ONE line per feature folder: `NNN-name — STATUS — summary (design doc) — tasks`.
> STATUS ∈ ACTIVE / DONE / PENDING. Folders are never archived — they are the addressable
> history of the project; closed ones are only opened when named.

- 001-bootstrap-engine — DONE — LuaJ engine + `:lua` REPL, disk loading (manifest), tick pump + events + timers (design/01,02,03,04,09) — tasks 001.1..001.3
- 002-read-api — DONE — Glob-backed read API: gob/world/map/player/time/sound/items/char/party + GobRef resolve (design/06) — tasks 002.1..002.3
- 003-widget-tree-reads — DONE — adapter mechanism (uimsg tap + poll) + vitals/buffs/food/study/skills/actionbar/equip reads & events (design/14) — tasks 003.1..003.4
- 004-saved-variables — DONE — hafen.store JSON persistence, per-char + account scopes, auto-save + flush lifecycle (design/02,05) — task 004.1
- 005-sandbox-reload-panel — DONE — strict sandbox + 2-layer watchdog, :reload + enabled set, AddOns panel + tooltip fix (design/05,10,12) — tasks 005.1..005.4
- 006-custom-ui — DONE — hafen.ui windows/widgets + LuaGOut draw wrapper + HUD & gob overlays (design/07) — tasks 006.1..006.2
- 007-hooks-hotkeys — DONE — hook levels L1/L2/L3 (input/action/message) + hafen.key hotkeys + keybind-panel integration (design/13) — tasks 007.1..007.5
- 008-widget-replacement — DONE — onWidgetCreate/adopt/replace + bags example (wrap-don't-reimplement) (design/08) — tasks 008.1..008.3
- 009-gap-subsystems — DONE — markers, lore, radar, slash, kin, speed, craft, quests, wounds, fight (all read; api-reference "Gap subsystems") — tasks 009.1..009.10
- 010-write-actions — DONE — per-addon permission + consent (D-027/D-028), hafen.act verbs + per-subsystem writes, walker demo (design/12) — tasks 010.1..010.7
- 011-virtual-entities — DONE — hafen.ghost client-only gobs: clickable, look, grid-anchored layouts, transform gizmo; planner example (design/16) — tasks 011.1..011.7
- 012-custom-rendering — DONE — hafen.render images/sprites/billboards + glTF models (textured, lit) on the shared entity core (design/17,18) — tasks 012.1..012.7
- 013-data-network — DONE — hafen.json (one serializer) + hafen.http async get/post, allowlist + IP block + safe redirects; netdemo (design/19) — tasks 013.1..013.3
- 014-ui-extensions — DONE — widget onDrop + g:resource + mouse mods (D-038/39/40) (design/07) — task 014.1
- 015-widget-introspection — DONE — WidgetNode tree walk + engine-true hit-testing; widgetstack framestack/inspector (design/20) — tasks 015.1..015.2
- 016-fonts — DONE — hafen.font: handles, own-drawing, all 11 scopes (chrome + world) + per-instance node:setFont (design/21, D-043) — tasks 016.1..016.9
- 017-gob-oop — DONE — Gob flat accessor → OOP class (`hafen.gob(id)`, `gob:health()`), interned identity, `hafen.player():gob()`, hard cut (D-044/045/046) — tasks 017.1..017.2
- 018-client-options — DONE — hafen.client:options(): read/write the Options-window settings (interface, video, audio, camera) with arity-as-verb chainable accessors + the unified keybindings registry; hafen.key retired, addon hotkeys start unbound (D-047); optionstest harness — tasks 018.1..018.4
- 019-profiling — DONE — hafen.client:profiling(): frame/CPU/GPU timings, memory, render, net, loader, per-addon cost + custom Lua scopes, armed by a single master switch (zero cost when off); new Options "Client" panel with "Enable profiling", also at options():client(); per-widget cost, named render passes (shadow/scene/ui2d, CPU+GPU) + armed GL counters; overhead accounting per tier, proven at 0.54% of frame (D-048..D-055); `profiler` addon — six tabs, pause + scrubbable timeline (design/10) — tasks 019.1..019.8
- 020-kin-oop — DONE — kin/buddy roster to OOP: `hafen.kin()` indexable roster (`[n]`/`:find`/`:list`/`:add`) + `hafen.kin(idOrName)` interned Kin (reads + chainable gated writes, `:endkin`/`:forget`, `setGroup` 0..254), Kin↔Gob both ways (`kin:gob()`/`gob:kin()` over the `ui/obj/buddy` attrib), `kin:info()` snapshot escape hatch, KinChanged payload = Kin[], hard cut of the flat table (arity is the verb, D-056) — tasks 020.1..020.3
- 021-actionbar-oop — DONE — action bar (hotbar) to OOP: `hafen.actionbar(n)` = the interned Slot at the raw 0-based game index (reads `:index/:empty/:res/:name/:cooldown/:info` + gated `:use([mods])` chaining on self), `hafen.actionbar()` = the 1-based iteration view of all 144 (same objects; a 0-keyed Lua array cannot have an honest `#`/`ipairs`, D-057), `:info()` snapshot escape hatch, `ActionbarChanged` payload = Slot, hard cut of the flat `slot`/`use` — tasks 021.1..021.3
- 023-menugrid-oop — DONE — the action menu (MenuGrid paginae) as OOP: `hafen.menugrid()` = every known action, `hafen.menugrid(resName|displayName)` = one interned Action (string key split by shape — a "/" means resource; no addressing by position, the catalogue grows on discovery), both keys → the same object (`:res/:name/:path/:parent/:children/:tooltip/:hotkey/:isnew/:exists/:info` + `:use()` driving `PagButton.use`, which reaches the id-only paginae no path can express; no `mods` — that path ignores `Interaction.modflags`), collection `:find/:roots/:list` — design/07's deferred menu-ability primitive, and the first section to own its own write verb ahead of the `hafen.act.*` restructuring — tasks 023.1..023.3
- 024-audio-oop — DONE — audio to OOP, and it is `hafen.sound` **alone**: `hafen.sound(resName)` = an interned Sound (D-056 callable namespace) with `:res/:play([volume])/:stop/:playing/:info`, `hafen.sound()` = the addon's own still-playing Sounds (auto-silenced on disable/`:reload`), hard cut of the flat `sound.play`; volume is `:play`'s FIRST argument and never entity state, since the handle is interned and shared (D-059), and there is no `:exists()` — a name-keyed handle has no lifetime to go stale (D-060); playback stays ungated (client-local) and the whole feature took **zero core edits**. The planned `hafen.music` Track section was built to spec and then **removed whole** (024.3, D-058): `haven.Music` is MIDI, driven only by the server's `"bgm"` message, which this server never sends (0 `midi` layers in 132,777 cached resources) — what players hear as music is `ActAudio.Ambience` on the `amb` channel, its own feature if ever wanted (design/06) — tasks 024.1..024.4
- 025-buffs-oop — DONE — buffs to OOP: `hafen.buff()` = the active Buff objects (bar order, `dest` excluded), `hafen.buff(nameOrRes)` = the first whose res or name contains the string (the old `has()` as a lookup), entity `:res/:name/:amount/:duration/:number/:exists/:info` (the meter is `:duration()` here — the run that is LEFT; only the action bar's identical meter is a `:cooldown()`) interned per addon on the `Buff` **widget identity** (not a name — the same res can be up twice), `BuffAdded/BuffRemoved/BuffChanged` payload = the object (a removed Buff keeps answering its reads with `:exists()` false), hard cut of flat `hafen.buffs`; the word "debuff" is gone from the whole area — the engine knows only `Buff`/`Bufflist` and publishes no positive/negative flag, and a meter is named for what it measures on ITS side (D-061) (design/14) — tasks 025.1..025.3
- 022-actionbar-set — DONE — assign actions to slots by resource name: `slot:set(resourceName)` sends the very `wdgmsg("setbelt", n, "res", name)` a menu-grid drag sends, gated on `actions` (D-027/D-028) and chaining on self — the write is ASYNC (the server echoes it back, so `:set():use()` uses the old content) and an unknown name is silently dropped; demo `:walker setbar <n> <res>`, `hello` checks the gate refuses — task 022.1
- 027-meters-oop — DONE — the HUD meters to OOP, and the end of the flat widget-tree reads: `hafen.meter()` = every meter in the `place == "meter"` slot (1-based, HUD order — not a hard-coded hp/stamina/energy triple read by tree position), `hafen.meter(needle)` = the first whose `IMeter.bg` **resource name** contains the needle (the names are server-published, so the API searches them and tells you to read them off a live client with `:res()` — no client-side alias dictionary, D-063), entity `:res/:index/:value/:color/:segments/:exists/:info` interned on widget identity like a Buff (a meter has a lifetime, so `:exists()` is real and a removed one keeps answering); colour and multi-segment bars reach Lua for the first time; `VitalsChanged` → `MeterChanged` + the lifecycle pair `MeterAdded`/`MeterRemoved` (which turns "vitals is nil for a beat after enter-world" from a documented gotcha into an event), all carrying the object through a `hasSub`-gated `fireMeter`; hard cut of `hafen.player():vitals()`, `VITAL_KEYS` and the `Vitals` type; one `// addon:` core edit — `AddonWidgets.hudMeters(GameUI)` over the private ordered `GameUI.meters`, replacing a `children(IMeter.class)` DFS whose order was a tree artefact (design/14) — tasks 027.1..027.3
- 038-gob-overlays — DONE — `overlay` is the ENGINE's word for a thing attached to a gob (`Gob.ols`/`addol`), and the API had spent it on `hafen.ui.gobOverlay` — a screen-space painter that is not one — while the real per-gob attachment hid behind `follow = gob` in `hafen.render` and `hafen.ghost`. All three collapse onto the entity that owns them (D-044): `gob:overlay(key, spec)` attaches or replaces, `(key, nil)` removes, `(key)` reads one, `()` reads all — keyed per addon, ONE spec table covering both the screen-space paint (`draw`/`text`) and the world-space anchor (`image`/`model`/`ghost` + `offset`), with the game's OWN overlays in the same read as `native = true` and read-only, and a hard cut of `hafen.ui.gobOverlay`, `gob:overlays()` and every `follow=`/`:follow` (design/07,17 → the standing design/24). **The rule is D-100 — the state belongs on the THING, and a sweep is the shape of having put it elsewhere**: the store moved onto the gob's own `GAttrib`, deleting the filter list, the throttled 5 Hz sweep, the per-frame re-match at the draw and the prune an addon-side map would have needed — and making 'an overlay dies with its gob' cost no handler at all, since `Gob.dispose()` disposes every `GAttrib`. D-102 is the half that is NOT free (a world overlay's visual is its own client gob and `OCache.remove` never calls `dispose()`, which is exactly the bug `follow=` had — a sprite anchored to a felled tree floating forever with no owner), so its end rides the `GobRemoved` the client already raises. D-101 was measured rather than assumed (13 of 33 decorated gobs carry two overlays of one resource, so a native overlay is a UNION over its resource name and `ov:count()` publishes the multiplicity); D-103 keeps ONE door onto an absorbed mechanism. `GobOverlayAdded`/`GobOverlayRemoved` are the first events over `Gob.ols` and the feature's ONLY core edits — three `// addon:` lines where the plan predicted two, because removal has three paths and `ctick`'s expiry of a finished sprite is how most of the game's overlays actually end — carrying D-104 (an event's scope follows its key's scope), D-105 (where the read collapses, the event follows the KEY, so a replace fires both halves) and D-106 (a queue whose handlers write back into it is drained one frame's worth, never to empty). Example addon `tagger` — tasks 038.1..038.4
- 037-map-database — DONE — the map database (`MapFile`) reaches Lua for the first time, and the area splits on the axis it was always on: **`hafen.world` is the LIVE world** (gobs, terrain, the session coordinate spaces, screen→ground, placement snapping — all of today's `hafen.map`) and **`hafen.map` is the RECORDED map** (segments, grids, zoom levels, minimap drawings, the `cplot`/`vlg`/`realm` overlay masks, with markers and icons as relations rather than namespaces beside it: D-066, and D-061 refuses the name `radar` for `GobIcon.Settings`). The anchor stays `gridPos` for a reason stronger than persistence: a **segment id is client-random** (`new Segment(rnd.nextLong())`) while a **grid id is the server's** (the mapdata `"m"` layer), so `{gridId, x, y}` is the only one of the two that means the same thing to another player — and `MapFile.merge` says it again, re-basing a segment's coordinates and rewriting the markers *inside* the file while an anchor in `hafen.store` silently points at the wrong place. Segment + tile coord is a read-only view, never a saved or sent shape; and since a marker's own anchor IS `{seg, tc}`, `marker:anchor()` is what makes a marker shareable (design/23). **Shipped in five tasks with ZERO core edits — every seam was already public.** Six decisions carry it and each answers a question the engine asked first: an entity's identity is the key a NAME can address (D-093, the icon registry's compound key collapsing to its resource); an intern key follows the engine's OWN stability (D-094 — a published id where a cache evicts and rebuilds, Java identity where a Marker is mutated in place); a read of a STORED world kicks the load and answers nil, never blocks and never takes a callback, because a minimap panel walks a dozen grids a frame and THE FRAME IS THE RETRY LOOP (D-095); a client-local id a MERGE re-bases is worse than one that goes stale, so the anchor is the only shape that leaves the client (D-096 — *before exposing an identifier, ask not whether it is stable but how it fails*); a ref-counted client toggle is a HOLD an addon takes and releases rather than a switch it flips, idempotent by arithmetic (D-097); and a DERIVED resource is keyed by what it PRODUCED and bounded, because nothing else ends it (D-098 — four grids under one zoom grid are literally one picture). The imagery turned out to need **no new type at all**: a grid drawing is an ordinary image handle, so `g:image`, `hafen.render.sprite` and the stylesheet's `bg = {image = …}` took it for free — which is what let the close measure a categorical zero, and D-099 is what that zero cost to make true (*a categorical cost claim is a property of the code's SHAPE, never a branch inside the callback*). The example addon `atlas` is a live minimap panel out of the database alone, painted by the engine, whose whole per-frame cost is a timer asking whether the picture changed — tasks 037.1..037.5
- 036-ui-layout — DONE — feature **E, the last of the run, and with it the whole skinning system**: it paid a debt written in the public contract — since 029 the Widget table said `:pos(x,y)` on a **native** widget was an error, *"layout, a later feature"*. `pos`/`size`/`anchor` joined the sheet (folded per property, D-076) with the geometry **verbs** as the hand-named top of that same fold (D-077): one cascade, not two mechanisms. The move is **real** (`c`/`sz`, the fields a drag writes), so hit-testing follows it. **The verified risk was answered by substitution, not restoration** (D-086): `GameUI.savewndpos` runs from `dispose()` **and every 60 s from `tick`**, so putting the widgets back around the write would snap a laid-out HUD once a minute — instead all eight `Utils.setprefc(key, w.c)` sites read `AddonWidgets.stockc(w)`, and the client persists what the USER placed. **`anchor` is the concept and `pos` its degenerate case — to the widget's own PARENT** (D-090), because `:pos()` reads the parent-relative `c`; a rule saying both is an error rather than a winner picked by table order. Layout is a **write**, so it is said where a widget is MATCHED and refused on a site key and in `widget:skin{}` (D-088) — which buys the categorical result 036.4 measured: **a sheet that only lays widgets out opens no frame and costs the draw nothing** (one string in two windows keeps ONE text-cache key; the same rule with a `font` in it takes two). An undo removes a LEVEL rather than emptying the cascade (D-089); a derived position is re-derived by POLLING the geometry it reads — never the tree — and clamped by `GameUI.fitwdg`'s own formula (D-091), since `UI.scalef` is restart-only and two of the three "events" are not events. **Profiles deliberately not built**: `theme` gained its layout as three anchor rules in `theme.json` (costing main.lua **zero** lines — a place is not a handle) and a dozen lines of `hafen.store` for `:theme save`/`forget`, which is the whole profile system. The close states the skinning boundary for good (D-092): what the sheet reaches, and the six things that would be a new chapter rather than a gap — tasks 036.1..036.4
- 035-ui-chrome — DONE — feature **C2, the last of the skinning run**: the sheet learns to **draw** — `bg`, `border` and `pad` on the window chrome and on the window-**less** panels. **Both seams already existed and neither was bent**: `Window.chdeco` swaps a deco live, so `SkinDeco extends DefaultDeco` overrides `drawbg`/`drawframe`/`iresize` and **nothing else** (close button, sizer, `checkhit` and the caption inherited — the caption through an extracted `checkcap()`, or `window.title` would die the moment a theme installed); and `IBox` is already an interface, so one level down the replacement is the **box**, routed exactly as fonts are (`Fonts.box("panel", …)`) at seven sites. **`window.frame` is a site key, sibling of `window.title`** — the chrome is a `@DefaultDeco` child with role `nil` and can never be *selected* — while a cascade level naming the **window** reaches its frame, which is how you theme one window rather than all. Geometry was the new risk class and its answer is one rule: **the margin belongs to whoever paints the frame** (D-080), so a skinned window is the stock formula with the theme's numbers in it (`content + insets + 2·pad`, no hidden minimum, not even for the caption) while a **panel is inert** because it fixed its size and its children's places when it was *built* (D-084). Three findings a person made and no assertion could: `bg` is bounded by the frame that sits on it (D-079), the sheet replaces a surface the client already paints and never invents one (D-083), and a heterogeneous family is named as a SITE, not a role (D-082). **Cost measured at the close and it is a non-result** — 1.574 ms themed vs 1.574 ms stock over 15 frames, with **0 draw callbacks**: a rule is parsed once into plain data and the engine paints from it. Closed with a `theme.json` that draws (chrome costs three lines of Lua, because only handles need mapping), a docs sweep that found its rot in *notes* rather than tables, and **D-085 — a suite proves its own task alone** (design/21 §F3a, 22) — tasks 035.1..035.4
- 034-ui-stylesheet-tree — DONE — feature **C1b**: the sheet styles **which** widgets, not just what **kind** of surface — 033 left tree keys parsing and inert (D-072). **F5's note came true literally**: widening the ONE frame the parent-first `Widget.draw` descent already opens, from a hand-named handle to *the rule a widget matches*, gave the whole feature with **not one render site re-routed and no drawing code changed** — the subtree comes free from the descent, and a widget built *after* a rule picks it up because `gen ^ stamp` was already contextual. Per-widget resolution is cached in a `WeakHashMap` (identity for free; weak keys mandatory — an owned list pins closed windows) and reaches the provider through the `Fonts.treeStyles` seam, so the provider still carries no dependency on the addon package. **Overlap decided, not discovered**: bare role ⇒ site key, role + refiner or a site-less role (`window`, `inventory`) ⇒ tree key, most specific wins by 030's rank. **D-075** — a read answers for the thing you point at, so `w:style()` folds the tree half alone and `nil` keeps one meaning; **D-076** — a cascade level takes the properties it names, not the ones it does not (the plan's "a tree rule outranks a site rule" read as wholesale replacement until the spec's own opening example refuted it), which also changed shipped F5; **D-077** — per-instance is a LEVEL, not a mechanism beside it: `widget:setFont`/`:resetFont` hard-cut for **`widget:skin{…}`**, and the per-widget registry LEFT `haven.Fonts` (~100 lines) to become the top of the one fold, because a read that must hand back a `FontHandle` cannot be served from a store that keeps none. Two interning levels are load-bearing and are not the same one (the per-widget `Resolved`, and the style per (handle, colour)); the suites' own method is the keeper — Lua cannot read a pixel, but `g:text`'s cache is keyed on the very value the frame stamps (design/21 §F5, 22) — tasks 034.1..034.3
- 033-ui-stylesheet — DONE — feature **C1a**: `hafen.ui.skin{ [selector] = {props} }`, a live stylesheet owned by the addon that installed it. C was **split while planning** (two different resolution mechanisms: **site keys** resolve at the draw site as fonts already do — C1a, the eleven surfaces 016 routed — while **tree keys** resolve per widget against the live tree, C1b next). The headline held exactly: C1a changed **who fills** the override stack, not how sites read it — **not one render site was re-routed and no drawing code changed**, so an eleven-surface feature is three tasks and the engine diff is a new `Sheet.java` plus deletions. Hard cut of `hafen.font.setFont`/`reset`/`scopes`; keys go through the existing `Selector` parser, so the grammar and its errors are `hafen.ui(sel)`'s and there is ONE vocabulary. **D-072**: forgive what the API will later understand (a tree key is silently inert), refuse what it never will (an unknown property errors — for every key, tree ones included). **The plan's `color` mechanism was refuted by the client**: riding the foundry's *default* colour reaches almost nothing, because nearly every routed site passes its colour per render — the answer is `Text.Foundry.fixcol`, a marker on the provider's own product, so **a rule's colour outranks the colour the SITE asks for** (**D-073**: a surface's look is the sheet's; a handle's `color` is for your own pixels). 033.3 closed it with the docs, a `theme` example whose whole look is a `theme.json` — which needed the loader's fourth type, `.json`/`.txt` → `"data"` with `:text()`, handing back the **file** and not an interpretation of it (**D-074**) — and the **property × key table**, which corrected two doc claims by reading the render sites: `heading` and the ordinary `button` caption are embossed like `window.title`, so they take a `font` rule and ignore a `color` one (design/21,22) — tasks 033.1..033.3
- 032-replace-verb — DONE — feature **B3b, the end of the plumbing**: `hafen.ui.replace` is DELETED and replacement is a **verb on the entity** — `w:replace()` reads the installed view, `w:replace(view)` installs, `w:replace(nil)` undoes, over the ONE `Hidden` record 031.2 already kept (nothing new is stored). It was the last place naming a window a different way and the only thing that could bind a view to a hidden window's toggle; **the waiting was never part of it** — `ui.on(sel,"appear")` already waits and already fires for what is open (D-068), so `LuaReplacer`, the registry, the `onWidgetPlaced` branch, the registration scan and all four matchers are a **deletion, not a port**, and the cascade took `LuaModel` (nothing constructs one once destroy-detection rides the hide record, D-071), `widgetTypes`, `descTable` and **the `UI.NewWidget.run` core seam itself** — one `// addon:` line FEWER in `haven`. **032.1 measured first**: `inventory[title=Inventory]` = 1 match, index #1 on a live HUD, so the grammar needed no extension (and the same measurement showed the role is wider than the class, and `[title=Inventory]` alone = 32). The verb owns the trap that justified the old function — it hides `nativeWindowOf(w)`, the **ENCLOSING** window, while `w:hide()` still hides exactly what you point at — and its four refusals are **thrown**, where the placement path could only log. The view's fate follows the substitution (undo / teardown / server destroy ⇒ destroyed, once). `bags` v0.3.0 arms by subscribing; `hello` v0.67.0 asserts the three arities, `replaceGone` and **the hop from both ends** (install through the grid, read back through its window). Docs: the descriptor's vocabulary is gone from `docs/addons/`, `hafen.ui.replace` left with one obituary (design/08,22) — tasks 032.1..032.3
- 031-window-lifecycle — DONE — feature **B3a**: a hidden native window stops coming back. `MenuCheckBox.setgkey` means the key and the menu button fire the SAME click, so both landed in the private `GameUI.togglewnd` (:1481), which flipped `visible` on the very window the addon hid — seven call sites, ONE method, its tick read by the private `wndstate` (:1475). **A native window you hid is a window you own** and 029's `Addon.hiddenNative` IS the ownership record (D-069): no new list, no new state, **no verb** — nothing to register, nothing to release. `w:hide()` alone ⇒ the toggle is **swallowed** and the tick answers false; `replace` binds the view its builder returned to that same record (the only place that knows both halves), so Tab and the menu button drive **your** window and `wndstate` answers `view.visible()` — no bookkeeping boolean, so the tick cannot drift from the screen. **One teardown rule, no branches: the window ends up as the user was seeing it** (D-070) — `Hidden.origVisible` DELETED, which is why a bare `w:hide()` now stays hidden through a `:reload`; **one window, one owner** (a second `w:hide()` throws naming the first, `replace` logs and skips); a stale owner falls through to stock, because a dead Tab is worse than a stock Tab. Ordering became load-bearing (`teardownHidden` BEFORE `destroyWidgets` — a destroyed view stands for nothing), `fitwdg` is re-derived rather than exposed, and the seam's identity is **asserted, not assumed**. The whole feature is the promised **two `// addon:` one-liners inside the two method bodies** — no visibility change, no new call site (design/07,08) — tasks 031.1..031.3
- 030-ui-selectors — DONE — feature **B2**: one way to point at a part of the UI. `hafen.ui` is CALLABLE (D-056) — `hafen.ui(sel)` the first match in tree order / `hafen.ui.all(sel)` every match (empty array, never nil) / `hafen.ui()` the root, `hafen.ui.root()` hard-cut; grammar `*` · role · `@Class` · `[title=]` · `[res=]`, parsed ONCE into a predicate over one widget, no descendant selectors. Roles reuse `Fonts.SCOPES` but not 1:1 (`"default"` → `*`, 10 carry over, `window` + `inventory` are new) and the scopes gave only the NAMES: the widget→role **classifier** (`LuaWidget.role`, one `instanceof` chain) is the heart of the feature, and **D-067** is what it taught — five promoted names classify NOTHING because they name a render *site*, not a widget, and guessing is worse than `nil` (174 of 625 widgets classify on a live HUD; `all("*")` = 0.08 ms). `[title=]` resolves against the nearest enclosing `Window` (a bare `Inventory` has no `cap`) and `@Class` is an exact `typeName`, no superclass walk. `hafen.ui.on(sel, "appear"|"disappear", fn)` **hard-cuts `onWidgetCreate`** and hands back the Widget entity: registration SCANS the live tree (**D-068** — "appear" is a state, not an edge, so a `:reload` no longer loses every open window), while `disappear` fires when a widget stops being **real**, not drawn (a closing `Window` lingers, readable, for its fade-out); cost scales with widget creation, not frames, via a structural/refiner split + a bounded 20-tick re-check for late captions. The spec's `[res=]`-for-windows promise was **refuted in-game** (no window here carries a resource; 69 of 625 widgets do) and the docs say so. The **inspector ships inside the feature**, in `widgetstack`: hover → role/class/title/res + every selector it has already RESOLVED, most specific first, in the honest form. `replace(selector)` deferred to B3 on semantics, not size. **Zero core edits** (design/20,21,08 → the standing `design/22-ui-selectors.md`) — tasks 030.1..030.4
- 029-widget-oop — DONE — feature **B1**: the three objects `hafen.ui` returned for one widget (window/widget handle · `adopt`/`replace` model · transient `WidgetNode`) collapsed into ONE interned entity — what you create and what you find are the same type, `:info()` the snapshot hatch, `==` the identity test (`:same()` cut). Reading stopped being coupled to replacing: `widget:items()` answers on ANY container with its window **visible and interactive** (proved on a Cupboard, a Belt, the study and three chests no adapter had ever been written for), and the lifecycle moved onto the entity (`:onItemAdded/:onItemRemoved/:onDestroy`, a per-tick `WItem` diff whose **subscription IS the registration**). Items are a relation, so **`hafen.items` is hard-cut** (`hafen.ui.inventory()/equipment()` are widget lookups, `hand()` stays a snapshot, `find()` got no replacement); geometry is arity-as-verb (`:pos()`/`:pos(x,y)`) so `:move()` is cut, and the write verbs are OWNED-only with two distinct refusals while a write on a stale widget is a silent chaining no-op. **`adopt` is deleted, not renamed** — its whole job was a readable handle, so what remains is `w:hide()` + the restore list; `replace` is untouched (it hides the enclosing WINDOW and replays the captured visibility). The intern cache is weak on **both** axes (`WeakHashMap<Widget, WeakReference<LuaValue>>`, D-064) to keep D-041's no-pin rule, and the gating measurement came out backwards: interning is **~19x cheaper** than the closure-table handles it replaced (513 ns → 27 ns per node). Provenance is DERIVED from the tree, never stored (D-065); a thing inside another is a relation, not a section (D-066). Closed the `LuaModel`/`LuaMesh` collision 028 deferred by deletion; **zero core edits** across the whole feature (design/07,08,20) — tasks 029.1..029.4
- 028-asset-loader — DONE — one loader for the files an addon ships: callable `hafen.asset(path)` (D-056) over ONE per-addon intern cache + the D-017 resolver moved to its own home; extension dispatch (image/font/mesh) and shared `:type/:path/:dispose`; hard cut of `hafen.font.load`/`hafen.render.image`/`hafen.render.model` and `render.sprite{image=}`/`object{model=}` go handle-only (one flow, D-012 — `planner` DOES pass path strings, so 028.2 must convert it: the spec's "no addon uses it" was wrong); built-in fonts become `hafen.font("sans")` (engine-owned ⇒ addressed, not loaded, D-060). Fixes the real defect that fonts had no cache at all (re-parse + re-`registerFont` per call); teardown stays typed because its order is load-bearing (R3b) — and R3b's claim that a manual `mesh:dispose()` breaks a live object was **refuted** by measurement (the object captured its samplers at mill time; what dispose forfeits is the freeing, not the picture). `RenderApi` is −347 lines and a scene namespace; the whole feature took **zero core edits**. Feature **A** of the five-step run to ElvUI-style skinning under one `hafen.ui` (design/17,21) — tasks 028.1..028.3
- 026-text-cache — DONE — kill the per-frame text re-rasterisation behind `g:text`/`g:atext`: a per-addon, content-keyed, bounded cache of the rendered `Text` in `LuaGOut` (both the fast and the rich path), invalidated on a `Fonts.gen()` bump and on teardown/`:reload`, with hit/miss/evict/bytes exposed through `hafen.client:profiling()`. Colour stays OUT of the key (it is a blit tint, F2). Invisible: zero API change, zero addon edits. The ROADMAP's alternative — an explicit text handle — is DISCARDED, not deferred: it costs the same on the volatile strings the cache misses, reaches only cache-hit parity on the static ones, and is permanent contract where the cache is a removable implementation detail (design/07,21) — tasks 026.1..026.3
- 039-uniform-api — DONE — the area-wide grammar, merging ROADMAP's "collection objects instead of arity-as-verb" and "finish the OOP migration": a section is CALLED and everything after it is a `:verb()`, arity is the verb, a collection is an object (`:list/:count/:get/:find/:add/:remove`, singular name, not indexable), no config table survives as named arguments, an explicit `nil` is an error except where it means *undo a layer* or *none*, and every read hands back a live object with `:info()` as the only snapshot. `hafen.gob` folds into `hafen.world():gob()` (D-066); `hafen.world()`/`hafen.map()` become deliberately parallel with ONE Grid entity behind two doors; the last nine flat sections (char, study, party, craft, quest, wound, fight, items) become entities, which restores `hafen.party():list()[1]:gob()` and `hafen.fight():target():gob()` — the regression 017 accepted — and pays off 017's transitional markers. **One `position()`**: a Position value object, computable *and* durable (`:offset/:distance/:tileCoord/:durable`), whose durability is EXPLORED rather than loaded, deleting `gridPos`/`fromGridPos`/`marker:anchor()`. Three plural sections renamed (`quests`/`wounds`/`events` → singular), five `get`/`set` pairs collapsed, four singular/plural dual pairs collapsed, two duplicate doors cut (`world.placeGrid`/`placeAngle` were the same `MapView` fields as `interface():posGran`/`angGran`, with the units disagreeing) — tasks 039.1..039.16 **Shipped in 16 tasks, and the four mechanisms are what made it 16 rather than one unreviewable cut**: `Section` (the per-addon singleton — a callable TABLE, never a function, so a retired field read reaches the refusal instead of failing as *attempt to index a function*), `LuaCollection`, `LuaPosition` and `Retired`, the last being pure data generated from `API.md`'s before-column, which is what let the close sweep 98 section spellings and ~29 of 47 entity rows in one pass rather than trusting sixteen tasks to have remembered. The Position is the feature's one new capability and it earned it by measurement — **209.7 bytes a call against 419.4** for the read plus the `{x, y}` table it replaced, with the store/JSON marshalling and the engine-side vector verbs being what make it a type rather than a rename. **What the build taught**: 028.3's `nil` contract was WRONG and the whole discipline rests on it being wrong (`narg()` does separate `f()` from `f(x)`; the one hole is a zero-return `f(g())`, stated on the page and asserted both ways); an entity metatable pointing `__index` straight at its methods makes a retired verb read as plain `nil`, found three times and each time only because something asserted the REFUSAL rather than the dispatch; a scripted corpus port cannot see `pcall(hafen.json.parse, x)` and rewrites the very spelling a refusal test pins; `luac -p` exits 0 on a syntax error; a thing that cannot EXIST without a place takes it on the constructor (D-127, and the world origin *raises*); an id the server re-uses cannot be an identity (D-138); and two filter paths over one collection disagreed on an unresolved name for thirteen tasks (D-141) because every consumer reached for the path that was right. Decisions D-107..D-143; standing design `design/25-uniform-api.md`, which supersedes `design/06` in full and `design/22`'s spellings
- 041-unified-events — DONE — one verb for every notification in the area, closing the last non-uniform half of the API (039 did the reads; this does the reactions). Three doors collapsed into one: `hafen.event():on("GobAdded", fn)` (N subs, `sub:off()`, PascalCase), `hafen.hook():input/:action/:message` (N per msg, `handle:remove()`, lowercase) and the per-widget `:onPress`/`:onChange`/`:onItemAdded`/8 `AddonWidget` callbacks (**one slot that silently replaces**, no unsubscribe, `onCamelCase`). **`X:on(key, fn) -> sub` / `sub:off()`, and the address picks the door — ¿tienes el objeto? `obj:on(...)`; ¿no? `hafen.event()`** (D-066 applied to notifications: a `MouseDown` has an owner, a `GobAdded` does not; L2/L3 stay on the bus because a `"click"` can come from any widget). N subscribers everywhere; `ev:preventDefault()` is the one cancel, so a handler's **return value is never read** and the OR rule (any cancels, all still run) keeps the outcome independent of registration order; PascalCase keys, unifying onto the bus's own spelling — 44 of its 88 call sites needed no rename, only the four `On`-prefixed lifecycle ones; closed sets throw listing their keys, `action`/`message` names stay open (D-129). Fixed four defects the inconsistency hid: input reaching 3 widgets of ~625 via magic tokens when `Widget.listen` is a `Widget` method, two spellings for one gesture (`onClick`, which is a `mousedown`), `ev.sender`/`ev.target` as bare class-name strings, and a selector-found native widget having no input surface at all. Every payload is read with a colon verb — `ev:msg()`/`ev:sender()`/`ev:args()` replaced the fields `LuaActionHook` built before, and the five composite bus payloads (`GobOverlay*`, the three `*Clicked`, `MarkersChanged`) became objects with them, so no object in the API mixes `.` and `:` — the last two of the five (`GobOverlay*`, `*Clicked`) were objectified only at the close (041.7), the one gap no `041.1`-`041.6` checklist had assigned. **`hafen.hook():grab{move, up}` dissolved into the feature's own rule and took `hafen.ui():mouse()` with it**: a grab constructs something you hold and `:release()`, so R4 cut its config table (the earlier excuse — per-call scope like `g:text`'s, D-143 — was wrong, because `g:text`'s table dies with its call), and the search for its address found that `:mouse()` was a `{x=, y=}` table hiding an entity — `m:x()`/`m:y()`, `m:over()`, `m:shift()`/`:ctrl()`/`:alt()` (**new**: `UI.modflags()` is public and reached Lua nowhere) and `m:grab()`, whose receiver is what lets the verb keep the engine's own word (D-061) with no compound name; `hafen.hook()` itself was then deleted whole rather than kept as an empty shell for one verb (D-173). `hafen.ui():at(x, y)` is not absorbed. Corpus ~330 Lua sites / 64 files / 44 of 76 doc pages (not the 33 estimated) / 30 Java files / 26 bus names; `EXAMPLES.md` is the frozen catalogue + port map + `Retired` source, on 039's `API.md` precedent. **The close (041.7)** swept every emitter × key in one pass (26 bus + 5 universal widget + 2 container + 4 surface + 12 control rows across 16 builders + 2 grab + 7 mouse verbs) and all 25 retired spellings, corrected `STATE.md`'s stale `ChatMessage` mention (never fires, no addon uses it), and marked `design/13`'s L1/L2/L3 addressing superseded (Level 4 stands, unbuilt). Decisions D-167..D-177 (design/13,25 · out: `:once`/`:listeners`, hook priority, post-hooks, L4, new events) — tasks 041.1..041.7
- 040-ui-controls — DONE — the client's own UI controls, reachable from Lua: 18 in two groups — the **direct** ones (`Button` `IButton` `TextEntry` `Label` `ILabel` `Img` `Progress` `HRuler` `CheckBox` `ICheckBox` `RadioButton` `HSlider` `Scrollport`+`Scrollbar`), concrete classes with plain constructors, and the **model-backed** five (`SListBox` `SDropBox` `SListMenu` `GridList` `TableBox`), abstract and generic over the item type, which reduce to `SListWidget`'s `items()`/`makeitem()` pair plus `GridList`'s `drawitem` — so four of the five fall out of one bridge and v1 needs no row factory at all (`TextItem.of`/`IconText.of` are ready-made rows). A control is a **Widget** (`docs/addons/api/ui/README.md`'s "there is one type"), so `:position`/`:size`/`:visible`/`:destroy`/`:style` and every selector work on one with nothing written for them; `:value()` is the one verb for a control's value (R2 arity) and `:onChange(fn)` the one notification; one builder per ROLE, not per class, with the setter choosing between `Button`/`IButton` (D-113, D-061). The one mechanism that moves is **provenance**, derived from the tree and written against the `AddonWidget` class (029.2) — it generalises to a contract (owner, root, dead) that every control adapter carries, which costs nothing extra because most controls need a thin adapter anyway (`HSlider.changed`, `TextEntry.activate`, `GridList.drawitem`, `SListWidget.items`/`makeitem` are overrides, not settable fields). Out: `RichTextBox`, the server-bound `FlowerMenu`/`ISBox`, `Tabs`, custom Lua rows, and adopting a control into a native window (design/07,25). **The close (040.13)** shipped the one proof no per-builder suite could: a roster-wide sweep over all 16 builders — each exists, builds bare (R4) and comes back as the class the roster promises — and a 192-combination matrix (12 shared verbs × 16 builders) proving every one answers non-nil exactly where the docs say and reads `nil` elsewhere, plus the feature's one retired spelling (`entry:text`) still throwing and a teardown that leaves the tree exactly where it started; zero `haven` edits. The docs tier (`controls.md`/`lists.md`, the `widget`/`custom`/`selectors` cross-links, both "API at a glance" rows) had already been carried incrementally since 040.1, so the close's own doc work was `examples.md`'s new row for the example addon `stockfilter` — the api-sketch.md worked panel, wired to real backpack/equipment items rather than an invented `query{}` — tasks 040.1..040.13
- 042-event-driven-reads — DONE — the addon layer stops POLLING the widget tree to synthesise its own events: `AddonManager.tick`'s per-frame poll stage (12 sites — 6 `TreeAdapter`s plus `pollReplaced`/`pollWidgetSubs`/`pollSelectorWatches`/`Layout.poll`/`pollMarkers`/`armPending`) is deleted, not gated, and every event is fired at the moment the client already announces the change, from the four seams it already publishes: the `UI.uimsg` tap (wired), widget **placement** (`onWidgetPlaced`, wired), **removal** (`Widget.remove` — override-proof where `cdestroy` is not, since 9 of 17 overrides skip `super`, which also makes `childseq` unusable), **geometry** (`Widget.resize`, which every size change funnels through — a target resizing, a window packing itself and the screen changing are ONE seam; a hand-drag rides the zero-edit `Widget.listen`), and `Loading implements Waitable` (`waitfor`/`Waiting.cancel`, wrapped by the new `Resolve`) for a value still streaming in — present in `haven`, used nowhere in `io.brodgar` before this feature — plus a notify inside the two `setbelt`/`setbelt2` paths that defer the `belt[]` write to a loader task. **Six core edits, not the three originally planned**: a widget that FADES (`Buff.reqdestroy`, then `Window.reqdestroy`) has no addon-visible "gone" signal at the moment the server actually said so, so the removal hub is called from a second site twice over (D-180); and `Window.resize` skips `super.resize` entirely for its own chrome sizing, so the geometry hub needed the identical second-site treatment (D-179's lesson, applied twice). No gate, no fallback, no `hafen.*` change — cost becomes proportional to CHANGES rather than to frames (design/14, which promised this and delivered it only to addons). Decisions D-178..D-182: **D-181 supersedes D-091** (a derived position re-derives on its own inputs' events, never a per-tick fold) and D-178 supersedes the `loader.defer` implies `poll()` rule in `learnings/widget-tree-reads.md`. **The close skipped the `hafen.prof` before/after measurement** on the maintainer's own call — the categorical zero rests on the idle-silence suite proof and the code-shape sweep instead of a measured figure — tasks 042.1..042.13
- 043-vr-namespace — DONE — **`hafen.vr()`: one section for everything client-only you stand in the 3D world**, and pure reorganization — no new rendering capability, a ghost placed after it must look pixel-identical to one placed before. It absorbs `hafen.ghost()` and `hafen.render()` whole (`:ghost()`/`:sprite()`/`:object()`, with `:widget()` arriving in 044), takes the **world-space kinds out of `gob:overlay()`** (`ov:image`/`ov:model`/`ov:ghost` hard cut, naming their replacement), and turns the **anchor into an argument** rather than a choice of door: `:add(what, p)` stands at a point, `:add(what, gob)` follows a game object. Three grounds and none is taste. **(1)** The world kinds were never engine overlays: `overlay` is the engine's word for `Gob.ols`/`Gob.Overlay`, but 038's `ov:image`/`model`/`ghost` turned out to be *their own client gob in the scene* — which is exactly what D-102 exists to handle — and `learnings/rendering.md` (038.2) records that such an entity is **already registered in `Addon.ghosts`/`sprites`/`objects` exactly like a free one** and was then deliberately *hidden* so there would be one door; so the word covered three mechanisms (engine overlays, screen painters, client gobs) and this undoes an overlap rather than breaking a category — which is also why the anchor-as-argument costs almost nothing. **(2)** It deletes a whole class of refusals: `ov:offset()` stops meaning *screen pixels or world units depending on the kind*, and the `:scale`/`:rotate`/`:tint`/`:alpha`/`:billboard` "refused on a screen-space overlay, naming the kinds" machinery disappears from the code and the page. **(3)** It answers the ROADMAP's own standing complaint that `hafen.render` is named for a **mechanism** while the other two places a drawn thing can live are named for the **place** — and better than that entry's proposed `hafen.world`, because `hafen.world()` is the live world *the server sent*, so fake props would become siblings of real gobs, while what actually separates a ghost from a gob is not where it is but **whose it is**. `gob:overlay()` keeps a tighter identity — **what is drawn at this gob**: the game's own overlays read-only, plus your screen-space `draw`/`text` at its projected point — and an anchored VR entity is surfaced in its `:list()` **read-only** down the path natives already take, so "what is at this gob?" keeps one complete answer while creation has one door. Adds `hafen.vr():list()` (everything you have stood, across kinds) and `:visible(b)` (hide it all, destroy nothing, and restore **what was visible** rather than turning everything on). Renames `:billboard(b)` → `:facing(mode)` with the two modes that exist today (`"fixed"`, `"screen"`) — done here rather than in 044 because every call site is being ported anyway — while `"camera"` **raises** until 044 makes it a real world quad, so it never means the wrong thing. Port surface measured, not estimated: **38 Lua call sites in 3 files** (`hello`, `planner`, `planner/gizmo.lua`), 2 overlay world-kind sites in `tagger`, 14 docs pages, and ~9 Java classes — smaller than the ROADMAP's "~64 sites", which predates 039. A **`hafen.vr():gob(g):add(...)` second door was considered and rejected**: it is D-103's two-doors-onto-one-object, and the docs would have to teach which to use instead of it being evident. Filed to ROADMAP on the way: world-space **text** and world-space **shapes**, two gaps the section makes visible. **The close (043.5)** did the rename and the whole docs sweep in one task: `:facing(mode)` stores the MODE AS A STRING rather than mapping a boolean at the edges (**D-190**), which is what lets 044 add `"camera"` as a value instead of re-opening a shape, and what makes `planner`'s persisted `billboard` boolean a `facing` string with no shim; `"camera"` is refused today exactly like any unknown word, because an alias is a wrong answer that would survive the fix. The docs tier moved wholesale — `api/ghost.md` and `api/render/**` deleted into a new `api/vr/**` (hub + ghosts + sprites + models + the gizmo, which moved with the handles it drives), `api/gob.md`'s Overlays rewritten around what 043.3 left, both glance tables re-rowed and 14 pages swept — 1404 links/anchors checked and 0 broken, with the checker falsified both ways. One thing the close could not do itself: `hafen.ghost`/`hafen.render` are now retired spellings and belong on area `docs`'s own §7 grep list, so the greps were run by hand (zero) and the list entry filed rather than written — tasks 043.1..043.5
- 044-spatial-ui — DONE — **widgets standing in the world**, the fourth collection of the section 043 builds: `hafen.vr():widget():add(w, anchor)` takes any Widget — one your addon built or **one of the client's own** — and stands it as a quad at a point or on a gob, its `Draw`/`Tick` rendering into a texture instead of onto the screen and clicks on that quad forwarding into its own `MouseDown`/`MouseUp`/`MouseMove`/`Wheel` in widget-local pixels. **One door, because 043 made the anchor an argument** — there is no sixth `gob:overlay()` kind. The whole feature is held to one rule, **transparency: if it works on screen it works in the world** — the same `:on("Draw", …)`, the same `:on("MouseDown", …)`, the same 040 controls, the same stylesheet and themes, dropdowns, tooltips, hover and keyboard focus included — so there is one new collection and nothing else for an addon author to learn. That is only true if the surface is a **real UI root** the widget is *reparented into*, never a texture with clicks forwarded: the client's hit-testing, focus, hover, popup placement and drag gesture all resolve against a root, so re-implementing them one by one is how each breaks differently. The one thing it rests on — and 044.5's first finding — is whether popup/tooltip placement targets the nearest root or a hard-wired `ui.root`; if hard-wired, that is the feature's single `haven` seam there. Standing a **native** window is the same family of write as `:position`/`:visible`/`replace` — a layer over client state that restores under 031's D-070, *the window ends up as the user was seeing it* — **ungated**, since the clicks reaching the server are the user's own and only where the button is drawn has changed; `:remove` is the undo, under one rule for both provenances: standing records where the widget was, removing puts it back. It **composes with `w:replace(view)`** (stand your view in the native window's place, then send that view into the world), which is the maintainer's own scenario rather than an edge case. Brings **`:facing("camera")`** — a world quad turning in yaw *and* pitch, with real world size, perspective, occlusion and shrink-with-distance, and still pickable by the ordinary 3D pass — to widgets **and sprites**, unavoidably rather than as scope creep, since both share the entity core. Planned on measurement: `Streamer`/`HeadlessClient` already render the **whole UI** into a `Texture2D` via `FragColor`, `GOut`'s `(Render, Pipe, Coord)` constructor is public, and the `TexRender`+`nofacecull` world-quad recipe is already in `codebase/world-3d.md`, so most of the pipeline is adaptation and the genuinely new work is the draw routing, input coming back (a corner-projection homography, so no camera matrices are ever exposed to Lua) and the root question. Cost is made proportional to what changed by a **dirty flag** (026's principle) and **frustum culling** (`Tick` keeps firing while culled, `Draw` does not), both **measured** through a new 019-style counter the suites assert on. The example addon is the maintainer's scenario and is **entirely event-driven** — watch the smelter's window appear, stand it on the smelter; the server closes it, swap in your own panel from the contents you snapshotted; it appears again, swap back — no distance checks and no timers, which is the shape 042 left the area in — tasks 044.1..044.9 (044.9 added by maintainer directive during the feature: a free entity over unloaded ground). A tenth, the client's own fade on a standing entity, was planned and then **dropped by maintainer directive**: the ramp reached a ghost and never a standing panel, through two independent publish channels, and neither failure reproduced outside the running client — nothing of it shipped
- 045-durable-places — DONE — **a place you stand something at is that place tomorrow**: a free `hafen.vr()` entity's place becomes its **durable anchor** (the server's grid id + the offset inside that grid) and the session coordinate becomes a **derived cache** the layer recomputes instead of trusting. The defect is one the maintainer verified in-game after 044.9: a session coordinate is re-based whenever the server drops the map (`MCache.invalblob` type 2 → `trimall`, then the same ground streams back under different grid coords), so walking into a cave or a house and coming out leaves the thing holding a number that has quietly stopped naming anywhere. Both forms already live in one type (`Position`, 039), so nothing is invented — what changes is which of the two the entity keeps, and three things follow from it. **(1)** The two edges swap: a place *recorded in another segment* is refused today by `LuaPosition.worldArg` and becomes legal, while a raw coordinate over *never-visited* ground is accepted today and becomes a **refusal** at `:add` and at `:position(p)` naming why — a hard cut, and the one line of 044.9's own archived suite it changes is stated rather than discovered. The common case does not move at all: ground you can see is ground that is streamed, and a streamed grid answers its own id. **(2)** An anchor this session cannot locate is legal and simply **waits, silently and forever** — no `Resolve` chain, because 042.12's lesson is that a retry chain wants one stable blocker and *the player has not walked there* is not one; the entity exists, answers `:position():info()` with the grid it was given, reports `:drawn()` false, and stands when that ground resolves with nothing else done to it. **(3)** Re-derivation rides mutation points, never a sweep: 044.9's `groundChanged` drain (which already walks the free list) gains one step in front of its ground test, and the coordinate space's own move gets the feature's single `haven` edit — one **guarded** `// addon:` line at `MiniMap.tick`'s `sessloc` assignment, guarded because `resolve` mints a `Location` every frame and an unguarded notify would be the per-frame poll 042 deleted. `<entity>:position()` answers the anchor form for a free entity, which is what makes the contract literal: `:info()` reads the same grid and offset across the walk while `:x()` is free to differ, and is nil while the cave holds you. Out: cross-session persistence (the addon's own business through `hafen.store`, which already round-trips a Position), gob-anchored entities (their place is the gob's), inventing a durable form for unexplored ground, and resolving an anchor through its **segment** rather than its own grid. **As built** (D-207/D-208/D-209): the gob is still created at `:add`, at the origin and outside the scene rather than deferred until the place resolves, so `attachScene` stays the ONE door in and `:drawn()` is false either way; the `(seg, tc)` equality test IS the tap, and it must listen to the **one** `MiniMap` the derivation reads, because the map window carries a second and either-instance dedup lets the earlier tick consume the change for the later; `<entity>:position(p)` was relaxed together with `:add` (one rule for both doors), which left `LuaPosition.hereArg` dead and deleted; and the guard needed a witness, so a **seventh pull-only counter** shipped — `hafen.client():profiling():entities()` → `{placed, waiting, passes}`, where `passes` climbs while you walk and holds still while you stand. The pages took the contract in the vr hub's own voice, and three pages the plan did not name (`api/conventions.md`, `guides/reading-the-world.md`, `api/vr/widgets.md`) carried the same "coordinates reset each login" model and were corrected with it — tasks 045.1..045.3
- 046-gob-scale — DONE — a NATIVE gob answers `:scale`, the verb its `hafen.vr()` siblings already have: a uniform in-place scale applied as a `Gob.SetupMod` attrib whose cached `Pipe.Op` makes the per-tick `GobState` compare propagate it with **no new `haven` seam**. Client-local, ungated, purely visual — the first WRITE on a read-only handle — and it **ends with the loaded object** by directive (re-apply on `GobAdded`); an addon that goes away leaves nothing distorted (design/16 §scale, D-194) — task 046.1. **As built** (D-210/D-211): a `GobScale extends GAttrib implements Gob.SetupMod` in the addon package, keyed by its own `attrclass`, with a `Location.scale(k)` minted once per VALUE — the cache is the mechanism, not an optimisation, because `Location` has no `equals` and a fresh op each tick would re-push every scaled gob's render state forever; `Pipe.Op.compose` returning the lone survivor itself is what makes the identity comparison exact. Writing `1` REMOVES the attrib, so "back to the original size" and "carries none of this state" are one sentence. The attrib records its writer and `UiApi.teardownGobScales` — one walk of the object cache beside `teardownGobOverlays` — is what pays for the ungating: nothing an addon that stopped running left distorted stays distorted. **The one deliberate divergence from its vr siblings is validation**: they clamp `0.01..100` because their factor arrives inside an options table with nowhere for a refusal to land, while `gob:scale(k)` is a direct argument on a direct verb and refuses `0`, a negative, a non-finite and a non-number naming the rule — a size is one of the few numbers whose wrong value does not LOOK wrong. Zero `haven` files touched, as planned. The suite went red once, on the suite: `hafen.world():gob():list()` hands back the client's own transient effect gobs (id -1, `OCache.Virtual` via `Skeleton`) whose handles `getgob` answers null for at once, and a `nil` read had been counted as "scaled"
- 047-flowermenu — DONE — `hafen.flowermenu()`: the radial right-click menu becomes readable and addressable — `:list()` hands back the petal **labels** (the set is frozen from `added()` to death, so entities would track nothing) with the position as identity (it IS the server's `num` and the `1`..`9` key, unlike `menugrid` where a position is an artefact), `:count()`, `:gob()` (the gob the menu belongs to — the server sends no such thing, so it is a CORRELATION the client makes: a consume-once `(gob, UI.lcc)` token, and because `lcc` moves on every press *before* dispatch, equality is a proof that nothing else was clicked in between, so an item- or Kin-opened menu answers `nil` without anything having to enumerate what else can open a ring, **D-214**; the attribution outlives the close and dies with the widget, so all three reads describe the same menu through the closing animation), and the gated `:select(label|n)`/`:cancel()` through `FlowerMenu.choose` so a client-side petal stays client-side; plus `FlowerMenuOpened` (end of `added()`, the only point where `addVoicePetal` has finished) and `FlowerMenuClosed` (the `uimsg` seam, which a client-side `BuddyWnd` menu also takes, with `destroy()` as the fallback that keeps every Opened paired to exactly one Closed). `hafen.act():flower` is NOT retired — maintainer directive, separate planned task (design/09, 25) — tasks 047.1..047.3 (3/3)
- 048-act-dissolved — DONE — **hafen.act() is dissolved**: the one section grouped by PERMISSION rather than by what it acts on becomes verbs on the things they change (D-187 generalised as **D-215**: a verb lives with what it CHANGES, not with what it COSTS — a permission is an attribute like being slow or being client-local, and none of the others earned a namespace). `moveTo`/`clickGob`/`useItemOn`/`item`/`place`/`select`/`raw` land as `hafen.player():move(p)`, `gob:click(button, mods)`, `hafen.player():hand():use(item|position|gob)`, `item:use(mods)`/`:take()`/`:drop(n)`/`:transfer(n)`, `hafen.world():place`/`:select` and `widget:send(msg, ...)`; `:menu`, `:flower` and `:enabled` are **deleted, not moved** — 023 and 047 already owned the first two doors (open D-103 obligations, and **no path door is built**) and the third only reported the caller's own manifest (**D-220**), which is what made the section *dissolvable* rather than merely smaller. `pag:use()` gained the gate it never had (**D-219**: a verb that COMMITS a real server action is protected wherever it lives) and `widget:send` **deleted an address space** rather than rehousing it — `raw`'s bare id and its `"mapview"`/`"gameui"`/`"root"` tokens are each an ordinary handle already, which the suite asserts rather than argues. The held-item verbs moved onto a **Hand** that is `nil` when the cursor is empty (**D-218**: the receiver must BE the message's implicit subject and be absent whenever it is), which is what makes the gesture guardable and what adds the one new message on the wire, `hand:use(gob)` carrying `iteminteract`'s `clickargs`. Nothing about the permission MODEL changed (D-027/D-028 hold, the declaration is still `"permissions": ["actions"]`) and nothing about the wire changed: every verb sends byte-for-byte what it sent from `act`. **As built**, four rules the rest of the API inherits: a moved verb retires under BOTH of its field reads (**D-216**, the colon one being the only spelling a shipped addon ever wrote and the one with no coverage at all); on a departed thing a client-local write is inert while a SERVER write RAISES (**D-217**); the gate runs before the argument check (**D-213**), so every argument refusal on a protected verb is invisible to a suite that declares nothing and has to be a `[manual]` line; and an argument that LEAVES the client is never coerced (`type() != TSTRING` — `act():raw` shipped with that hole, and `send(42)` would have put `"42"` on the wire as a message name). The adjective went with it: `gated` → **protected** across `src/` and the docs tier, where it is **two** substitutions rather than one (`ungated` carries the retired word) and every renamed group heading MOVES AN ANCHOR. The docs tier was the last task: `api/act.md` deleted, the permission stated once in `conventions.md` and in full in the actions guide, every verb documented on the page of what it changes — tasks 048.1..048.8 (8/8)
- 049-css-selectors — DONE — the selector grammar becomes **real CSS**: the space is a **descendant combinator** (`window[title=Cupboard] inventory`), so the ancestor test moves out of `[title=]` and every attribute reverts to testing the widget it is written on (`Selector.windowTitle` deleted, CSS-pure) — `[title=]` off the `window` role and `[text=]` on it are **parse errors naming the other**, so the two keys are disjoint by construction and the retired spelling throws instead of silently missing; a new `[text=]` rides `LuaWidget.text`; the four CSS operators `=`/`*=`/`^=`/`$=` replace the implicit "title is exact, res is a substring" rule; and `w:find(sel)`/`w:all(sel)` search one subtree, the only correct answer inside an `appear` callback. All three consumers share the parser, so `:find`/`:all`, `:on()` and `hafen.ui.skin` keys migrate together, with `atlas`/`bags`/`cupboard`/`widgetstack`/frozen `hello` and the docs tier swept (design/22, whose "no descendant selectors, no operators" scope line this reverses)  Shipped: **D-221** greedy right-to-left matching, sound only while every combinator is descendant · **D-222** a retired spelling refused at PARSE time naming its replacement · **D-223** the two keys disjoint at BOTH ends, since a grammar rule alone left `*[text=Cupboard]` matching a window · **D-224** `find` refuses an ambiguous answer (the classifier's *never a wrong answer in place of no answer*, reaching the lookup beside it) · **D-225** a scope narrows the CANDIDATES, never the selector · **D-226** a departed subject REFUSES at both scoped doors where every flat read answers nil/empty · **D-227** the caption seam hands over the WINDOW, so both caches act on its subtree. The inspector builds chain candidates by mixed-radix enumeration over grammar KEYS and offers `find(...)` only where the candidate names the widget ALONE; the docs pass closed the boundary lists the feature falsified — tasks 049.1..049.5
