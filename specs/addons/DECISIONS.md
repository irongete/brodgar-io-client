# Decision Log (ADR-lite) — index

> The decisions (D-001..D-108) live in `decisions/`, split by category — the same shape
> as `learnings/`. **Never read the whole set**: each line below says which D-numbers a file
> holds; open only the entry you need (every `### D-xxx — <title>` header doubles as its
> one-liner — `grep -h "^### D-" decisions/*.md` lists them all). **Adding a decision:**
> append the full entry to its `decisions/<category>.md` file; add a line here only when a
> new category file is created.

- [filesystem-build](decisions/filesystem-build.md) — addon/savedata dirs, jars via ant, JSON formats, saved-data scopes (D-001, D-002, D-003, D-014, D-015, D-016, D-023)
- [architecture-api](decisions/architecture-api.md) — engine layout, invasiveness (D-011), one canonical way, hafen.* namespacing, refs/handles, addon hotkeys unbound by default (D-047), package layout: addon system vs client features (D-048), profiling is one switch (D-049), profiling reads are snapshot tables / absent ≠ zero (D-050), profiling counters are pull-only (D-051), per-addon cost is the watchdog's own measurement split by category (D-052), per-widget cost is a self-clearing field on Widget (D-053), named render passes nest under the client's own draw part and report self time (D-054), profiling's own cost: control frames + per-tier model (D-055), arity is the verb — `hafen.x()` collection vs `hafen.x(key)` entity (D-056), a fixed-index collection iterates 1-based and the entity carries its own key (D-057), verify a subsystem has content before designing an API over it — there is no `hafen.music` (D-058), playback parameters are call arguments, not entity state (D-059), `:exists()` belongs to entities with a lifetime, not to name-keyed handles (D-060), the API's vocabulary comes from the engine, not from the genre (D-061), performance goes behind the existing surface as a cache, not in front of it as a handle (D-062), an entity's key is what the engine publishes about it, not what the genre calls it (D-063), an entity's identity is the key a NAME can address — a compound engine key collapses to it (D-093), an intern key follows the engine's OWN stability — a published id where the object is rebuilt, object identity where it is not (D-094), a read of a STORED world kicks the load and answers nil (D-095), a client-local id that a MERGE re-bases is a view, never a stored position (D-096), a ref-counted client toggle is a HOLD an addon takes and releases, never a switch it flips (D-097), a DERIVED resource is keyed by what it PRODUCED and bounded, because nothing else ends it (D-098), a CATEGORICAL cost claim is a property of the code's shape, never a branch inside the callback (D-099), the state belongs on the THING and a sweep is the shape of having put it elsewhere (D-100), where a name cannot separate two engine objects, publish the COLLAPSE rather than reaching for the id (D-101), the END of a derived thing rides the event its SOURCE already raises (D-102), an ABSORBED mechanism keeps one door — the old option is refused AND the old listing stops handing it back (D-103), an event's SCOPE follows its key's scope — owner-scoped where the name is private, broadcast where it is public (D-104), where the read COLLAPSES the event follows the KEY rather than the engine object (D-105), a queue whose handlers write back into it is drained ONE FRAME'S WORTH, never to empty (D-106), when a config table becomes chained setters the ACTION moves off the constructing call (D-107), a mechanism built for later tasks ships with a consumer or it ships unproven (D-108), an intern cache's key strength follows the population it keys (D-064), per-entity state a weak intern cache would lose must be derived, not stored (D-065), a thing that lives inside another is a relation on it, not a section of its own (D-066), forgive what the API will later understand; refuse what it never will (D-072), a loader hands back the file, not an interpretation of it (D-074) (D-008, D-011, D-012, D-013, D-020, D-022, D-047, D-048, D-049, D-050, D-051, D-052, D-053, D-054, D-055, D-056, D-057, D-058, D-059, D-060, D-061, D-062, D-063, D-064, D-065, D-066, D-072, D-074, D-093, D-094, D-095, D-096, D-097, D-098, D-099, D-100, D-101, D-102, D-103, D-104, D-105, D-106, D-107, D-108)
- [lifecycle](decisions/lifecycle.md) — in-game management, Reload UI, WoW enable/disable, load order (D-004, D-005, D-006, D-019)
- [security-sandbox](decisions/security-sandbox.md) — strict sandbox, two-layer watchdog (D-017, D-018)
- [widgets-ui](decisions/widgets-ui.md) — wrap-not-reimplement, hook priority, replacement descriptor, drops/g:resource/mods, WidgetNode + hit-testing, a classifier answers only where the widget IS the thing (D-067), a discovery primitive answers for what is already there (D-068), an addon's hide is authoritative — hiding a native window takes its toggle (D-069), a restore replays what the user was SEEING, not what the widget was (D-070), a relationship's lifetime is watched on the relationship, not on a bookkeeping object beside it (D-071), a read answers for the thing you point at — a site rule is not a widget's style (D-075), replace only what the engine itself would have built and restore its CONFIGURATION, not its identity (D-078), a background is bounded by the frame that sits on it (D-079), the margin belongs to whoever paints the frame (D-080), a rule's pixels are raw pixels — only a type size is scaled (D-081), a heterogeneous family of surfaces is named as a SITE, not as a role (D-082), the sheet replaces a surface the client paints — it does not invent one (D-083), a size-changing property applies where the surface can RE-LAY-OUT, and a panel cannot (D-084), an addon's layout is a LAYER over the client's, never a write into it (D-086), a position is not a toggle — two addons may layer over one widget (D-087), a property that is a WRITE is said where a widget is MATCHED (D-088), an undo removes a LEVEL, it does not empty the cascade (D-089), `pos` is the DEGENERATE anchor and its target is the PARENT (D-090), a derived position is re-derived by POLLING what it reads, and clamped by the client's own rule (D-091) (D-009, D-021, D-024, D-038, D-039, D-040, D-041, D-042, D-067, D-068, D-069, D-070, D-071, D-075, D-078, D-079, D-080, D-081, D-082, D-083, D-084, D-086, D-087, D-088, D-089, D-090, D-091)
- [actions-permissions](decisions/actions-permissions.md) — the actions tier and its per-addon permission (D-027 -> D-028) (D-010, D-025, D-027, D-028)
- [virtual-entities](decisions/virtual-entities.md) — client-only ghosts, handles, clickability, gizmo, placement snapping (D-029, D-030, D-031, D-032, D-033)
- [rendering](decisions/rendering.md) — hafen.render namespace, glTF 2.0 static subset (D-034, D-035)
- [network-data](decisions/network-data.md) — hafen.json, hafen.http + network allowlist (D-036, D-037)
- [fonts](decisions/fonts.md) — per-addon font system: private handles + owned overrides, a surface's look is the sheet's and a handle's colour is for your own pixels (D-073), a cascade level takes the properties it names, not the ones it does not (D-076), per-instance is a LEVEL of the cascade, not a mechanism beside it (D-077) (D-043, D-073, D-076, D-077)
- [process](decisions/process.md) — spec language, gap build order, a suite proves its own task ALONE and duplication beats delegation (D-085), a system's boundary is a DECISION on the page, not a gap in the code (D-092) (D-007, D-026, D-085, D-092)
