# STATE — what works right now

> Maintained by REPLACING (max 60 lines). Branch `feature/addons`; per-feature detail: its `NNN-` folder.

**ACTIVE: `039-uniform-api`** — the area-wide grammar. It merges the two entries 038's planning parked in `ROADMAP.md` ("collection objects instead of arity-as-verb" and "finish the OOP migration"), because either alone leaves the API half-uniform: uniform syntax over non-uniform returns is a veneer. **One rule** — a section is CALLED, everything after it is a `:verb()`, arity is the verb (`:x()` reads, `:x(v)` writes and chains), a collection is an object with `:add`/`:remove`/`:list`, and every read hands back a live object with `:info()` as the only snapshot. Measured surface: **3,115 `hafen.*` call sites**, 34 sections → 33 (`hafen.gob` folds into `hafen.world():gob()` by D-066), ~290 config-table sites, 14 new entities, 8 constructors de-tabled, 6 events re-payloaded. It also creates **one `position()`** — a Position value object that is computable *and* durable, deleting `gridPos`/`fromGridPos`/`marker:anchor()` and moving 38 arithmetic sites into the engine, because a grid is 1100 world units and `p.y + 22` is a bug at `y = 1095`. Planned in **16 tasks** (15 done, `039.16` next), each running in its own context: `spec.md` is normative (§2 is the grammar every later feature follows), `API.md` is the exhaustive before→after inventory and the source of the retired-name table, `plan.md` holds the build order and the measured risks, `tasks.md` is self-contained per task.

**039.1 DONE — the machinery, and the grammar is now four Java files rather than a paragraph.** `Section` (the per-addon singleton, minted in `installHafen` and handed back by identity), `LuaCollection` (`:list/:count/:find/:get/:add/:remove`), `Retired` (old name → a message naming its replacement) and `Args` (the `nil` discipline), applied end to end to the eight sections that carry no entities — `time slash json http hook timer log`, and `hafen.events` → **`hafen.event()`**, the first of the three plural renames. **The section stays a callable TABLE, never a function**, which is the whole reason a cut is legible: `hafen.time.clock` is a *field read*, so the refusal hangs off it and says `hafen.time():clock(…)` instead of failing three lines later as "attempt to call a nil value" — across 825 ported Lua sites that is the difference between porting an addon and hunting one. `hafen.log(msg)` → `hafen.log():write(msg)` is 641 of those sites, taken deliberately (§8.1): one exception in the busiest verb is the exception every reader meets first. **The finding that mattered is that 028.3 was wrong, and the whole `nil` discipline rests on it being wrong**: `narg()` *does* separate `f()` from `f(x)` when `x` is nil — a Lua caller writing `f(x)` emits one argument whatever it holds, and a table field is such an argument, which is where both measured hazards live. The one genuine hole is `f(g())` with a **zero-return** `g`, indistinguishable from `f()`; the page says so and the suite asserts **both** halves, including the hole, rather than claiming exactness. **D-107 — *when a config table becomes chained setters, the ACTION moves off the constructing call***: R4 turns `opts.headers`/`opts.timeout` into `req:header(name, value)`/`req:timeout(ms)`, which changes *when the object is complete* — the old path submitted it to a pool thread that reads those fields, so a `:timeout(5000)` written one character later was a data race whose loser is **silent** (a request that went out with the default looks exactly like one that took the setter). So a request is sent on the **next tick**, a setter after that throws, and one cancelled in the same call is never sent at all. **D-108 — *a mechanism built for later tasks ships with a consumer, or it ships unproven***: `LuaCollection` would have been unreachable from Lua in this task — its defining refusals (`#coll`, `coll[1]`, a string filter over nameless members) cannot be asserted without a section that has one — so `hafen.timer()` is both the section object and the collection of the addon's live timers. It carries no `:remove`: a timer ends with `t:cancel()`. **Not indexable is a design refusal, not a capability one** — userdata *does* take `__len` — and the falsification proved the message matters as much as the refusal: without `__len`, `#coll` still throws, with a sentence that guides nobody. Suite `039-uniform-api.1`: **23 pass, 0 fail, 0 manual**, and it needs one command and nothing else (D-085). Pre-checked headlessly **51/51** — including the shipping suite run end to end by pulling its handler off `Addon.slashCommands`, which needs an owner declaring **nothing** (`Manifest.internal` is the trusted REPL owner and passes every gate, so no refusal can be asserted through it). Three falsifications bit **1 / 3 / 2**. **Two lessons, both about what a green dry run does not prove**: 019.4's `name`-shadowing trap bit a captured *method parameter* this time and every message read `hafen.null(...)` — it compiles, it runs, and only an assertion on the *text* catches it; and a check that demanded a number from `hafen.time():clock()` was red headlessly and green in-game, because it was asserting the session's existence rather than the task's claim (it now asserts the routing, 6/6, and prints the clock). Also learned the hard way that a scripted port cannot see `pcall(hafen.json.parse, x)`: rewritten mechanically it fetches a method without its receiver and fails only on the error path, which is the path nobody runs. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**; link/anchor checker **0 broken over 1196 links / 75 files**, self-verified against a planted bad path and a planted bad anchor; `api/events.md` → `api/event.md` with 64 links repointed, and `conventions.md` rewritten as the statement of §2. No coverage owed.

**039.2 DONE — one `position()`, and it is a value that is computable AND durable.** `hafen.world()` is a section, `hafen.gob` folds into **`hafen.world():gob()`** (one read-only collection: `:get(id)` — still never nil — `:list/:count/:find/:nearest/:within`), `hafen.act()` follows, and `gridPos`, `fromGridPos`, `worldToTile`, `placeGrid` and `placeAngle` are **cut**: four position verbs became one, and the two placement reads were the same `MapView` fields as `options():interface():posGran()/:angGran()` with the units disagreeing. **The headline is measured, not described**: offsetting past the southern edge of the player's grid lands in a DIFFERENT grid id, and the same check asserts that `gi.y + dy` would have run past 1100 — the bug the engine now owns instead of every addon. **D-109 — *a value with two forms DERIVES, never converts, and offers no equality it cannot keep***: a Position holds either a session coordinate or a durable anchor and derives the other on demand, because converting eagerly would throw half of it away at the door (a place recorded in another segment has no session coordinate at all, and keeps `:info()`/`:durable()` while `:x()` answers nil). There is deliberately **no `__eq`** — the store round trip is `ul + (wx - ul)`, exact to the tile and not to the last bit of a double, so an equality over the numbers would call one place two, which is why the acceptance criterion says *the same TILE*. **D-110 — *a read that asks WHERE something is must not put traffic on the wire***: `MCache.getgrid` **requests** the grid it misses (up to five re-sends) and `p:durable()` runs in loops, so the live half of the lookup is `AddonWidgets.loadedGrid`, a plain lookup that answers null. **D-111 — *a PREDICATE about the stored world answers from memory, because "not yet" and "no" are one word***: D-095's kick-the-load model is right for a read that hands back data (nil reads as *ask again*) and a **lie** for a boolean, so the new one-line `MapFile.Segment.gridid(sc)` reads the coord→id map that already arrived with the segment — which is what let *"explored but not streamed IS durable"* be a plain check rather than a staged one. **`hafen.store` and `hafen.json` marshal a Position** and it comes back a Position, keyed by its own three-key wire shape; a non-durable one is **refused** by `encode` naming `:durable()` rather than written as `null`. Suite `039-uniform-api.2`: **20 pass, 0 fail, 0 manual**, plus the parked `kept` round after a `:reload` — **2 pass** — which is the only way to see the marshalling the store actually uses (a suite cannot watch its own reload). **The allocation claim came back better than parity and was measured, not assumed**: `gob:position()` is **209.7 bytes a call against 419.4** for the same read plus the `{x, y}` table it replaced. Pre-checked headlessly **33/33** (`Probe0392`), and the recipe is the keeper — a real `MapFile` over a 10-line in-memory `ResCache` with two adjacent grids, plus an `Unsafe`-allocated `GameUI`/`MiniMap` for `sessloc` and **no `MCache` at all**, so the RECORDED durability path is the only thing under test, which is precisely what an in-game round cannot isolate (the ground under the player is always streamed). Falsifications bit **3 / 1+abort / 1**, and the abort is worth the word: an unguarded `lua(...)` after a red check propagated out of `main`, so the checks behind it were never evidence either way. **Two lessons in the tooling.** `luac -p` **exits 0 on a syntax error** — the 35-file sweep read "0 failed" while `luac` printed a stack, and only a planted `x = = 1` caught it: check the OUTPUT, not the status. And 029.1's shadowing trap fires on **`arg`** too (`LuaValue.arg(int)`), which this time the compiler caught with a message about nothing at all — the forbidden-name list is *anything `LuaValue` declares*. A retired ENTITY method also needs its own `__index` (`Retired.methodIndex`): `LuaGob`'s metatable pointed `__index` straight at the methods table, so a removed verb read as plain nil however complete the retired table was. Ported: 8 suites, `atlas planner tagger walker`, frozen `hello` (v0.65.0). Docs: `world.md` rewritten around the Position, `gob.md` is now the Gob **object's** page, `act.md`/`conventions.md`/`store.md`/`json.md`, and a 30-file sweep leaving the retired-name grep at **zero**; link/anchor checker **0 broken over 1193 links / 74 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300 lines. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. Coverage: `specs/codebase/state.md` gained `getgrid`'s request behaviour and `mapfile.md` gained `Segment.gridid` — mapfile.md is now **74 lines, over the 70-line budget**, and its marker half is the natural split when a task next needs it. **Open for 039.15**: `gob:info().isplayer` keeps its snake case while the verb is `:isPlayer()`; `API.md` schedules no rename of the snapshot field.

**039.3 DONE — `gob:overlay()` is the COLLECTION, and the spec table is thirteen setters.** The four old arities are one refusal naming every verb that replaced them; `:list/:count/:get/:find/:add/:remove` is the whole door; `ov:pos()` -> **`ov:position()`**, a Position; `ov:tint(nil)` stays. **D-112 — *a builder that ATTACHES is attached INERT, and what it draws is the completion***: D-107 answered the *sending* case by deferring a tick, and an attachment cannot be deferred that way, because the caller configures it across statements and a frame may fall between any two. Holding the record out of the store until it is complete would restore the silent failure the grammar exists to delete — an overlay you forgot to give a kind would simply never appear, with nothing to read back and nothing to blame — so `:add(key)` attaches at once, `ov:kind()` is nil, `:exists()` is true, and the draw pass skips a record that has not said what it draws. *A half-configured overlay never paints* is therefore a property of the shape, not a rule; and because the events are queued (D-106) a handler still reads a fully configured overlay one frame later. **D-113 — *a setter that changes how the visual is BUILT rebuilds it; every other setter is live***: a config table was read once, so construction-only versus live never had to exist. With setters it does, and the three alternatives are all worse — requiring `:billboard` before the kind inverts the natural reading order, refusing it after makes that order illegal, and deferring the whole build moves *there is no map view* off the call site that caused it, which is exactly what 038's eager parse bought. So the two construction properties rebuild, and `:scale/:alpha/:tint/:rotate/:offset` apply to the live entity **and** are remembered on the record, so a rebuild comes back looking the same. **`FollowMoving.off` is live again** (038.2 left it *set once, at create*): `ov:offset(x, y, z)` moves an anchored thing where it stands, written to BOTH the entity's desired offset and the published attrib, because a ghost's create is deferred. **D-114 — *on a departed owner a creation RAISES and a removal is INERT***: they read symmetric and are not. A removal already happened — the overlay died with the gob, which is the feature's whole claim — so it is D-084's inert. A creation hands back something the caller is about to chain onto, and answering nil turns the next `:text(...)` into *attempt to index a nil value* one line later, the exact failure the retired-name table exists to prevent. Suite `039-uniform-api.3`: **32 pass, 0 fail, 1 manual**, `park` 1/0/1 and **`gone` 5 pass / 0 fail / 0 manual** — 3 of 3 parked objects despawned, `GobOverlayRemoved` fired for each, and all three **came back bare**, so 038's central claim is re-measured under the new shape rather than assumed to have survived it. Pre-checked headlessly **32/32** (`Probe0393`: the retired rows *and their message text*, the record's state machine, the collection shape over a stub Source), falsified against 3 planted defects for 4 red lines. **The clean rebuild earned its keep**: `ant` said BUILD SUCCESSFUL while `UiApi` still read `Attach.off`, a field that had become `screenOffset()` — the incremental pass hides a field becoming a method, which is precisely what de-tabling does. Also learned: a record read by the DRAW pass and written by Lua setters needs `volatile` fields *and* a paint filter that skips a kindless one, or a bare overlay reaches `gwrap.label(g, null, …)`; and 039.2's `Retired.methodIndex` lesson repeated on `LuaOverlay`, whose metatable still pointed `__index` at the methods table, so `ov:pos` read as plain nil. Ported and version-bumped: the four `038-gob-overlays` suites, `tagger` (the chain is now one statement), frozen `hello` (v0.77.0). Docs: `api/gob.md`'s Overlays section rewritten around the collection and the setters, plus 9 pages; link/anchor checker **0 broken over 1202 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page <= 300 lines; the retired-name greps at zero. **Filed to area `docs`**: `style-guide.md` §7 still lists `:offset(` as retired, where it is now live twice over (`p:offset` since 039.2, `ov:offset` here). No coverage owed.

**039.4 DONE — `hafen.map()` is five collections, and a grid STOPS BEING TWO THINGS.** `:segment()` (with `:current()`, the distinguished member the old zero-argument accessor had swallowed), `:grid()`, `:marker()`, `:icon()`, `:overlay()` — each minted once and handed back by identity — and **zero core edits**, five tasks in a row now. **The headline is that both halves of the world already keyed on the same number**: `WorldApi` publishes `MCache.Grid.id` and `MapApi` interns `LuaMapGrid` on the same `long`, so `hafen.world():grid():at(p)` and `hafen.map():grid():get(id)` hand back the SAME object and the two questions become `:live()` (streamed?) and `:exists()` (written down?), each door answering nil for what its own half lacks. 037.2's cross-check — recorded tile and live tile agreeing by name — stops being something a reader must remember to make and becomes a property of the object. Renames land with it: `:sc()` → `:segmentCoord()`, `:pos()` → `:position()` (a Position anchored on the grid's OWN id, so it is durable wherever the grid is known at all), `:mtime()` → `:modified()`, `grid:overlays()` → `grid:overlay():list()`, `seg:grids(area)` → `seg:grid():list(area)`, and **`marker:anchor()` is CUT because `marker:position()` IS it**. **D-115 — *a set that CANNOT be enumerated refuses to be, naming the two verbs that do work***: `hafen.map():grid()` and `seg:grid()` throw from `:list/:count/:find`, because the database is every grid the character ever walked and the three available answers are a **lie** (empty), a thousand disk reads (everything) or the refusal — which is also what `MiniMap` itself does, walking the rectangle it draws. It costs no machinery: `LuaCollection.Source.members()` throws and all three verbs propagate one message. **D-116 — *a ref-counted hold is not a property, so it gets verbs rather than an arity***: the four display switches become an entity (`:shown() :held() :hold() :release() :where() :what()`), because D-097's multiset means `(tag, false)` never could turn an overlay off and `(tag)` answered *the screen* rather than *your write* — arity was claiming a read/write pair that did not exist. A marker in **another segment** now builds its Position from the segment's own coord→id map (D-111's read), so it answers now where `:anchor()` waited for that grid's tiles off the disk; `marker:add(name, p)` creates the pin **bare** and `m:color(...)`/`m:onMap(b)` are read/write pairs that persist; and `hafen.map.icons`'s split-the-argument-by-shape heuristic (D-093) is **deleted** rather than renamed — `:get(res)` beside `:list(needle)` says which you meant, so no rule about slashes survives. Suite `039-uniform-api.4`: **22 pass, 0 fail, 0 manual**. Pre-checked headlessly **46/46** (`Probe0394`), and the recipe extends 039.2's by one step — the segment's private coord→id `map` reflect-filled, so the durable half answers with **no `MCache` at all**, which is the case an in-game round cannot isolate. Falsifications bit **7-in-1 / 2 / 1**. **The in-game round found two defects and BOTH were in the suite.** One asserted `grid:overlay() == grid:overlay()`, contradicting §2.3: a collection owned by an ENTITY is a *view*, re-derived per call, while only the section-level ones are singletons — identity belongs to the members. The probe missed it because it asserted the same wrong thing, and the rule was **absent from `docs/` entirely**, which is why it was possible to get backwards twice; it is now on `map/README.md` and `map/overlays.md`. The other was a `[manual]` asking for a state nobody can be in: `MapFile.update`'s `inout` records the 3×3 around the player on every move, so *streamed but not yet written down* lasts a fraction of a second — replaced by the invariant underneath it, checked over every streamed grid at once. **The falsification lesson repeats 037.2's at one remove**: planting *the tile CORNER instead of its centre* left the probe green, because `math.floor(x/11)` puts both in the same tile — a plant finer than the quantum proves the check is coarse, not broken. Ported: 7 suites, `atlas`, `planner`, frozen `hello` (v0.78.0) — and `037-map-database.1` was found still reading `m.name`/`found.seg`, snapshot fields that became objects in **037.2** and were never updated, so that check had been failing since. Docs: the six `api/map/**` pages rewritten plus 7 more; link/anchor checker **0 broken over 1203 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300 lines; the retired-name greps at zero. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. No coverage owed. **Filed**: the general statement of the view-vs-singleton rule belongs in `conventions.md`, which is at exactly 300 lines and is 039.15's §2 pass. **Filed to area `docs`**: `style-guide.md` §7's grep list still calls `hafen.world.gridPos`/`fromGridPos` live, where 039.2 cut them. **Not re-run this round**: the 7 ported suites, which are syntax- and probe-checked but not verified in-game.

**039.5 DONE — `hafen.ui()` is the section, and the ROOT it displaced is a verb.** `hafen.ui()` was the top of the client tree (36 sites) and is now the section object, so the tree's top is **`hafen.ui():root()`** and `find/all/node/at/mouse/inventory/equipment/hand/on` are colon verbs — one expression could not be both the namespace and a member of it. The Widget entity takes the grammar's spellings with it: `:pos` → **`:position()`**, `:rootpos` → **`:rootPos()`**, `:show`/`:hide` **CUT** for **`:visible(b)`** (R6 — a boolean property is a property, and that pair was the last place in the API where the value lived in the verb's NAME), and `:replace()`'s read half → **`:replacement()`**, the one place in the whole surface where a read and a write of one property do not mean the same thing (putting a view in place is an *act*; the thing standing there is a *replacement*). **The rename that is not a rename is `:position()`**: a widget's place is PIXELS, and now that a place in the world is a TYPE, `hafen.world():tile(w:position())` refuses *"screen pixels are not a place in the world"* instead of walking you somewhere that merely has the same two numbers — asserted through an **ungated** spatial verb, because `hafen.act():moveTo` meets the permission gate first and the gate is not the claim under test (TESTING.md's own rule, applied). **D-117 — *a section whose verbs move over SEVERAL tasks is mounted with the un-moved ones still on it***: `window`/`widget`/`overlay`/`skin` stay plain fields on the callable table, because they are not renames — 039.6 de-tables the builders and 039.7 makes the sheet a Sheet — and spelling 130 `skin{` sites `hafen.ui():skin{` now, then spelling all 130 again a task later, is churn that proves nothing. A field is found by `rawget` and never reaches `Retired`, so the two halves need no rule between them and the transitional state is the OLD code rather than new code written to be thrown away. **D-118 — *the retired-name table carries what THIS migration renamed, and nothing else***: `hafen.ui.root` stays plain `nil` though `hafen.ui():root()` is now live, because membership has to mean exactly one thing for the "a row in the inventory with no entry is a porting error" check to work — and frozen `hello` asserts that `nil` as a contract. Suite `039-uniform-api.5`: **30 pass, 0 fail, 0 manual**, verified in-game. Pre-checked headlessly **54/54** (`Probe0395`), and the recipe extends 036.3's to the HUD: an `Unsafe`-allocated `GameUI` + `Inventory` hand-wired under the root make `hafen.ui():inventory()` answer, so a suite that needs the HUD is dry-runnable — the shipping suite ran end to end at 30/0/0 before the client started and the in-game round printed the same 30. Falsifications bit **4+4 / 1 / 1**. **The port's lesson is that a verb that moves is a call site AND a field site**: the sweep of `hafen.ui.all(` missed `pcall(hafen.ui.all, s)` and three `pcall(wnd.hide, wnd)`, where the field is fetched OUTSIDE the pcall and the guiding refusal escapes the guard written to contain it — 039.1's `pcall(hafen.json.parse, x)` finding at corpus scale, found by a second grep with a word boundary and no paren. **Two defects the probe found were in the SUITE**: a hit-test on an owned *window* is unanswerable (`DefaultDeco.checkhit` owns the caption strip and the content area, not the transparent pixels between), so the probe is a BARE widget and the negative point can be the old top-left + 2 exactly as 036.1 demands; and 036.2's `named()` picker takes the first uniquely-titled window, which can be the suite's OWN probe — on an owned window the layout verb writes `c` directly, so `:position(nil)` is a no-op and the restore check reddens for a reason that is not the feature. Ported and version-bumped: 11 suites, `atlas bags theme walker widgetstack`, frozen `hello` (v0.79.0). Docs: 24 pages, `ui/README.md`/`widget.md`/`selectors.md`/`native.md`/`replace.md` rewritten; link/anchor checker **0 broken over 1209 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300 (`conventions.md` back to exactly 300); the retired greps and the 12 new spellings at zero. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. No coverage owed. **Filed to area `docs`**: `style-guide.md` §7 calls `hafen.ui.all` and `widget:replace()` live — the first is retired and only `replace`'s *write* survives; `:show()`/`:hide()` cannot join the grep list as bare names (live on ghosts, sprites, objects and icon categories) and need the `follow`/`offset` treatment.

**039.6 DONE — the three UI builders are built BARE, and a surface never paints half-configured.** `hafen.ui():window()`/`:widget()`/`:overlay()` take no arguments; the 13-key `opts` table is thirteen chained setters on the Widget they hand back — `:title :parent :position :size :font` and the eight `on*` callbacks — **each with a matching bare read**, so a callback is readable after construction and is no longer fixed at construction either. `pos` is spelt `position` (§2.7) and answers pixels. **D-119 — *a builder is attached INERT and skips its DRAW; holding it out of the tree buys nothing and costs a capability***: the deferred-attach version was built first and reverted, because it is strictly stronger on paper (not drawn, not hit-tested, not laid out) and that extra strength IS the defect — *find the widget I just built* stops working, which 039.5's own suite asserts, and `live()` treats *not under `ui.root`* as dead **and nulls the handle**, so every setter in the chain became a silent no-op needing a `pendingRoot` walk to paper over. So a surface is in the tree at once, paints nothing until `armPending()` clears its flag on the next `AddonManager.tick`, and the chrome is skipped by an **anonymous** `Window` subclass — anonymous because `LuaWidget.typeName` climbs past those, so `w:type()` still reads `Window` and every `@Window` selector, role and deco keeps matching. D-112's answer, one level up. **D-120 — *a set whose members have no key is a BUILDER, not a collection***: `hafen.ui():overlay()` MINTS where `gob:overlay()` and `hafen.map():overlay()` collect, because those key on an overlay key and a display tag and a HUD painter is anonymous — `:get()` has no question to answer, and a collection of anonymous members is a handle list wearing a collection's name. It ends with `:destroy()` (R7) like the two builders beside it, and the old handle's `:remove()` is a retired row. **D-121 — `:parent(w)` takes a WIDGET**, not `parent = "gameui"`: the last string in this section that named a widget, deleted rather than translated (032.2's rule), with the HUD reached as `hafen.ui():find("@GameUI")`. It answers while the surface is being built and refuses once on screen, naming `:position(x, y)` — re-homing a live window is a capability the config table never had. Suite `039-uniform-api.6`: **21 pass, 0 fail, 0 manual**, verified in-game. Pre-checked headlessly **45/45** (`Probe0396`, 039.5's fabricated `UI`+`GameUI`) plus the shipping suite end to end at 20/1 — the one red being the paint round, which needs a real draw pass. Falsifications bit **3 / 2 / 3**. **The lesson is where a timing claim can be measured at all**: the suite's slash command is dispatched BEFORE `ui.tick()`, so a surface built there is armed and painted in the same frame with the mechanism removed — the check passes and proves nothing. The discriminating site is a widget's own `onTick`, after `armPending()` has run, where the draw traversal (which restarts from `child` and so always reaches a new tail) would otherwise paint it that very frame; a clock widget counting frames and doing the building is the whole fixture. A staged round also needs a **deadline**, or a red line becomes a missing `[summary]` that reads like a crash. Also learned: eight callback slots are one `volatile LuaValue[]` replaced on write, not eight volatile fields — which makes the setters one loop over `AddonWidget.CALLBACKS` and the suite's proof two lines instead of twenty-six. Ported: 18 addons / 40 sites, all version-bumped (frozen `hello` v0.80.0). Docs: 17 pages, `api/ui/custom.md` rewritten around the builders; link/anchor checker **0 broken over 1206 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300; the retired-name greps at zero. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. No coverage owed. **Found in passing (039.5 escapes, both fixed)**: `widgetstack` still called `n:pos()`, and five docs pages still wrote `hafen.ui.on` — retired at 039.5, both throwing.

**039.7 DONE — the stylesheet is a DOCUMENT you edit, and a rule is a NAME for a level.** `hafen.ui.skin{…}` was the last config table in the section and the densest cut in the feature (141 sites), and it is the one R4 could not answer: a stylesheet is a selector → properties **map**, which has no chained spelling at all. So **D-122 — *a selector-keyed map becomes a document you edit, plus the two verbs that say whether it is in force***: `hafen.ui():sheet()` is the addon's one Sheet, `sheet:rule(selector)` a Rule interned per selector, its eight properties setters with matching bare reads, and `:install()`/`:drop()` the only pair that says whether the document is applied. **An edit to an installed sheet lands at once**, so there is no re-apply verb and "install again to change one rule" is gone with the table; whether it is installed is **derived** (`owner.skin != null`), because teardown drops a sheet without asking this object and a stored flag would already be stale. `sheet:load(rules)` keeps 036.4's headline — a whole theme for zero lines of Lua — as the one door for a whole document, and the suite re-proves it under the new shape: a `sheet.json` parsed and handed over **unmapped** lands the anchor 8 px in from the screen's bottom-right corner and the placed window's content box, **to the pixel**. **D-123 — *a handle to a level of a cascade is a NAME, not the record***: `LuaRule` holds only its binding, so `rule:remove()` ends the level while the handle goes on working (a setter says that level again), which is what lets one type serve both a matched sheet rule and `widget:rule()`, the hand-named level that replaced `w:skin{…}`. **D-124 — *where two spellings write ONE slot, the later SETTER replaces and only an unordered table refuses***: D-090 refused a rule saying both `position` and `anchor` because a table's keys have no order — setters have one, so the refusal is re-derived where it still applies (inside `:load`) instead of copied, and `rule:position()` answers nil while an anchor holds the slot. **D-125 — *a CLOSED vocabulary throws for an unknown verb where an open entity reads nil***: with the properties as verbs, D-072's "a misspelt property has no future meaning" had to move with them, or `rule:colour(…)` would read nil and fail one character later as *attempt to call a nil value*. The sheet key **`pos` is `position`** (the verb's spelling, in `w:style()` too), `hafen.ui.skin` and `widget:skin` throw naming their replacements, and with `skin` gone the callable table is empty: `hafen.ui` drops back to plain `Section.install`, retiring D-117's transitional mount one task after it was invented. Suite `039-uniform-api.7`: **33 pass, 0 fail, 0 manual**, verified in-game. Pre-checked headlessly **57/57** (`Probe0397`) plus the shipping suite end to end at 30/3, the three reds being the text-cache round, which needs a real draw pass. Falsifications bit **1+1 / 2+1 / 1+1**, and the middle one is the keeper: `load` made to MERGE reddened the probe and left the shipping suite green — not a probe win but a **hole in the suite**, since the in-game round is what the maintainer runs, so a falsification round is also a coverage diff between the two harnesses. **Two tooling lessons.** A fabricated `RootWidget` needs the **res jars** (`bin/*res*.jar`) or `ConsoleHost.<clinit>` → `Text` → `ui/fraktur` kills the probe with a message that fingers the resource system and whatever class was allocated last — 028.1's wall, met from a new direction. And a table-driven setter/read round must read each property **as it writes it**: writing all eight then reading all eight reddened `position` for a reason that was not a defect, because `anchor` legitimately took its slot. Ported and version-bumped: 16 addons (12 suites, `atlas`, `theme` + its `theme.json`, 036.4's `theme.json`, frozen `hello` v0.81.0). Docs: 13 pages, the six `ui/style/**` rewritten around the Sheet and the Rule with a new `#a-sheet-from-data` section; link/anchor checker **0 broken over 1209 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300 lines; the retired-name greps at zero; LuaJ parse sweep 40/40 (reading the output, not the status). Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. No coverage owed.

**039.8 DONE — four sections become collections, and a thing in the world takes its PLACE on the constructor.** `hafen.render():sprite()`/`:object()`, `hafen.ghost()` (the section object IS the collection, §2.1), `hafen.asset()` and `hafen.font()` — three config tables and two arities-as-verbs gone in one task. The three world entities get **one vocabulary**, every property a read/write pair on one name: `:position(p [, a])` (a Position, so `gob:position()` goes straight in), `:rotate :scale :alpha :tint :visible :clickable :onClick :exists`, plus `:image`/`:mesh`/`:res(name, spawnData)` and `sprite:billboard(b)`. The five "unchanged" verbs the plan listed were **write-only** and gained their read arity, which is what actually made them pairs. **The headline is measured through the API's own reads**: a sprite at `p` and one at `p:offset(11, 0)` stand ONE TILE apart, so a Position built by the engine, offset by the engine and handed back by the engine never becomes two loose numbers on the way. **D-126 — *a value handle is writable only while it is an unused DRAFT***: R4 turns `h:derive{size=12}` into `h:derive():size(12)`, which makes a FontHandle mutable where it was an immutable value — and it is shared twice over, interned per addon *and* read by each consumer at the moment it is handed over (`AddonWidget`'s ctor, `Sheet`'s rule set, `LuaGOut`'s per-call resolve). So a built-in refuses a setter naming `:derive()`, and a draft is SEALED the moment `FontHandle.resolve` gives it to anyone: both silent no-ops become different refusals, because they are different mistakes. Functional setters were built and rejected — indistinguishable at the call site from a mutating chain, so `h:size(12); use(h)` would silently use the unmodified handle, which is worse than the bug being fixed. **D-127 is the in-game round's finding, and it OVERTURNED my first answer.** I had built `:add(asset)` bare and let a setter place it, reasoning — in D-119's own words — that holding it out of the tree buys nothing, since a bare entity sits harmlessly at world origin. It does not: `addClientGob` is `basic.add(gob.placed)`, and `Placement()` resolves the **tile** under the gob, so at the origin the engine raises *"Waiting for map data..."* straight out of the Lua call that built it. D-119 is about a state the engine SUPPORTS; this was one it REFUSES — a world entity with no place is **unbuildable**, not inert — so §2.5's own rule decides it and `:add(image, p)` / `:add(res, p)` carry the place positionally. The world-origin state is now absent from the API and the pages. Separately, ground that is **explored but not currently streamed** is a normal case (a saved layout reloading as the map arrives) and there *not yet* is not *no* (D-095): the create catches `Loading`, marks the entity `pending`, and `RenderApi.armPending` retries on each addon tick. The anchored path was never affected and that is why every `gob:overlay()` suite stayed green — `applyEntityFollow` sets a `FollowMoving` before the add, so `Placement` takes its `flwxf` branch and never reads the map. Suite `039-uniform-api.8`: **52 pass, 0 fail, 0 manual**, verified in-game. Pre-checked headlessly **99/99** (`Probe0398`), and the recipe is the keeper: a `LuaWorldEntity` constructs directly and every verb guards on `gob != null`, so pulling the private `ghostHandle` off `RenderApi` exercises the entity's WHOLE vocabulary with no client — yet it still missed D-127, because the defect lived in the one call the probe could not make. Falsifications bit **3 / 1 / 1+abort**, plus one on the fix round. **Three lessons, all about a green run proving less than it looks.** 029.1's shadowing trap fired on a `final String name` parameter captured inside an anonymous `VarArgFunction`, so the dispatch fell through to its LAST branch and *every* font read answered `:italic()` — it compiles, it runs, and only asserting each property's VALUE catches it. A mechanical corpus sweep rewrites the very spelling a REFUSAL test pins: both regex ports ran over the suite being written and turned `refuses(function() return hafen.asset(ICON) end)` into a call to the live door, reporting `got: <no error>` — which reads exactly like an engine defect (039.5's `pcall(hafen.ui.all, s)` finding, one level up). And a retired row's message is asserted as TEXT, so widening a constructor is a two-place edit: three reds in round 3 with the engine perfectly correct, each pasted line showing the *right* replacement being named. Ported and version-bumped: 21 addons — 16 suites, `profiler`, `tagger`, `theme`, frozen `hello` (v0.82.0), and `planner` + its `gizmo.lua` by hand, where the gizmo now translates a Position to its own flat `{x,y,a,scale}` value in one place. Docs: 14 pages, `ghost.md` and the three `render/**` rewritten around the collections; link/anchor checker **0 broken over 1210 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300 lines; the retired-name greps at zero; LuaJ parse sweep **42/42**, reading the output rather than the status, self-verified against a planted `x = = 1`. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. No coverage owed. **Filed to area `docs`**: `style-guide.md` §7's grep list still calls `:offset(` retired — 039.4 filed it and it is live twice over (`p:offset` since 039.2, `ov:offset` since 039.3).

**039.9 DONE — the last four flat-ish sections become collections, and KEYLESSNESS takes away exactly one verb.** `hafen.kin() actionbar() menugrid() sound() buff() meter()` are `LuaCollection`s mounted through 039.1's own `Section.mount`, so `hafen.x(k)` → `hafen.x():get(k)` cost **no new machinery at all** — the six sources are ~20 lines each and every verb, refusal and message came from the shared type. Four entity renames ride with them: `kin:setGroup(g)` → **`kin:group(g)`** and `slot:set(res)` → **`slot:res(name)`** (R2 — the read already existed, so each pair was one name too many), `kin:endkin()` → `:endKin()` and `pag:isnew()` → `:isNew()` (N2). **D-128 — *a set the ENGINE owns and you cannot name is still a collection, and it carries no `:get`***: D-120 answered the keyless case with *a keyless set is a BUILDER*, and that is right for what the addon **mints** — an anonymous HUD painter has no key because nothing else claims it. The buff bar is the same question from the other side and comes out opposite: the addon mints nothing, the engine owns every member, and *how many buffs* and *is one of them poison* are real questions. So `hafen.buff()`/`hafen.meter()` keep `:list/:count/:find` and lose exactly `:get`, which throws naming `:find`. It also **had** to be that way — two buffs can share a resource and a `"ch"` uimsg replaces a live buff's resource under it, so a name is not an identity even for one instant. **The one correction the port forced is `menugrid:find(text)`**, which returned *every* match where §2.2 gives that to `:list(filter)` and makes `:find` first-match everywhere; the shared collection carries exactly one of each, so the plural search became `:list(text)` — and with it case-**sensitive**, like every other string filter in the API. `kin():get(<number>)` stays **never nil** (D-056's asymmetry, the same one `world():gob():get(id)` has) while `:get(<name>)` misses to nil, and `kin():add(secret)` hands back the **collection**: there is no Kin yet, the server decides and `KinChanged` reports it. `sound():get` and `:list()` deliberately address **different sets** — any clip the game owns (sound resources are not enumerable, so a Sound exists on demand) versus only what this addon still has in the air — which is why neither `:add` nor `:remove` exists there. Suite `039-uniform-api.9`: **17 pass, 0 fail, 0 manual**, verified in-game. Pre-checked headlessly **70/70** (`Probe0399`) plus the shipping suite end to end at **15/2**, the two reds being the only rounds that need a live HUD — written so the `got:` says *why* ("the catalogue is empty") and a client-less run reads as a precondition rather than a defect. Falsifications bit **2+1 / 1+1 / 1+1**, and **the third found a hole in the SUITE, not the engine**: deleting `requireActions` from `kin:group` left the gated round **green**, because the next line raises *"no Kin window"* — which names the same verb. Every gated verb has a second refusal that names itself, so the wanted text is now the permission clause (`"<verb>: this addon did not declare"`), not the verb. The probe caught it and the suite did not, which is 039.7's rule again with the harness the maintainer actually runs on the losing side. Ported: `hello` (v0.83.0) and `walker` (v0.12.0) — the only two consumers in the whole corpus, since no past suite touches these six. Docs: 15 pages, the six section pages rewritten around the collection and `conventions.md`'s three reference subsections folded into two; link/anchor checker **0 broken over 1213 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300 (`conventions.md` back to exactly 300); the retired-name greps and this task's four spellings at zero; LuaJ parse sweep **43/43**, reading the output. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. No coverage owed — the task read no `haven` source at all. **Filed**: the addon manifests' `description` blobs still carry pre-039 spellings throughout; every task in this feature has left them alone, and 039.16's audit is where they are either swept or declared history.

**039.10 DONE — the last `get`/`set` pair in the API, and a saved variable stops being a FIELD.** `hafen.speed():current()` / `:current(n)` collapses the fifth and last R2 pair on the sections, `hafen.client()` loses the API's one colon-on-the-namespace, `hafen.player()` becomes a section of one whose `:worldToScreen(p)` takes a Position and answers plain pixels, and the keybinding registry's `kb:get`/`kb:set` become **`kb:key(name[, k])`** — the pair R2 forbids, gone from the entity tier too. `hafen.act()` needed nothing: 039.2 had already shipped all ten verbs. **The headline is `hafen.store()`, the one section whose ACCESS PATTERN changed rather than its spelling**: a declared field (`hafen.store.cfg.foo = 1`, the line every persisting addon in the corpus wrote) became `hafen.store():get("cfg")`, and what `:get` hands back is the **live persisted table**, never a copy — a copy would go on accepting writes and quietly stop saving them, which is exactly the silent failure this grammar exists to delete. Proved across a `:reload`, and the proof carries a **stamp**: the file outlives the run that wrote it, so *the data is there* is not *this run put it there* (038.4's rule, one shape along). **D-129 — *a set closed at LOAD refuses an unknown key; a set the world fills answers nil***: D-125 drew that line over a value's verb set and it holds over a KEY set, the test being **when the set is fixed** — saved variables are read out of the manifest before the addon's first file runs and never grow, so an undeclared name is a typo and nothing else, and the refusal can enumerate the alternatives (*"Declared: `cfg`, `layout`"* is a fix, not a diagnosis). A kin roster or a menu catalogue fills while the addon runs, so there a miss genuinely means *not yet* and nil stays the honest answer. **D-130 — *where the retired NAMES are the addon's own, the refusal is built per owner***: §2.10's table is static data shared by every sandbox and cannot hold `hafen.store.cfg`, so `Section.mount` takes the callable table's `__index` as an argument and `StoreApi` builds one off the manifest, delegating everything else — registering each addon's names into the shared map instead would grow a process-global with every reload. The **18 option methods** gained §2.9's nil refusal, each carrying its own name for the message: with arity as the verb, an accidental `nil` was a write that silently became a read. Suite `039-uniform-api.10`: **21 pass, 0 fail, 0 manual**, plus the parked `kept` round after a `:reload` — **2 pass**. Pre-checked headlessly **69/69** (`Probe03910`) plus the shipping suite end to end at 15+4/2, the two reds being the only rounds that need a live client. Falsifications bit **1+1 / 3+1 / 1+1**, and the third is 039.9's lesson holding: with the gate deleted the next refusal names the same verb, so only the *permission clause* discriminates. **The probe's real find was a SHAPE assertion run in a loop over all five sections**: `hafen.player()` read `nil` for an unknown verb where every other section throws — its metatable pointed `__index` straight at the methods table, the **third** occurrence of that defect in this feature (039.2 `LuaGob`, 039.3 `LuaOverlay`), each found only because something asserted the refusal rather than the dispatch. The grep is `INDEX, methods`, and the eleven remaining hits are a renaming task's checklist. Also learned: the client **canonicalises** a keybinding's spelling (`Ctrl+Shift+Y` reads back `Shift+Ctrl+Y`), so a remap round trip compares tokens, not strings. Ported and version-bumped: 22 addons (13 suites, `atlas bags hogtest optionstest planner profiler theme walker widgetstack`, `planner/gizmo.lua` by hand, frozen `hello` v0.84.0). Docs: 25 pages, `speed.md`/`store.md`/`player.md`/`client/README.md`/`client/keybindings.md` rewritten; link/anchor checker **0 broken over 1214 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300 lines; the retired-name greps at zero; LuaJ parse sweep **44/44**, reading the output. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. No coverage owed — the task read no `haven` source. **Filed for 039.15/16**: the five option subsystem handles and `ProfHandle` still read `nil` for an unknown verb where D-125 says a closed vocabulary throws; widening them was outside this task's rows.

**039.11 DONE — the character sheet is four collections, and a set the server PARTITIONS is one entity with a READ.** `hafen.char()` carries `:attr() :skill() :credo() :experience()` (each minted once, handed back by identity) beside the scalars `:lp() :weight() :food()`, and `hafen.study()` carries `:slot()` and `:summary()` — ten flat readers into six new interned entities (Attr, Skill, Credo, Experience, Food, StudySlot), `skillsAvailable()` becoming `:skill():available()` (R8, §2.3's distinguished sub-list) and `comp` becoming `:composite()` (N1). **D-131 — *where the server PARTITIONS one set, one entity type covers it and the partition is a read***: known and buyable skills were two readers producing two SHAPES (`{name,res}` against `{name,res,cost}`), and credos were three (two arrays plus a differently-shaped `pursuing` table), so the same thing could not be compared with itself. Buying a skill or taking up a credo moves it between groups **without making it a different skill** — which is exactly the moment an addon cares — so a type per group would break `==` precisely there; the server agrees, partitioning with a `has` flag on one record. `:known()`/`:acquired()` now flip under a live handle, and the pursued credo compares equal to the same credo in `:list()`, its five progress reads being properties of that member and nil on every other. **D-132 — *an entity with no id of its own is keyed by what it DERIVES from, and does not exist until that resolves***: `SkillWnd` replaces `csk`/`nsk`/`ccr`/`ncr`/`exps` **wholesale** on each uimsg, so Java identity churns on data the addon did not change and a stashed handle would die on every resend — Skill and Credo key on the server's token, and Experience, which carries no token at all, keys on its **resource**, so a lore entry appears once that resolves rather than being handed out twice under two identities. The attribute key set is **closed**, which is D-129 applied one level down: `:attr():get("strength")` throws listing the nine, while a real name is **never nil** and it is `:base()`/`:composite()` that answer nil until the server publishes — `Glob.getcattr` auto-creates a zero entry for any name, so `base == 0 && comp == 0` is *unpublished*. `FepChanged` hands a **Food object** and `StudyChanged` an array of **StudySlot objects**, both minted per subscribing owner behind the `hasSub` gate; the adapters keep their snapshot diffs, because an interned object compares by identity and cannot detect a slot's numbers resolving, which is most of what they exist to notice. Two corrections to `API.md`'s entity sketch, made in place with the reason: an Attr has no `:value()` (a second name for `:composite()`), and Food's verbs are `:cap/:total/:feps/:hunger/:label/:efficacy`, which is the whole of what the flat reader published. Suite `039-uniform-api.11`: **17 pass, 0 fail, 1 manual**, plus the parked `gone` round **2 pass** — a curiosity taken out of the study window still answered `gfx/invobjs/bloodsoil` with `:exists()` false and was gone from `:find()`, which is the widget-identity claim measured rather than described, and the one thing here a program cannot set up. Pre-checked headlessly **89/89** (`Probe03911`) plus the shipping suite end to end at 9/8, the eight reds being exactly the rounds that need a live client. Falsifications bit **1 / 2 / 1**. **The in-game round found one defect and it was in the SUITE**: the `StudyChanged` check latched the FIRST payload, which at login is an **empty** array, so it went green having proved that a table is a table — 028.3's fake pass in the shape a *latch* gives it, where the precondition is not *am I in the world* but *does this particular sample carry anything*, and the first arrival systematically does not. It now latches the first non-empty payload, asserts the member is userdata answering `:res()`, and counts the empty ones so an empty-only session says so in `got:`; the probe could not have caught it, since no event fires headlessly. Also learned: **a LuaJ number IS a string** (`isstring()` is true for `LuaNumber`), so `:get(3)` reached the name test and reported a missing *attribute* rather than the wrong kind of key — caught only because the probe asserts each refusal's TEXT; and a falsification plant must sit where the value is COMPUTED, since editing the argument of an install-time call left both harnesses green and proved nothing. Ported: frozen `hello` (v0.85.0), the only consumer in the corpus. Docs: `char.md` and `study.md` rewritten around the collections and the entities, plus `types.md` (four shapes), `event.md`, `meter.md`, `http.md`; link/anchor checker **0 broken over 1224 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300 lines; the retired-name greps at zero; LuaJ parse sweep **45/45**, reading the output. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. Coverage paid: `specs/codebase/services.md`'s Skills row is now skills/credos/lore in full — the wholesale-swap rule, `Skill.has`/`cost`, the six `CredoGrid` pursuit fields and `pcr` being a separate instance — at 79 lines, over the 70-line budget, so the options half is the natural split when a task next needs it.

**039.12 DONE — the party and the fight resolve their GOBS, and 017's accepted regression ends in both places.** `hafen.party()` **is** the roster (§2.1 — a section of one thing IS that thing): `:list/:count/:find`, `:get(gobId)` and `:leader()` as the distinguished member (R8), every member handing back a live Gob with `member:gob()`. `hafen.fight()` is a section of four — `:maneuver()` (a keyless collection, D-128), `:deck()` (a plain array of DeckCards, R3's layout half), `:summary()` (a FightSummary entity) and **`:target()`**, whose `:gob()` names the creature you are fighting. 017 wrote *the combat target's gob becomes unreachable until Fight migrates — assumed and accepted*, and both halves of that sentence are now one verb each. **D-133 — *a second entity is worth minting only where it answers a question its target cannot***: handing the Gob straight back was the obvious shape and loses exactly one fact — `gob:exists()` says *is this creature in the world* and *are you still fighting it* is a different lifetime (a rabbit that runs out of view still exists; the fight can end while it stands there), so **Opponent** earns its place on `:exists()` alone. The same test then decides what stays OFF it: `opp:position()` would answer what `opp:gob():position()` answers, which is D-013's dual style — while `member:position()` passes, because a member's is the **last-known** place the roster keeps and it answers after the gob has gone. So an Opponent is four verbs and a paragraph saying so: who, and the gob answers the rest. **D-134 — *a member of a LAYOUT is keyed by its PLACE, not by what is standing in it***: `FightWnd.order[]` is fixed-length with reassigned entries, so loading another school rewrites every occupant while the slots stay put — and the slot index is what the write path sends. A DeckCard therefore interns on the slot, an emptied hotkey still reads `:slot()`/`:key()` with `:exists()` false, and `card:maneuver()` hands back the **same object** the maneuver collection lists, so the two projections of one window share their members instead of duplicating them. **D-135 — *a bag of scalars is an ENTITY when it is a window's fields and a plain read when it is derived***: `hafen.study():summary()` stays a table because it is the sum over its own slots, with nothing behind it to exist; fight's five are fields of a widget built at login and replaced on relog, so `:exists()` is a real question. N1 expands the client-programmer names (`:maxActions() :used() :deckSize() :saveCount() :activeSave()`) while `:info()` keeps them, and `sum:used()` equalling the sum of `man:used()` over the collection is a cross-check the suite makes. A Maneuver interns on the window's own record (the `avail` uimsg replaces the LIST but carries an existing entry over via `findact`), a PartyMember and an Opponent on the **gob id**. Suite `039-uniform-api.12`: **12 pass, 0 fail, 1 manual** solo, then **13/0/0** in combat — the target resolved `gfx/kritter/rabbit/rabbit`. Pre-checked headlessly **70/70** (`Probe03912`, which mints the three id-keyed entities directly and so exercises their WHOLE vocabulary on the departed branch an in-game round cannot reach), plus the shipping suite end to end at 7/3, the reds being the rounds needing a live character sheet. Falsifications bit **3 / 1+abort / 1 / 1+1**. **Two lessons, both about a suite that invites a SECOND run.** Its `[summary]` counted across runs — 25 over 13 printed lines — because the counters were file-level and initialised once at load, which every past suite hid by being run once per login; the reset belongs at the top of `run()`, not where they are declared. And the party `[manual]` never fired at all: the server sends a **one-member party list**, so `hafen.party():count()` is 1 while alone — 039.4's *a `[manual]` must be a state the maintainer can be in* from the other side, and the fallback branch is what made it harmless. Ported: frozen `hello` (v0.86.0), the only consumer in the corpus. Docs: `party.md` and `fight.md` rewritten around the collections and the entities, plus `types.md`, `conventions.md`, `gob.md`, `api/README.md`; link/anchor checker **0 broken over 1235 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300; the retired-name greps at zero; LuaJ parse sweep **46/46**, reading the output. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. Coverage paid: `specs/codebase/services.md` splits Combat into the live `Fightview` and the `FightWnd` deck builder — at **80 lines, over the 70-line budget**, so the audio half is the natural split when a task next needs it — and `state.md`'s Party row gains `Partyview.uimsg` `"list"`, which reuses the `Member` for an id that stayed.

**039.13 DONE — the last three flat sections become entities, and the decision the whole task was booked for turned on a premise that was FALSE.** `hafen.quest()` and `hafen.wound()` **are** their collections (§2.1), which retires the last two plural section names, and `hafen.craft()` is a section of one verb whose Craft carries the recipe *and* the gated `:make()` — the button belongs to the recipe. Four new entities (Quest, Condition, Wound, Craft) and two events re-payloaded. **D-136 — *a distinguished member reads NIL, and an inert entity would break every guard already written***: `spec.md` §4.4 and `plan.md` §4.5 both booked this as an open question because `hafen.craft.make()` with no window *"was a plain no-op"*. It was not — `ActApi.actCraftMake` **threw** *"no crafting window open (open a recipe first)"* — so nothing silent was being fixed and §2.2's own rule decides it: `:current()` is nil. What settles it beyond consistency is the cost of the alternative, which is measurable in the corpus: `if hafen.craft.current() then` is the line every crafting addon already writes, and an always-truthy inert entity turns each of those guards into one that **passes and then reads nothing**, which is the silent failure this grammar exists to delete, introduced by the fix meant to prevent one. D-056's `hafen.kin(<unknown id>)` precedent does not reach it: that entity is minted from a key the CALLER supplied, and an open recipe that is not open has no key at all. The guidance is not lost — it moves onto the retired row, which names the replacement *and* says `:current()` is nil while no recipe is open. **D-137 — *a sub-record is an ENTITY where its PLACE outlives the array, and a value where the whole set is replaced with its owner***, and the engine decided it rather than taste: `Box.uimsg("conds")` looks an objective up by `findcond(desc)` and **carries the record over** (`Condition.desc` is `final` — the server's key for an objective is its text), so `c:status()` flips from `"pending"` to `"done"` under a stashed handle and watching one objective is the reason to reach past the quest; `Makewindow.uimsg("inpop")` **destroys and rebuilds every `Input` widget even for a one-slot update**, so "input slot 2" means nothing a moment later. Condition is therefore an entity keyed by (quest id, desc) and CraftSpec a plain value (§2.8). Three corrections to `API.md`'s entity sketch, made in place with the reason: Craft has **no `:res()`** (the window publishes a name and no resource of its own, and `qmod` -> `:qualityInputs()` by N1); Quest keeps `:status()` rather than the engine's `:done()`, since a second differently-typed name for one property is D-013's dual style; and Wound regains the whole **tree** the sketch had dropped — `:parent()` resolves the old `parentid` to the Wound above it, which is the subsystem's one distinguishing fact. Suite `039-uniform-api.13`: **14 pass, 0 fail, 0 manual**, verified in-game with a recipe open — every `[manual]` fallback was a state the maintainer was already in, so none fired. Pre-checked headlessly **102/102** (`Probe03913`), and the recipe is the keeper: with no client every entity is on its **departed** branch, which is the half an in-game round cannot isolate (a quest you can see is a quest that exists), so the probe owns the vocabularies-on-a-dead-handle and the in-game round owns the tree. It also proved the gate order — `requireActions` fires **before** `Args.written`, which is right because arity is not a read/write discriminator on `make()` — by asserting the permission clause under a `Manifest.test` owner and the nil refusal under `Manifest.internal`. Falsifications bit **1 / 3 / 1**, and a fourth bit **0** honestly (the `w:parent()` root marker needs a live wound). **Two tooling lessons.** A plant that makes the value's own `toString` throw **kills the harness while it is printing the red line** — 039.2's abort at one remove, and the fix is to stringify `got` inside a `try`. And a `perl -0pi` plant inside a heredoc lost a backslash, so the substitution never applied and the round read "0 failed", which looks exactly like a check that cannot discriminate: 039.11's rule one step earlier — *before distrusting the check, grep the file and confirm the plant is actually there*. Ported: `hello` (v0.87.0) and `walker` (v0.14.0), the only two consumers in the corpus. Docs: `api/quests.md` -> **`quest.md`** and `api/wounds.md` -> **`wound.md`**, both rewritten around the collections and the entities, `craft.md` rewritten, plus 8 more; link/anchor checker **0 broken over 1238 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page <= 300 lines; the retired-name greps at zero; LuaJ parse sweep **47/47**, reading the output. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. Coverage paid: `specs/codebase/services.md` gains a Quest-log row and a Wounds row (the in-place mutation, the tab move, the `findcond`/`inpop` pair, `treesort` writing `level` on the UI thread, severity being a magnitude and not seconds) — at **82 lines, over the 70-line budget**, where the character-sheet half is the natural split. **Filed for 039.16**: STATE.md is now **62 lines, over its own 60** — the sixteen `039.x` entries collapse into one at the close, which is what that task is for.

**039.14 DONE — the Item entity, and the one migration that was genuinely open: an id the server RE-USES cannot be an identity.** `widget:items()`, `hafen.ui():hand()` and `EquipChanged` hand back an interned **Item** — `:res() :name() :num() :wear() :quality() :cell() :slots() :handle() :exists() :info()` — and `hafen.act():item` takes the object, with the number and the table both **refused**. **D-138 — *an entity addressed by a RE-USED id is keyed by the OBJECT, and its write resolves through the object***: D-094 asks what the engine does to the object, and here it does something no other subsystem does — it hands the id BACK (`UI.removeid` drops both map entries and the server may issue the same number next). So a handle keyed on that number does not go stale, which is the failure every other entity here can report; it **retargets**, and a gated write through it moves something the caller never named — silent, and destructive at exactly the tier the permission gate exists to guard. Keyed on the item, the resolve answers *this item* or *nothing*: a departed one still says what it WAS (which is what makes a stashed `onItemRemoved` payload worth holding) while `:cell()`, `:slots()` and `:handle()` go empty, because where it is is precisely what it no longer has. Measured end to end headlessly — id re-used by another item, the stale handle still naming the old one, the write refused, the new occupant untouched — and **not assertable from the suite**, which meets the permission gate first, so the suite asserts the observable half. **D-139 — *a container hands each item ONCE, and where it sits is a property of the item***: an `Equipory` draws one item per slot it fills, so the pre-entity list held two entries for one thing — harmless as copies, a lie once interned, since `#items` would count two of something you own one of and `==` would be true across them. `pos`/`slot` (a table in a backpack, a string in the equipment window — D-093's shape heuristic in the return position) became `:cell()` and `:slots()`. **D-140 — *a value only PUBLISHED CODE knows is read by class NAME, not by adopting the class***: `learnings/client-limits.md` recorded typed quality as absent, which is right about `haven` and stops one step short — the number is a `public double q` on the class inside `ui/tt/q/quality`. Adoption would pin a version whose miss answers null for **every** item silently; a named lookup can only stop finding the field, which is what `nil` already means. Amended in the learning, which now says the general thing: *the resources ship code too — before recording something as absent, read the `.res`.* **The in-game round found one defect and it was the ENGINE's**, in the half a probe wearing hand-placed gear never reaches: the equipment window publishes a name for **22 of its 23 slots**, so `:slots()` dropped the unnamed one and an item worn there read as *not worn* — `#slots == 0` being exactly what that means. The tell was arithmetic inside a `[pass]` line (*18 items fill 18 slots*, with a two-slot axe among them, needs one item at zero), because the check asserted `filled >= #eq`, which **an under-report satisfies**. An unnamed slot now falls back to the engine's own identifier, the check reconciles (*every* item names ≥ 1 slot, offender in `got:`), and the plant reddens both harnesses instead of only the probe. Suite `039-uniform-api.14`: **10 pass, 0 fail, 1 manual**, plus the parked `moved` round **2 pass** — the maintainer's own item, gone and still `gfx/invobjs/herbs/windweed`. Pre-checked headlessly **60/60** (`Probe03914`), and the recipe extends 039.5's to real item containers: place an item with the container's own **`addchild`**, never `add()`, because both keep a `wmap` that `cdestroy` reads back unguarded — an item placed the short way NPEs at the exact moment the staleness test needs it. A resource's published class was **fabricated** too (a 12-line stand-in under the real package name), which made the quality preference an ordinary assertion with no resource loaded. Falsifications bit **2 / 1+1 / 2 / 2+1 / 2+1**. Ported and version-bumped: `hello` (v0.88.0), `bags` (v0.8.0), `walker` (v0.15.0) — the whole corpus, since only three addons read an item. Docs: `api/ui/items.md` rewritten around the object, plus `types.md`, `act.md`, `conventions.md`, `event.md`, `ui/widget.md`; link/anchor checker **0 broken over 1242 links / 75 files**, self-verified against a planted bad path AND a planted bad anchor; every page ≤ 300 (`conventions.md` back to exactly 300); the retired-name greps at zero; LuaJ parse sweep **48/48**, reading the output. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. Coverage paid: `specs/codebase/state.md` gains the item-container rows (the `WItem`-per-slot rule, the unguarded `cdestroy`, the cursor item being a real bound `GItem`, quality's published class) at **54 lines**, and `widgets.md`'s destroy gotcha gains the sentence D-138 rests on — the id goes back to the pool.

**039.15 DONE — the sweep, and the demolition that turned out to be a NO-OP.** The cross-cutting pages (`api/README.md`, `conventions.md`, `types.md`, `event.md`, the landing table, `getting-started.md`, `runtime.md`, `examples.md` and all nine guides) plus area `docs`'s §12 run over the whole tree. **§4.9's three transitional markers were never planted**: `git log -S "SUPERSEDED by D-044"` shows the string only ever entered 017's own `plan.md`/`tasks.md` and 039's `spec.md`/`tasks.md`, `D-012`/`D-013`/`D-022` carry a plain ✅, and the file 017 aimed them at (`API-REFERENCE.md`) was deleted wholesale long since — so a *delete X* task begins by proving X exists, which is one `git log -S`. What HAD survived was the demolition's **substance** in `docs/`, and that is what was fixed: `conventions.md` still told a filter predicate it gets "a snapshot table in the flat sections", `event.md` still said "radar categories". **Nine defects fixed that none of the six checks can see**, because they are prose, link text and snippet SHAPE — the sharpest being `actions-and-permissions.md` teaching `hafen.act():moveTo(p:x(), p:y())`, which **raises** (`LuaPosition.worldArg` demands a Position: the page was unpacking the very type the feature created), plus `w:pos`/`w:hide` listed as live, two `onDraw = function…` fragments (the config table's shape one task after it was cut), `["*"]` selectors written as table keys, `get("toggle")` for `key("toggle")`, a Gob called a "live handle", `ov:tint{…}` where `tagger` writes `:tint(255, 200, 90)`, and two counts that disagreed with their own page. Two questions settled: `types.md` now states that **a snapshot field keeps the client's own wire spelling (`isplayer`, `mtime`, `qmod`) while the live read is the camelCase verb** — 039.2's parked question, and it turns twenty per-row reconciliations into one rule — and `conventions.md` gained 039.4's filed **view-vs-singleton** rule (a section's collection is one object, a thing's is re-derived; identity lives on the members). **The in-game round found an ENGINE defect and the maintainer chose to fix it here rather than file it, which is D-141**: `hafen.world():gob()` had two filter paths that disagreed on one argument — `:nearest`/`:within` skip a gob whose `Drawable` has not resolved, while `:list`/`:count`/`:find` **threw** on it — because `Source.needle()`'s `null` meant both *this KIND has no name* (D-115) and *this MEMBER's name has not arrived yet* (D-095). `Source.named()` now declares the kind, defaulting to the **refusing** answer so a source that forgets the pair fails loudly; 25 sources declare it beside their needle, the 6 without keep D-115 untouched, reconciled by count (26 ↔ 26). Live since **039.2** and uncatchable: no addon and no suite used a string filter on that collection, every consumer reaching for `:nearest`/`:within`, the path that was right — and it needed an unresolved gob in view at that instant, so the shipping suite can only ever say *it did not throw today*. **No page was wrong**; the fix changed no spelling. Suite `039-uniform-api.15`: **22 pass, 0 fail, 0 manual**, and it IS the paste — every Lua block printed on `conventions.md`, `getting-started.md` and the eight guides executed as written (two liberties: a handler is named so it can be fired, a free variable the prose binds is bound), then the uniformity asserted. Pre-checked headlessly **22/22** against a stub `hafen`, falsifications **1 / 1 / 3**; the engine fix has its own **8/8** Java probe calling `LuaCollection.keeps` reflectively with no client — the deterministic half the suite cannot be — falsified by restoring the old conflation from a file copy for **2 red**, one reproducing the maintainer's pasted error text verbatim. §12 over 75 pages: links/anchors **0 broken over 1243** (checker falsified BOTH ways), `wc -l` ≤ 300 **0 over**, headings **0** (slugs checked for `--`, not just the em dash), retired names **0 over 54 spellings** (falsified by planting `hafen.markers`), symbols **0** unregistered; wrap **0 over** with 35 inherited offenders re-wrapped; reachability BFS 74 pages at depth ≤ 2. LuaJ parse sweep **49/49** reading the OUTPUT. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**. No coverage owed. **Filed to area `docs` for the fourth time**: `style-guide.md` §7's grep list calls `:offset(` and `:anchor(` retired where both are live, and calls `hafen.ui.all`/`hafen.ui.node`/`hafen.world.gridPos`/`fromGridPos` live where 039.2/039.5 retired or cut them.

**`038-gob-overlays` DONE — `overlay` is the ENGINE's word for a thing attached to a gob, and the API finally spends it on one.** `Gob.ols`/`addol`/`findol` had a word the API had given to `hafen.ui.gobOverlay` — a *screen-space painter* that is not one — while the real per-gob attachment hid behind `follow = gob` in `hafen.render` and `hafen.ghost`: three mechanisms, one concept, none of them on the gob. **`gob:overlay(key[, spec])` is now the one way to attach anything to a game object and the one way to read what is already attached** — four arities, keyed per addon, one spec table covering **both spaces** (`{draw}`/`{text}` at the gob's projected point; `{image}`/`{model}`/`{ghost}` + `offset` in the 3D world), with the game's own overlays in the same read as `native = true`, read-only, and both superseded surfaces hard cut. **Four tasks, and exactly THREE core edits in the whole feature** — all of them 038.3's event seams. **The rule that carries it is D-100 — *the state belongs on the THING, and a sweep is the shape of having put it elsewhere***: `LuaGobOverlay` stopped being a stateless painter and BECAME the store on the gob's own attrib, which deleted the filter list, the throttled 5 Hz sweep, the per-frame re-match at the draw and the prune an addon-side `gobId → record` map would have needed — and made "an overlay dies with its gob" cost **no handler and no prune**, since `Gob.dispose()` disposes every `GAttrib`. **D-102 is the half that is not free**: a world-space overlay's visual is its own client gob in the scene and `OCache.remove` does **not** call `dispose()`, so the end of a derived thing rides the event its source already raises (`gobGone` from the `GobRemoved` drain, O(1) because the store is on the gob) — which is precisely the latent bug `follow=` had, a sprite anchored to a felled tree floating there forever with no owner. **D-101 was MEASURED, not assumed**: 13 of 33 decorated gobs carry two overlays of one resource, so a native overlay is a **union** over its resource name and the multiplicity is published (`ov:count()`) rather than reached for as `findol`'s id — which is `-1` whenever the server gave none, a number where every other key here is a name, and does not survive a re-add. **D-103**: an absorbed mechanism keeps ONE door — `follow=` is refused *and* `hafen.ghost.list()` stops handing back the overlay's raw handle, which would have carried a `:destroy()` that kills the visual behind a record still reading as attached. The events are **D-104** (scope follows the key's scope: yours are owner-scoped, the game's broadcast; a gob's death fires synchronously so an overlay is never reported dying after the thing it was on; teardown fires nothing), **D-105** (where the read collapses the event follows the KEY — the second `foo` is not an add, and a replace fires the removal AND the add) and **D-106** (a queue whose handlers write back into it is drained ONE FRAME'S WORTH, never to empty — otherwise a re-attaching handler hangs the *engine's* loop, which no watchdog catches). **The plan said two seams and the code needed three**: `addol`'s body, `Overlay.remove`, and **`Gob.ctick`'s expiry of a sprite that ended by itself**, which is how most of the game's overlays actually end — measured at 119–559 native events per session. **038.4 DONE — the close, and the churn is the part that had to be proved.** The example addon **`tagger`** is the feature's argument in one file (labels in screen space, a pinned icon in the world with the composed verbs chained, `:tagger read` printing yours beside the game's) and is also what the missing filter form looks like in practice: a `GobAdded` handler plus one loop. The design doc `design/24-gob-overlays.md` is written and `design/07`/`17` carry their supersession notes. Suite `038-gob-overlays.4` — **18 pass / 0 fail / 2 manual** before and after a `:reload`, `park` 1/0/1, and **`gone` 5 pass / 0 fail / 0 manual**: 3 of 3 parked objects despawned, `GobOverlayRemoved` fired for each, all three read bare and all three **came back bare**, which is the feature's central claim measured rather than parked. Four things the round taught, all in `learnings/testing-tooling.md`: **a suite cannot watch its own teardown**, so that one check legitimately spans two runs through the store (D-085's only exception here, and stated in the suite's header); **a cross-reload count of something the server owns measures the world**, so "we left the natives alone" is asserted as the *refusal* that makes touching one impossible; **"it still exists" is not the test for "it came back"** — the event is, and the round that both drives and observes one mechanism must announce its deliberate removals and swallow them; and **counters reset inline are reset in the wrong frame** when the events are queued (the dry run read 85 where 80 was expected). **The in-game round found exactly one defect and it was in the DOCS**: the suite runs `gob.md`'s and `custom-ui.md`'s snippets *verbatim*, and `gob:info().overlays` came back nil — the page claimed it "is still the raw list" where, like every `GobInfo` field, it is **absent rather than empty** when the gob carries none (`types.md` already said "optional"). Both were fixed: the page says so, and the check became 038.1's reconciliation invariant (`#info.overlays == sum(ov:count())`) taken on a gob known to carry natives — a type test on a gob that may carry nothing proves nothing. A stub cannot report a field the engine never sets, which is why 19/19 headless was green and the client was not. Clean `rm -rf build/classes` rebuild **BUILD SUCCESSFUL**; link/anchor checker **0 broken over 1183 links / 74 files**, self-verified against a planted bad path and anchor. No coverage owed: 038.1–038.3 already paid it into `specs/codebase/state.md` (47 lines, inside the 70-line subsystem budget), and 038.4 read no `haven` source at all.

**`032-replace-verb` DONE — feature B3b, the end of the plumbing: `hafen.ui.replace` is DELETED and replacement is a VERB on the entity.** `w:replace()` reads the view standing in for this window, `w:replace(view)` installs, `w:replace(nil)` undoes — arity as verb (029), over the ONE `Hidden` record 031.2 already keeps, so nothing new is stored: the verb fills in a field that exists. Two wrongs, one change: the namespace function was the **last place naming a window a different way** (030.2 killed `onWidgetCreate`, so the server descriptor survived only as its argument, D-012) and it was **privileged** — 031 made hiding a native window take its toggle, but only `replace` could bind a view to it. **The waiting is not part of the verb**: `hafen.ui.on(sel,"appear",fn)` already waits, already fires for what is already open (D-068) and already returns a handle. **032.1 began with the measurement** and the hypothesis held: `inventory[title=Inventory]` = 1 match, index #1 on a live HUD (`widgetstack`'s self-validating inspector; full table in the feature's `selector.md`), so the grammar needed **no extension** — and the live HUD gave three readings a hand-built tree could not: `@Inventory` adds nothing, the **role is wider than the class** (`inventory` = 4 vs `@Inventory` = 3, the `Equipory` classifies too, D-067), and `[title=Inventory]` alone = **32**, every widget *inside* the wrapper answering the refiner — which is the enclosing-window rule working and why the role part is load-bearing. **The verb's whole transplant is that it hides `nativeWindowOf(w)`, the ENCLOSING window** (replace the grid and the stock frame would stay around a hole) while `w:hide()` still hides exactly what you point at — a line the docs carry, or the pair reads as a bug. Four refusals, all **thrown** (a Lua call can, where the placement path could only log). **D-071**: *a relationship's lifetime is watched on the relationship, not on a bookkeeping object beside it* — `pollReplaced` sweeps the hide records with a view bound (`stillHidable`, gated on the `anyHidden` volatile), and its evidence was a maintainer-found bug: after a `:lua` replace, `:reload` restored the stock inventory **and left the custom window floating on top** — restoring is only *half* an ending, and the other half (`destroyWidgets`) lived elsewhere, invisible for a loaded addon and fatal for the `:lua` REPL owner, which survives a reload. **032.2's deletion ran further than the plan drew it**: `LuaReplacer`, the registry, the `onWidgetPlaced` branch, the registration scan and all four matchers went — a deletion, not a port, because D-068's *registration scans the live tree* already IS both old match paths — and **`LuaModel` went with them** (once nothing **constructs** one, `Addon.models`/`pollModels`/`teardownModels` are dead; the test that finds this is *"who still constructs one?"*, never a grep for the type name), cascading to `widgetTypes` → `descTable` → **the `UI.NewWidget.run` core seam itself**, so `haven` carries one `// addon:` line FEWER and the surviving seam is `onWidgetPlaced(id, wdg)`. Both addons ported in that same task or the client would not run: `bags` **v0.3.0** ARMS by subscribing and DISARMS with `watch:remove()` + `grid:replace(nil)`. **032.3 closed it, docs + Lua only.** The docs sweep is what the task actually was, and it taught that **a retired vocabulary hides in prose, not in the API tables**: the tables were rewritten with the code, so what survived was the descriptor's *language* in asides and examples that never name the function — "the container `replace` hands your callback", `hafen.ui.node(desc.id)`, the field list inside a "this is gone" note. `hafen.ui.replace` now appears **once** under `docs/addons/`, in an obituary that keeps the name and drops the vocabulary. `hello` **v0.67.0** asserts the verb's own contract inside the rounds `readToggle` already ran: the three arities, `replaceGone`, and **the hop proved from both ends** — install through the GRID, read the same view back through its enclosing window (`installedView` hops too), one record demonstrated rather than described. Pre-checked headlessly (5 scenarios / 24 checks on the sliced-out `readToggle`); the link/anchor checker over `docs/addons/` reports **0 broken over 557 links**.

**`031-window-lifecycle` DONE — feature B3a: a hidden native window stops coming back.** Observed with `bags`: the custom inventory **coexisted** with the stock one — Tab opened the client's too. The cause was never in `bags`: `MenuCheckBox` calls `setgkey`, so **the key and the menu button fire the SAME click**, and both land in the private `GameUI.togglewnd` (:1482), which flipped `visible` back on the very window the addon hid — **an addon's hide was a suggestion the client overruled one keypress later**. Seven call sites, **one private method**, its tick read by the private `wndstate` (:1475). **D-069**: hiding a native window **takes its toggle**, and 029's `Addon.hiddenNative` IS the ownership record — no new list, no new state, **no verb** (nothing to register, nothing to release; the same restore that gives the widget back gives the toggle back). The whole core edit is the promised **two `// addon:` one-liners INSIDE the two method bodies**, no visibility change and no new call site — the seam is `AddonWidgets.toggleWnd/wndState` → `AddonManager` → `UiApi`, so `haven` keeps one file that knows the addon layer exists. Ownership is matched by **widget identity against the CURRENT owners only** (`AddonManager.addons` + the `:lua` REPL), which covers every staleness case at once — a disabled addon, a relog, 030's fading corpse — so no per-frame liveness test is paid and a stale owner falls through to stock (a dead Tab is worse than a stock Tab). The frame path (`ACheckBox.state` is a per-draw `Supplier<Boolean>` on six checkboxes) **allocates nothing**: one volatile `LuaWidget.anyHidden` read when nothing is hidden, indexed loops otherwise. With nothing standing in for the window the toggle is **swallowed** and the tick answers `false`; with a view bound, `toggleWnd` does `show(!visible())` + raise/fit/focus and `wndState` answers **`view.visible()`** — no bookkeeping boolean, so the menu tick cannot drift from the screen. **`replace` wires itself** (031.2): it joins the one `Addon.hiddenNative` record `w:hide()` makes and binds its view to it — one record per window, `LuaModel` three fields lighter — because it is the only place that knows both halves. **D-070 is what the feature actually settled**: *a restore replays what the user was SEEING, not what the widget was*. `Hidden.origVisible` is **DELETED**; teardown, `:remove()` and a server destroy all restore as `view != null && view.visible()`, one rule with no branches — an addon whose custom inventory is open, then disabled, must leave the **stock** one open even though the wrapper it hid was hidden at the time. The two agree on the canonical unbound case, which is exactly why the old record *looked* right — **a remembered state that agrees with a derived one on every tested case is not evidence the state is needed**. Shipped behaviour change: a *bare* `w:hide()` on something that WAS visible now stays hidden through a `:reload` (correct — nothing stood in for it — and recoverable, since the toggle comes back with it). **One window, one owner** falls out of the same place: `w:hide()` on an already-owned native widget **throws** naming the first addon, `replace` **logs and skips** (the placement path must not throw into the engine). **Ordering became load-bearing**: `teardownHidden` runs **BEFORE** `destroyWidgets` — the rule reads the view's visibility and a destroyed view stands for nothing, a failure that would have been silent and always in the plausible direction. `fitwdg` is **re-derived, not exposed** (`UiApi.fitView`, over the view's own parent — the more correct frame), and the identity the seam rests on is **asserted, not assumed** (`assertToggleTarget`). **031.3 closed it, docs + Lua only:** `api/ui.md` needed no new section (031.1/031.2 each landed theirs as they shipped) — the close was the sweep plus two clauses, `replace`'s log-and-skip refusal and **what you own is what you point at** (hiding the grid leaves its window's key stock, which is what `hello`'s own `bags` hotkey does); `getting-started.md` gained the paragraph; the link/anchor checker over `docs/addons/` reports **0 broken over 551 links**. `bags` **v0.2.0** **KEEPS its hotkey with a changed meaning** — it ARMS and DISARMS the replacement, since Tab is the show/hide key now and nothing else can start or stop a replacement (the plan's "it may now drop it" was the right question with the opposite answer); its X restores an **open** stock window, because `reqclose` fires `onClose` before the view is killed. `hello` **v0.66.0** gained `readToggle(tag)` (login, +3s, `:hello wnd`): the swallow, the same-owner idempotence, the foreign-owner refusal **as the check's gate**, and **both halves of the teardown rule driven from Lua** through two live `replace` + `handle:remove()` rounds — then it restores the HUD **with the rule under test**. What Lua cannot do is press a key, so `:hello wnd swallow` **parks** the swallowed state for a human. Pre-checked headlessly by slicing `readToggle` out of the live `main.lua` and dry-running it under the LuaJ jar against a stub that models the engine's *rules* (5 scenarios / 18 checks), which caught a real bug: `(cond ~= nil) and w:visible() or nil` collapses a `false` to `nil`. Coverage paid across the feature: the new **`specs/codebase/gameui-windows.md`** (the toggle path, `Window`'s anim state machine — **`visible()` is animation-aware, so a fading-out window already reads false**, which is what lets a per-frame tick read a view directly — `GameUI.fitwdg`, and the correction that `show(boolean)` returns its own argument, not "did it change").

**Before that**, all DONE (detail in each `NNN-` folder): `037-map-database` (the RECORDED map: `hafen.map` on disk beside the live `hafen.world`, segments, grids, markers, masks and minimap drawings), `036-ui-layout` (position/size/anchor in the sheet, D-086..D-091, and a whole theme as a data file), `035-ui-chrome` (the sheet learns to DRAW: bg, border, pad), `034-ui-stylesheet-tree` (a tree key says WHICH widgets, not just what kind of surface), `033-ui-stylesheet` (ONE table says what the client looks like), `030-ui-selectors` (one way to point at a part of the UI — `hafen.ui` callable, a tiny CSS-shaped grammar parsed once into a predicate, `[title=]` resolving against the nearest ENCLOSING window and `@Class` an exact `typeName`; D-067's rule that a role is claimed only where the class IS that thing, and D-068's that registration SCANS the live tree so `appear` fires for what is already open; the inspector ships inside the feature, in `widgetstack`), `029-widget-oop` (the three objects `hafen.ui` handed back for one widget became ONE interned entity, `:info()` the snapshot hatch; reading stopped being coupled to replacing, so `widget:items()` answers on ANY container whose window is visible and interactive), `028-asset-loader` (callable `hafen.asset(path)` — ONE loader for an addon's own files, interned per RESOLVED path, typed by extension (image/font/mesh), `:type/:path/:dispose` + each type's verbs, sandboxed D-017; built-in fonts carry none of it, D-060; `font.load`/`render.image`/`render.model` GONE and the world builders handle-only, D-012; `RenderApi` −347 lines, zero core edits), `027-meters-oop`, `026-text-cache` (a per-addon content-keyed bounded LRU behind `g:text`/`g:atext`; 130 → 220-240 FPS on the harness; colour is a blit tint so it stays OUT of the key; D-062), `025-buffs-oop`, `024-audio-oop` (the Track section was built to spec and then CUT — this server sends no MIDI; D-058/059/060), `023-menugrid-oop`, `022-actionbar-set`, `021-actionbar-oop`, `020-kin-oop`, `019-profiling`, and 001–018.

## Engine & runtime
- **Engine**: LuaJ embedded (`src/io/brodgar/addon/`); per-addon sandboxed envs (D-017), instruction watchdog + soft per-tick CPU budget with auto-disable (D-018). **Loading/lifecycle**: `manifest.json` discovery from `addons/`; `:reload` rebuilds the addon layer only (D-005); enabled set persisted (D-006). Console `:lua` / `:addons` / `:reload`; AddOns panel (toggle/status). **Event bus** `hafen.event():on` (OnLoad/OnEnterWorld/OnUpdate/OnDisable, GobAdded/Removed (a Gob), MeterAdded/Removed/Changed (a Meter), Buff*/Fep/Study/Actionbar/Equip Changed, GhostClicked, ChatMessage…) — error-isolated, UI-thread-marshalled; timers `hafen.timer():after/:every`. Container item events are **not** on the bus — they are per-widget (`w:onItemAdded/:onItemRemoved/:onDestroy`, 029.3). **Store**: `hafen.store():get(name)` JSON saved-vars, per-char + account (D-023).

## The `hafen.*` surface — the docs tier IS the contract

- **One grammar** (039 §2 · `docs/addons/api/conventions.md`): a section is CALLED and is a per-addon singleton, everything after it is a colon verb, **arity is the verb** (`:x()` reads, `:x(v)` writes and chains), a set is a **collection** (`:list/:count/:find/:get/:add/:remove`), every read hands back a live interned object with `:info()` as the only snapshot, an explicit `nil` raises except where a page documents a meaning for it, a builder is constructed bare and configured by chained setters, a place is a **Position**, and every retired spelling **throws naming its replacement**. **33 sections**; the per-verb surface is `docs/addons/api/` and is deliberately not copied here.
- **Gated tier**: `hafen.act()` and the per-subsystem write verbs, behind the per-addon `actions` permission with enable-time consent and no global switch (D-027/D-028); `hafen.http` carries its own `network` host allowlist. Everything else observes, or writes client-local only and says so on its page.
- **Example addons** (12, installed, dormant until asked): frozen `hello`, `bags`, `theme`, `atlas`, `planner`, `tagger`, `widgetstack`, `profiler`, `optionstest`, `walker`, `netdemo`, `hogtest` — each described in `docs/addons/examples.md`.
