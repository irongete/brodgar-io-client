# STATE — what works right now

> Maintained by REPLACING (max 60 lines). Branch `feature/addons`; per-feature detail: its `NNN-` folder.

**`032-replace-verb` DONE — feature B3b, the end of the plumbing: `hafen.ui.replace` is DELETED and replacement is a VERB on the entity.** `w:replace()` reads the view standing in for this window, `w:replace(view)` installs, `w:replace(nil)` undoes — arity as verb (029), over the ONE `Hidden` record 031.2 already keeps, so nothing new is stored: the verb fills in a field that exists. Two wrongs, one change: the namespace function was the **last place naming a window a different way** (030.2 killed `onWidgetCreate`, so the server descriptor survived only as its argument, D-012) and it was **privileged** — 031 made hiding a native window take its toggle, but only `replace` could bind a view to it. **The waiting is not part of the verb**: `hafen.ui.on(sel,"appear",fn)` already waits, already fires for what is already open (D-068) and already returns a handle. **032.1 began with the measurement** and the hypothesis held: `inventory[title=Inventory]` = 1 match, index #1 on a live HUD (`widgetstack`'s self-validating inspector; full table in the feature's `selector.md`), so the grammar needed **no extension** — and the live HUD gave three readings a hand-built tree could not: `@Inventory` adds nothing, the **role is wider than the class** (`inventory` = 4 vs `@Inventory` = 3, the `Equipory` classifies too, D-067), and `[title=Inventory]` alone = **32**, every widget *inside* the wrapper answering the refiner — which is the enclosing-window rule working and why the role part is load-bearing. **The verb's whole transplant is that it hides `nativeWindowOf(w)`, the ENCLOSING window** (replace the grid and the stock frame would stay around a hole) while `w:hide()` still hides exactly what you point at — a line the docs carry, or the pair reads as a bug. Four refusals, all **thrown** (a Lua call can, where the placement path could only log). **D-071**: *a relationship's lifetime is watched on the relationship, not on a bookkeeping object beside it* — `pollReplaced` sweeps the hide records with a view bound (`stillHidable`, gated on the `anyHidden` volatile), and its evidence was a maintainer-found bug: after a `:lua` replace, `:reload` restored the stock inventory **and left the custom window floating on top** — restoring is only *half* an ending, and the other half (`destroyWidgets`) lived elsewhere, invisible for a loaded addon and fatal for the `:lua` REPL owner, which survives a reload. **032.2's deletion ran further than the plan drew it**: `LuaReplacer`, the registry, the `onWidgetPlaced` branch, the registration scan and all four matchers went — a deletion, not a port, because D-068's *registration scans the live tree* already IS both old match paths — and **`LuaModel` went with them** (once nothing **constructs** one, `Addon.models`/`pollModels`/`teardownModels` are dead; the test that finds this is *"who still constructs one?"*, never a grep for the type name), cascading to `widgetTypes` → `descTable` → **the `UI.NewWidget.run` core seam itself**, so `haven` carries one `// addon:` line FEWER and the surviving seam is `onWidgetPlaced(id, wdg)`. Both addons ported in that same task or the client would not run: `bags` **v0.3.0** ARMS by subscribing and DISARMS with `watch:remove()` + `grid:replace(nil)`. **032.3 closed it, docs + Lua only.** The docs sweep is what the task actually was, and it taught that **a retired vocabulary hides in prose, not in the API tables**: the tables were rewritten with the code, so what survived was the descriptor's *language* in asides and examples that never name the function — "the container `replace` hands your callback", `hafen.ui.node(desc.id)`, the field list inside a "this is gone" note. `hafen.ui.replace` now appears **once** under `docs/addons/`, in an obituary that keeps the name and drops the vocabulary. `hello` **v0.67.0** asserts the verb's own contract inside the rounds `readToggle` already ran: the three arities, `replaceGone`, and **the hop proved from both ends** — install through the GRID, read the same view back through its enclosing window (`installedView` hops too), one record demonstrated rather than described. Pre-checked headlessly (5 scenarios / 24 checks on the sliced-out `readToggle`); the link/anchor checker over `docs/addons/` reports **0 broken over 557 links**. **Next is C** — `hafen.ui.skin` → D window chrome over `Window.Deco` → E layout.

**ACTIVE:** `033-ui-stylesheet` — **feature C1a**. **033.2 DONE: `color`, and the settlement.** `color` is the sheet's second property (`{200,210,220}` or the keyed `{r=,g=,b=}` — `luaColor` now takes **both**, because the docs' own literal, positional, silently parsed to nothing: `hello`'s F2 per-call coloured `g:text` line had never been coloured, in shipped code) and it **stands alone** — a colour-only rule keeps the site's own font, so `Fonts.Spec` tolerates a null base. **The plan's mechanism was refuted by the client**: `color` was to ride the existing `Spec` field into `new Text.Foundry(f, col)`, i.e. the foundry's *default* colour — which reaches almost nothing, because nearly every routed site passes its colour **per render** (`Label` its `col`, every tooltip `Text.white`, every chat line the speaker's `TextAttribute.FOREGROUND`). Correct in the provider, invisible on screen. The answer is a marker on the provider's **own product** — `Text.Foundry.fixcol`, set only on foundries `Fonts.Spec` built, plus one substitution in `render(text,c)`/`renderwrap`, and the same idea at the rich sites (`ChatUI.fndcol(site)`, `RichText`/`MenuGrid` deriving `FOREGROUND`): **a rule's colour outranks the colour the SITE asks for.** Stock foundries never carry it, so the identity fast path and byte-for-byte teardown are untouched. **D-073** is what that forces: *a surface's look is the sheet's; a handle's colour is for your own pixels* — a `FontHandle`'s `color` is cut on a surface in the sheet **and** in `widget:setFont` (a native widget is a surface too), while it still colours `g:text` and your own widgets; `size`/`aa`/`bold`/`italic` travel everywhere, `color` is the one option that does not, because two answers to "what colour is this text" leaves the losing one spelled nowhere in the sheet. Three limits are stated rather than discovered: **`$col` markup still wins** (it is part of the *string*, not the site's choice — so a tooltip's green/red deltas survive), **`window.title` ignores colour** (`DefaultDeco` tiles a *texture* over the glyph mask — nothing there to colour), and while a rule is on, colour that carried *meaning* on that surface is **flattened** with the rest. A tree key's **properties are now validated** before the key is dropped — what C1a defers is resolving the key, not reading the rule. The two-addon stack needed **zero new code** (D-043 literally), verified with the `:lua` REPL as the second owner: last wins, dropping it falls back to the other and not to stock. `hello` **v0.69.0**: the eleven toggles became **property-scoped** (`setProp(key, prop, value)`, an emptied rule dropped) or `:hello chat` would silently wipe what `:hello color` just set — `:hello color` installs three rules at once (the `*` cascade, a colour-only `tooltip` rule, and `chat` carrying a **pink** handle that must come out **green**), plus a `readSkin` login check. Pre-checked headlessly (43 checks / 0 failed, including a **rasterised glyph** proving the sheet colour beats the caller's); docs landed with the code, link/anchor checker **0 broken over 581**. **033.1 DONE: the sheet exists and the font-scope API is gone.** `hafen.ui.skin{ [selector] = {props} }` is one table saying what the client looks like, applied live and owned by the addon; `hafen.font.setFont`/`reset`/`scopes` are a **hard cut** and read as plain nil (`hafen.font(name)` keeps its one job, D-060). The headline held exactly as planned: **C1a changed WHO FILLS the override stack, not how sites read it** — every routed site still calls `Fonts.foundry(scope, stock)`, **not one render site was re-routed and no drawing code changed**, so an eleven-surface feature is three tasks and the whole engine diff is a new `Sheet.java` plus deletions. Keys go through the **existing `Selector` parser**, so the grammar and its errors are `hafen.ui(sel)`'s and there is one vocabulary instead of a scope enum beside it. **The site/tree test is two lines and both are load-bearing**: any refiner (`@Class`/`[title=]`/`[res=]`) can only be answered by a *widget*, so it forces a tree key; and a refiner-less selector with no role can only have been written `*` (parse rejects an empty selector), which is what maps `*` to `"default"` with **no new field on `Selector`** — what is left is the bare role, and only `Fonts.isScope` ones name a site (`window`/`inventory` are widget roles with nothing behind them, D-067 from the other side). **D-072** is what the task settled: *forgive what the API will later understand; refuse what it never will* — a **tree key is silently inert** (C1b will resolve that exact key, so erroring would make the task boundary visible from Lua), while an unknown **property** is an error, because `fnt` is never going to mean anything and silence would leave a typo indistinguishable from a working rule. Three implementation rules earned their place: the sheet is **parsed before the old one is dropped** (a typo in rule five must not leave the client unstyled *and* erroring); the drop is **per-scope, not `Fonts.removeOwner`**, which also sweeps the per-instance `widget:setFont` registry — a convenience method written when the owner owned one kind of thing, whose name never changed when the second arrived; and a rule with no property pushes **nothing**, keeping the `active` fast path that makes an addon-less client byte-for-byte stock. `Addon.fontOverrides` became one `volatile Sheet skin`. `hello` **v0.68.0** keeps ONE `skinRules` table and re-applies the whole sheet on every toggle — which *is* the contract, exercised on all eleven surfaces instead of described — and rides an `@Inventory` tree key through `:hello font` to prove it is accepted and inert. Pre-checked headlessly (27 checks over `Sheet.parse`); docs landed with the code (`ui.md` gained the stylesheet section, `fonts.md` became "what each surface does"), link/anchor checker **0 broken over 574**. **Next 033.3** — docs (`fonts.md` rewritten around the sheet; the **property × key table**: what each of the eleven sites accepts, what it ignores, and the honest limits), the `theme` example addon carrying a `theme.json` (a theme with no code of its own), and the close. Then **C1b** tree keys + `widget:skin` (where a per-widget `color` returns, D-073) → C2 textures + `Window.Deco` chrome → E layout.

**`031-window-lifecycle` DONE — feature B3a: a hidden native window stops coming back.** Observed with `bags`: the custom inventory **coexisted** with the stock one — Tab opened the client's too. The cause was never in `bags`: `MenuCheckBox` calls `setgkey`, so **the key and the menu button fire the SAME click**, and both land in the private `GameUI.togglewnd` (:1482), which flipped `visible` back on the very window the addon hid — **an addon's hide was a suggestion the client overruled one keypress later**. Seven call sites, **one private method**, its tick read by the private `wndstate` (:1475). **D-069**: hiding a native window **takes its toggle**, and 029's `Addon.hiddenNative` IS the ownership record — no new list, no new state, **no verb** (nothing to register, nothing to release; the same restore that gives the widget back gives the toggle back). The whole core edit is the promised **two `// addon:` one-liners INSIDE the two method bodies**, no visibility change and no new call site — the seam is `AddonWidgets.toggleWnd/wndState` → `AddonManager` → `UiApi`, so `haven` keeps one file that knows the addon layer exists. Ownership is matched by **widget identity against the CURRENT owners only** (`AddonManager.addons` + the `:lua` REPL), which covers every staleness case at once — a disabled addon, a relog, 030's fading corpse — so no per-frame liveness test is paid and a stale owner falls through to stock (a dead Tab is worse than a stock Tab). The frame path (`ACheckBox.state` is a per-draw `Supplier<Boolean>` on six checkboxes) **allocates nothing**: one volatile `LuaWidget.anyHidden` read when nothing is hidden, indexed loops otherwise. With nothing standing in for the window the toggle is **swallowed** and the tick answers `false`; with a view bound, `toggleWnd` does `show(!visible())` + raise/fit/focus and `wndState` answers **`view.visible()`** — no bookkeeping boolean, so the menu tick cannot drift from the screen. **`replace` wires itself** (031.2): it joins the one `Addon.hiddenNative` record `w:hide()` makes and binds its view to it — one record per window, `LuaModel` three fields lighter — because it is the only place that knows both halves. **D-070 is what the feature actually settled**: *a restore replays what the user was SEEING, not what the widget was*. `Hidden.origVisible` is **DELETED**; teardown, `:remove()` and a server destroy all restore as `view != null && view.visible()`, one rule with no branches — an addon whose custom inventory is open, then disabled, must leave the **stock** one open even though the wrapper it hid was hidden at the time. The two agree on the canonical unbound case, which is exactly why the old record *looked* right — **a remembered state that agrees with a derived one on every tested case is not evidence the state is needed**. Shipped behaviour change: a *bare* `w:hide()` on something that WAS visible now stays hidden through a `:reload` (correct — nothing stood in for it — and recoverable, since the toggle comes back with it). **One window, one owner** falls out of the same place: `w:hide()` on an already-owned native widget **throws** naming the first addon, `replace` **logs and skips** (the placement path must not throw into the engine). **Ordering became load-bearing**: `teardownHidden` runs **BEFORE** `destroyWidgets` — the rule reads the view's visibility and a destroyed view stands for nothing, a failure that would have been silent and always in the plausible direction. `fitwdg` is **re-derived, not exposed** (`UiApi.fitView`, over the view's own parent — the more correct frame), and the identity the seam rests on is **asserted, not assumed** (`assertToggleTarget`). **031.3 closed it, docs + Lua only:** `api/ui.md` needed no new section (031.1/031.2 each landed theirs as they shipped) — the close was the sweep plus two clauses, `replace`'s log-and-skip refusal and **what you own is what you point at** (hiding the grid leaves its window's key stock, which is what `hello`'s own `bags` hotkey does); `getting-started.md` gained the paragraph; the link/anchor checker over `docs/addons/` reports **0 broken over 551 links**. `bags` **v0.2.0** **KEEPS its hotkey with a changed meaning** — it ARMS and DISARMS the replacement, since Tab is the show/hide key now and nothing else can start or stop a replacement (the plan's "it may now drop it" was the right question with the opposite answer); its X restores an **open** stock window, because `reqclose` fires `onClose` before the view is killed. `hello` **v0.66.0** gained `readToggle(tag)` (login, +3s, `:hello wnd`): the swallow, the same-owner idempotence, the foreign-owner refusal **as the check's gate**, and **both halves of the teardown rule driven from Lua** through two live `replace` + `handle:remove()` rounds — then it restores the HUD **with the rule under test**. What Lua cannot do is press a key, so `:hello wnd swallow` **parks** the swallowed state for a human. Pre-checked headlessly by slicing `readToggle` out of the live `main.lua` and dry-running it under the LuaJ jar against a stub that models the engine's *rules* (5 scenarios / 18 checks), which caught a real bug: `(cond ~= nil) and w:visible() or nil` collapses a `false` to `nil`. Coverage paid across the feature: the new **`specs/codebase/gameui-windows.md`** (the toggle path, `Window`'s anim state machine — **`visible()` is animation-aware, so a fading-out window already reads false**, which is what lets a per-frame tick read a view directly — `GameUI.fitwdg`, and the correction that `show(boolean)` returns its own argument, not "did it change").

**`030-ui-selectors` DONE — feature B2: one way to point at a part of the UI.** `hafen.ui` is **callable** (D-056): `hafen.ui(sel)` = the first match in tree order, `hafen.ui.all(sel)` = every match (empty array, never nil), `hafen.ui()` = the root — **`hafen.ui.root()` hard cut**. The grammar is tiny and CSS-shaped, parsed **once** into a predicate over one widget, no descendant selectors: `*` · role · `@Class` · `[title=]` · `[res=]`, refiners in any order and at most once. **The classifier is the heart** (`LuaWidget.role`, ONE `instanceof` chain) and **D-067** is what it taught: a role is claimed only where the class genuinely IS that thing, so the five promoted `Fonts.SCOPES` names that name a *render site* — `window.title`/`heading`/`tooltip`/`world.nick`/`world.speech` — stay valid grammar and classify **nothing**. Measured on a live HUD: **174 of 625 widgets classify**, `all("*")` = **0.08 ms / 625 widgets** — hence the docs rule *hold your result* (free, since entities are interned). Two rules settled inside the matcher: **`[title=]` resolves against the nearest enclosing `Window`** (the widget's own caption would make the most obvious selector anyone writes silently never match), and **`@Class` is an exact `typeName`**, no superclass walk (anonymous subclasses are the norm). **The spec's `[res=]` promise was refuted in-game**: NO window on this server carries a resource (69 of 625 widgets do — items, the 3 `IMeter`s, the chat channels whose code ships inside a res), so `[title=]` is the only key for windows; **`:res()` was NOT in the plan** and had to be added. **`hafen.ui.on(sel, "appear"|"disappear", fn)`** hangs off the `onWidgetPlaced` seam and hands back the **Widget entity**; **`hafen.ui.onWidgetCreate` and `LuaWidgetObserver` are DELETED** (the descriptor survives ONLY as `replace{match=fn}`'s argument, which matches against the SERVER parent). **D-068**: registration **scans the live tree** and fires `appear` for what is already open — "appear" means *is in the tree as of now*, a state reached either way, not an edge — which is why every `:reload` used to lose every open window; **`disappear` fires when a widget stops being *real*, not *drawn*** (a closing window lingers, unbound and fully readable, for its whole fade-out), so at `disappear` the entity is a **key to match**, never a last chance to read. Cost scales with widget **creation**: `Selector` splits into `matchesStructure` + refiners, so only a `[title=]`/`[res=]` selector queues a `PendingMatch`, re-checked **20 ticks** (`-Dhaven.addon.selrecheck=`). **The inspector ships inside the feature**, in `widgetstack` **v0.4.0**: hovering turns into role (or an honest `nil`) · class · `[title=]` · `[res=]`, then **every selector built from those parts that actually matches**, most specific first, each with its match count and this widget's index — **the list is SELF-VALIDATING** (a candidate survives only after `hafen.ui.all()` resolved it and found the widget inside), which forces the honest form (`hafen.ui("…")` only when it is the FIRST match). Its in-game surprise: **hovering a window's frame never gives you the window** — the chrome is a CHILD (`@DefaultDeco`, role `nil`); `[title=]` still resolves *through* it. **Zero core edits in the whole feature.**

**`029-widget-oop` DONE — feature B1: the THREE objects `hafen.ui` handed back for one widget are ONE interned entity**; **what you create and what you find are the same type**, `:info()` the snapshot hatch. The consequence that mattered: **reading stopped being coupled to replacing** — `widget:items()` answers on ANY container with its window **visible and interactive**, where the only previous door was `hafen.ui.adopt(id)`, which HID the native window. The intern cache is `WeakHashMap<Widget, WeakReference<LuaValue>>`, weak on BOTH axes (**D-064**), because a strong-keyed map over a whole tree would pin every destroyed widget and break D-041. **The measurement gate passed the wrong way round**: the OLD `nodeHandle` cost **513 ns** per node (a `LuaTable` + fifteen closures), an interned hit **27 ns** — interning is ~19x **cheaper** than the handles it replaced. Creation doors intern on the **ROOT** (`content != root` is the whole "is this a window?" test); geometry is **arity-as-verb** (`:move()` HARD CUT); **provenance (OWNED vs BORROWED) is DERIVED from the tree, never stored (D-065)**, with two distinct refusals and a write on a STALE widget a **silent chaining no-op**. **`adopt` is DELETED** (its whole job was a readable handle); what remains is `w:hide()` and its restore record. Items are a **relation** on their container (**D-066**), so `hafen.items` is a HARD CUT, `inventory()`/`equipment()` are widget LOOKUPS and `hand()` stays a snapshot; the lifecycle (`:onItemAdded/:onItemRemoved/:onDestroy`, a per-tick `WItem` diff) is **hasSub-gated by construction** — the subscription IS the registration. That is what let `replace`'s bespoke model handle be deleted: `fn(model)` receives the ENTITY. 029.1 also established that a real `haven.UI` **can** be built off-screen (recorded in `specs/codebase/boot-and-loop.md`). **Zero core edits in the whole feature.**

**Before that**, all DONE (detail in each `NNN-` folder): `028-asset-loader` (callable `hafen.asset(path)` — ONE loader for an addon's own files, interned per RESOLVED path, typed by extension (image/font/mesh), `:type/:path/:dispose` + each type's verbs, sandboxed D-017; built-in fonts carry none of it, D-060; `font.load`/`render.image`/`render.model` GONE and the world builders handle-only, D-012; `RenderApi` −347 lines, zero core edits), `027-meters-oop`, `026-text-cache` (a per-addon content-keyed bounded LRU behind `g:text`/`g:atext`; 130 → 220-240 FPS on the harness; colour is a blit tint so it stays OUT of the key; D-062), `025-buffs-oop`, `024-audio-oop` (the Track section was built to spec and then CUT — this server sends no MIDI; D-058/059/060), `023-menugrid-oop`, `022-actionbar-set`, `021-actionbar-oop`, `020-kin-oop`, `019-profiling`, and 001–018.

## Engine & runtime
- **Engine**: LuaJ embedded (`src/io/brodgar/addon/`); per-addon sandboxed envs (D-017), instruction watchdog + soft per-tick CPU budget with auto-disable (D-018). **Loading/lifecycle**: `manifest.json` discovery from `addons/`; `:reload` rebuilds the addon layer only (D-005); enabled set persisted (D-006). Console `:lua` / `:addons` / `:reload`; AddOns panel (toggle/status).
- **Event bus** `hafen.events.on` (OnLoad/OnEnterWorld/OnUpdate/OnDisable, GobAdded/Removed (a Gob), MeterAdded/Removed/Changed (a Meter), Buff*/Fep/Study/Actionbar/Equip Changed, GhostClicked, ChatMessage…) — error-isolated, UI-thread-marshalled; timers `hafen.timer.after/every`. Container item events are **not** on the bus — they are per-widget (`w:onItemAdded/:onItemRemoved/:onDestroy`, 029.3). **Store**: `hafen.store` JSON saved-vars, per-char + account (D-023).

## Read API (`hafen.*`), UI (`hafen.ui`), hooks, input
- **Gobs (OOP, D-044/045)**: `hafen.gob(id)` → an interned Gob (`:pos/name/health/facing/moving/speed/speech/icon/overlays/isplayer/distance/exists/id/info`); the flat `gob.*(ref)` table and the `"player"`/`"partyN"` tokens are GONE. `world.*` hands out Gobs. **Kin (OOP, D-056)**: `hafen.kin` is CALLABLE-ONLY — `hafen.kin()` = the roster of interned Kin (+`:find/:list/:add`), `hafen.kin(idOrName)` = one Kin (`:id/:name/:group/:color/:online/:exists/:info`; gated `:rename/:setGroup(0..254)/:endkin/:forget` chain on self); `KinChanged` = a per-addon `Kin[]`. **Kin ↔ Gob**: `gob:kin()` = the `ui/obj/buddy` attrib, `kin:gob()` = an `OCache` sweep **preferring the body**. **Actionbar (OOP, D-057)**: CALLABLE-ONLY — `hafen.actionbar(n)` = the Slot at the raw 0-based game index (0..143), `hafen.actionbar()` = the **1-based** array of all 144; `slot:index/:empty/:res/:name/:cooldown/:info` + gated `:use([mods])`/`:set(res)` (ASYNC — the server echoes it back). **Map** `map.*`: tile/height/grid/gridPos + conversions, grid ids = exact decimal strings. **Meters (OOP)**: `hafen.meter()` = every HUD bar (1-based, HUD order) / `hafen.meter(needle)` = the first whose server-published `IMeter.bg` name contains it; `MeterAdded`/`MeterRemoved`/`MeterChanged` carry the Meter. **Player/char**: `hafen.player()` → Player object (`:gob()/:name()/:worldToScreen()`, D-046); `char.*` (attrs/food/skills/lp/weight), `time/party/buffs/study.*`. **`hafen.items` is GONE (029.3)** — items are `widget:items()`. **Gap subsystems** (A1–A11) `markers/radar/kin/speed/craft/quests/wounds/fight` — widget-tree adapters. Overlays `ui.overlay`/`ui.gobOverlay`; `LuaGOut` draw wrapper.
- **Client options** `hafen.client:options()` → `interface/video/audio/camera/client/keybindings`, one per OptWnd panel over the stores OptWnd writes; arity is the verb (`opt:name()` reads, `opt:name(v)` writes + chains). `keybindings` = `register(name, fn)` (UNBOUND, D-047) + `get/set/list/unregister`; `hafen.key` GONE; slash `hafen.slash.register`.
- **Profiling** (019): Options ▸ **Client** ▸ "Enable profiling" = ONE switch (D-049), next-frame. `hafen.client:profiling()` → `:frame()`/`:history(n)`/`:reset()`; absent key = not measured, off ⇒ `{}` (D-050). **Pull-only counters** `:memory/:net/:loader/:render/:textcache` (D-051) answer **off**; `:addons()`+`:scope`/ `:measure` = the D-018 watchdog's OWN measurement (D-052); `:widgets()` = ONE `// addon:` `long[] prof` on `Widget` (D-053); `:passes()` = FIXED `shadow`/`scene`/`ui2d`, CPU+GPU (D-054; `:gl()` = ARMED-ONLY); `:overhead()` = five tiers, **0.54% of frame** (D-055).
- Hooks: `hook.input` (L1 pre-widget), `hook.action` (L2 outbound `UI.wdgmsg`), `hook.message` (L3 inbound). Discovery/replacement: **`ui.on(sel, "appear"|"disappear", fn)`** (030.2, D-068 — the entity, and registration scans the live tree; `ui.onWidgetCreate` GONE), `ui.replace` (bags replaces the native inventory **and takes its Tab/menu toggle**, restoring both on disable); `widgetstack` = `/framestack` **+ the selector inspector** (hover → role/class/title/res + every selector it has already RESOLVED). **Selectors (030, D-056/067/068)**: `ui` is CALLABLE — `ui(sel)` first match / `ui.all(sel)` every match / `ui()` the root (`ui.root()` GONE); grammar `*` · role · `@Class` · `[title=]` (the nearest ENCLOSING window's caption, exact) · `[res=]` (substring), no descendant selectors; roles = the `Fonts.SCOPES` names promoted (`"default"` → `*`, + `window`/`inventory`), of which five name a render SITE and classify nothing. **Widget entity (OOP, 029, D-064/065/066)**: ONE interned type from `ui.window{}`/`widget{}` (OWNED), `ui(sel)`/`node(id)`/`at(x,y)`/`inventory()`/`equipment()` (BORROWED) and `replace`'s callback — `:type/:role/:res/:id/:children/:parent/:pos/:size/:visible/:text/:items/:exists/:info/:walk/:at/ :rootpos/:setFont/:resetFont`, the OWNED-only writes `:pos(x,y)/:size(w,h)/:pack/:destroy`, `:hide()/:show()` on any widget (a native one carries the restore — and, if the client can open it, **its toggle**: 031, D-069/070), and `:onItemAdded/:onItemRemoved/:onDestroy` (hasSub-gated); `ui.hand()` is a snapshot (the cursor item is not a widget). `==` is the identity test (`:same()` GONE), liveness = `hasparent(ui.root)`, no pin (D-041), hit-testing mirrors the engine's own pointer dispatch (D-042).

## Actions (gated write tier), virtual entities, rendering, data, fonts, example addons
- `hafen.act.*`: moveTo/clickGob(gob)/useItemOn/place/select/raw, menu/flower, item verbs + per-subsystem verbs (`slot:use`/`:set`, speed.set, `kin:rename/…`, craft.make…; **`pag:use()`** ungated). Per-addon permission + enable-time consent, NO global switch (D-027/D-028); write addons default-disabled. **Testing**: `specs/addons/TESTING.md` — one self-checking addon per task (`addons/<NNN>-<feature>.<X>/`, `[pass]`/`[fail]`/`[manual]` lines pasted back), all of them re-running every login. `hello` is FROZEN at the coverage it reached through 033 and no longer grows. **Example addons (docs demos)**: `hogtest`, `bags`, `planner`, `widgetstack`, `netdemo`, `walker` (writes), `optionstest`, `profiler`.
- **Assets (028)** `hafen.asset(path)` — docs `api/asset.md`: the ONE loader for an addon's own files — interned per resolved path, typed by extension (image/font/mesh), `:type/:path/:dispose` + the type's own verbs, sandboxed (D-017); `font.load`/`render.image`/`render.model` GONE, and `render.sprite{image=}`/`object{model=}` take a **handle only** (D-012).
- **Ghosts** `hafen.ghost`: client-only virtual gobs (D-029..D-033) — clickable, oriented, grid-anchored layouts, transform gizmo. **Render** `hafen.render`: sprite (fixed + billboard world quads), glTF 2.0 static models — textures, materials, lighting (D-034/D-035); a SCENE namespace now, its files come from `hafen.asset`. **Data/net**: `hafen.json` (D-036); `hafen.http` async get/post, manifest `network` allowlist (D-037). **Fonts** `hafen.font(builtin)` (D-043): all 11 scopes + `widget:setFont`; a `.ttf` is `hafen.asset`, its size is `:derive{size=…}`.
