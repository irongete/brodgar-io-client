# STATE — what works right now

> Maintained by REPLACING (max 60 lines). Branch `feature/addons`; per-feature detail: its `NNN-` folder.

**Active:** `027-meters-oop` (027.1–027.2 DONE, 027.3 open) — the last flat widget-tree read becomes an OOP section: `hafen.meter()` = every HUD meter (the `place == "meter"` slot, not a hard-coded triple), `hafen.meter(needle)` = the first whose **server-published** `IMeter.bg` res name contains the needle; entity `:res/:index/:value/:color/:segments/:exists/:info` interned on widget identity; `VitalsChanged` → `MeterAdded`/`MeterRemoved`/`MeterChanged` carrying the object; hard cut of `hafen.player():vitals()` and the `Vitals` type. One `// addon:` core edit (`AddonWidgets.hudMeters` over the private `GameUI.meters`). **027.1 DONE** — `LuaMeter` (userdata + per-addon weak-valued identity `Cache`, the `LuaBuff` shape: `IdentityHashMap<IMeter,Ref>` + `ReferenceQueue` drained on every access) with `:res/:index/:value/:color/:segments/:exists/:info`, and the callable `hafen.meter` table (list / needle / number-key + empty-string errors / miss → nil). ONE shared scan `AddonWidgets.hudMeters(GameUI)` — the engine's OWN ordered `place == "meter"` list, not a `children(IMeter.class)` DFS whose order is a tree artefact — backs the list form, the lookup, `:index()`, `:exists()` and (027.2) the poll. That list was `private`, so the feature's single `// addon:` core edit is `private` → package-private on `GameUI.meters` (same-package `AddonWidgets` still cannot read a private field). Every read is `Loading`-guarded → nil (`bg.get()` throws until cached), only `:exists()` always answers, and a destroyed meter keeps reading with `:exists()` false. Colours are `AddonManager.color` → `{r,g,b,a}` 0..255, the `kin:color()` shape; `snapshot()` always sets `segments` (possibly empty), the rest unset when absent. Hard cut: `hafen.player():vitals()`, `readVitals`/`vitalsEqual`/`VITAL_KEYS`/`vitalsCache` all deleted; `VitalsAdapter` still compiles but fires NOTHING (`VitalsChanged` is dead until 027.2 renames it), `interested` narrowed to `IMeter` + `"set"`/`"col"`. `hello` carries an INTERIM edit only (`readVitals` → `readMeters`, the HUD overlay draws a bar per meter in `meter:color()`) — 027.3 owns the contract check, the handlers and the version bump, and the panel rows overlap the map-lock line while mounted. Verified in-game: the array matches the HUD bars in order with live values, and **mounting adds two bars the old positional triple could never see** (`häst`, `mount`) — the observed names are in `027-meters-oop/observed-res-names.md`, with the warning that `häst` is non-ASCII so the docs must advise an ASCII needle. **027.2 DONE** — `VitalsAdapter` → `MeterAdapter` (the `BuffsAdapter` split): **poll** diffs `LuaMeter.hud()` per tick for `MeterAdded`/`MeterRemoved` (the removal fires BEFORE the entry is dropped), **refresh** (`interested` = `IMeter` + `"set"`/`"col"`) re-reads the cached meters for `MeterChanged`; all three carry the **Meter object** through the new `AddonManager.fireMeter(String, IMeter)` beside `fireBuff` (`hasSub`-gated, per-addon interning, addons + the `:lua` owner), so an addon subscribing to none of them costs nothing. `VitalsChanged` is gone. The change key is the **segment array** (`LuaMeter.segments`), not `snapshot()` — `:value()`/`:color()` are its first entry, so one comparison covers both uimsgs and a multi-segment bar moving is a real change too; `:res()` is out of the key (leaving `Loading` renames nothing) and so is `:index()` (layout). The snapshot is a diff KEY only, never handed to Lua (`meter:info()` is that) — an `IdentityHashMap<IMeter, LuaValue>`, UI-thread-only, re-instantiated per session; membership is `==` everywhere. Engine order is refresh→poll and the poll seeds the cache at fire time, so a new meter surfaces as ONE `MeterAdded` with its values applied. Verified in-game: relog → one `MeterAdded` per bar (`hp`/`stam`/`nrj`, +2 on mounting), moving → `MeterChanged` on `stam`/`nrj` only with **no idle spam**, a stashed `MeterRemoved` payload still reads with `:exists()` false, `hello` regression clean. Known and owed to 027.3's docs: a payload can be younger than its resource — the bar added at mount fired while its `bg` was still `Loading`, so `:res()` was nil (`Meter(?)`) at fire time and answered a beat later. Prior feature: `026-text-cache` **DONE** (026.1–026.3) — killed the per-frame text re-rasterisation behind `g:text`/`g:atext` (`LuaGOut.drawText`: both paths ended render→tex→blit→dispose, ~0.28 ms/line per 019.8, ~90 FPS on the `hello` harness) with a per-addon content-keyed bounded cache of the rendered `Text`; colour stays out of the key (blit tint, F2); stats through `hafen.client:profiling()`. The ROADMAP's explicit-text-handle alternative is DISCARDED — it costs the same on volatile strings and is permanent contract. **026.1 DONE** — `LuaGOut.Cache`/`Key`/`Entry` + `teardownTexts`, one per `Addon` (`Addon.texts`), strong-valued (a cache, not an intern map) and bounded by BOTH entry count and texture bytes (provisional 512 / 16 MiB, one place, 026.2 tunes them); LRU = an access-ordered `LinkedHashMap`, eviction disposes, `hits/misses/evictions/entries/bytes` counters already in place for 026.2's reader. `bind` grew an `Addon` owner (`LuaWidget`, the HUD pump — now bound PER ADDON, not once round the loop — and the gob overlay), and `AddonRegistry.teardown` + the `:reload` sweep of the `:lua` REPL drop and dispose the entries, so we are the only disposer of a cached `Text`'s `Tex`. The fast path **no longer calls `GOut.atext`**: a private `render(str, fh)` does that method's own three lines minus the `dispose()`, so both paths are one lifecycle. The **key is `(string, FontHandle identity, `Fonts.gen()`)`** — the plan's clear-on-gen-move was REVISED: `gen()` is not frame-global (F5 XORs the per-instance override's stamp in, so it alternates between draw sites within one frame) and clearing on it would be worse than no cache; keyed, it is correct under F5 for free. Colour is out of the key by construction — it is applied in `blitText` as a `chcolor` tint on a white raster. **Zero core edits.** Measured in-game: `hello` enabled went from 130 back to 220–240 FPS (disabled baseline 220), all draw output, live font restyles/resets and `:reload`/disable cycles unchanged. **026.2 DONE** — `hafen.client:profiling():textcache()`, **pull-only** (D-051: answers with profiling OFF, like `:memory/:net/:loader/:render` — every number is one the cache already keeps to bound itself): top level = the CALLING addon's cache (`entries/bytes/hits/misses/evictions/hitRate` + `maxEntries/maxBytes`, the ceiling beside the count), `total` = the same five summed over every Lua owner + `owners`, which is what makes the leak check readable from `:lua` with all addons disabled; `hitRate` is ABSENT (not 0) until the first lookup (D-050). Counters are cumulative since the addon loaded (the `Cache` is a final field of `Addon`, so `:reload` = a fresh cache + a fresh count) and `p:reset()` deliberately does NOT touch them — it owns the frame ring, not a cache's own bookkeeping. Harness: `:hello textcache [stress]` + a static/volatile pair in the HUD overlay; the stress toggle draws at **alpha 0** (rasterised and cached for real, painted not at all) and feeds a fixed pool of 2000 distinct strings **32 per frame** so the client stays usable. **The caps were then tuned against the reading** (`plan.md` § "Measured (026.2)"): 512 entries held **7.91 MiB** = ~15.8 KiB an entry, so the provisional 16 MiB byte cap could never bind before the entry cap — decoration, not a bound. **`MAXBYTES` → 8 MiB**, where the two caps meet at the measured average width (narrow text bounded by count, wide by bytes); **`MAXENTRIES` stays 512** because a cap below one frame's distinct strings evicts every entry before its next use and is strictly worse than no cache. Verified: `total.bytes` settles at 8265728 (7.88 MiB) — both caps are live bounds; disable-all + `:reload` → `total.bytes` = 0, so teardown disposes every cached texture. Bonus: the stress delta (240 → 80 FPS for 32 rasterisations/frame) = **0.26 ms a line**, against 019.8's 0.28 ms measured a different way. **026.3 DONE** — the close, docs only (no code): `docs/addons/api/ui.md` gains "Text is cached across frames" under the `g` wrapper (key = string + font, colour is a tint so two colours are one entry; the honest line that a string changing every frame is re-rasterised every frame — budget a live readout by how often its *text* changes; overrides invalidate next frame; 512 entries / 8 MiB), `client.md` the `textcache()` row + section (the five figures, `hitRate` absent until looked up, the caps, `total`/`owners` as the leak check) and "four counters" → "five" everywhere, `fonts.md` the cross-refs; **D-062** "performance goes behind the existing surface as a cache, not in front of it as a handle"; four `learnings/ui-widgets.md` entries (the 019.8 handle prediction lost and why, a generation is only a clear signal if it is frame-global, a cap that can never bind vs. one below the working set, own the `Tex` or leak). No new index row, no `hello` edit — 026.2's `:hello textcache [stress]` + the static/volatile HUD pair already ARE the harness; no coverage toll (no `haven` file read). Prior: `025-buffs-oop` **DONE** (025.1–025.3), ROADMAP's OOP migration continues (party/fight/… still flat). Last feature: `hafen.buffs.list()/has()` (flat snapshots) → `hafen.buff()` = the active Buff objects / `hafen.buff(needle)` = the first whose res or name contains it, entity reads `:res/:name/:amount/:duration/:number/:exists/:info`, interned on the `Buff` **widget identity**, and `BuffAdded/Removed/Changed` payloads become the object (a removed Buff still answers, `:exists()` false). **025.1 DONE** — `LuaBuff` (userdata + per-addon weak intern `Cache`, `IdentityHashMap<Buff,Ref>` + `ReferenceQueue` drained on every access — identity is the WIDGET, since the same res can be up twice and `Bufflist` child order is arrival order), the callable `hafen.buff` table (number key → an error naming the string key, `""` → an error, a miss → `nil`; the needle is NOT trimmed, that would silently change a substring search) and the hard cut of flat `hafen.buffs`. ONE shared scan `LuaBuff.actives()` — `Bufflist` children minus `AddonWidgets.buffDest` (`Buff.reqdestroy` only flags `dest` + fades 0.35 s, so a removed buff is a live child for ~21 frames) — feeds both the list form and the `BuffsAdapter` poll, and is also `:exists()`. `:amount/:duration/:number` are one accessor each over a shared `info(b, Class)` helper with `snapshot()` re-expressed over them, so `:info()` keeps the documented table shape. **025.2 DONE** — `AddonManager.fireBuff(event, b)` beside `fireGob`/`fireKin`/`fireSlot` (`hasSub`-gated, per-addon interning, addons + the `:lua` owner) and all three events carry the OBJECT; the adapter's `IdentityHashMap<Buff, LuaValue>` snapshot cache STAYS, purely as the value-comparable `buffEqual` diff KEY (an object cache cannot detect content change — interned objects compare by identity), it is simply never handed to Lua: `buff:info()` is that on demand. `BuffRemoved` fires the widget it just dropped, BEFORE `it.remove()`. **025.3 DONE** — `docs/addons/api/buffs.md` rewritten for `hafen.buff` (the two call forms, "handle, not snapshot", the read table, the kept "0..1 fractions, NOT seconds" note, and the footnote on why there is no `buff:click()`), the four cross-ref pages + both index rows follow; `hello` v0.60.0 checks the whole contract once per login in `readBuffs` (interning, lookup hit/miss, the number-key error, `:exists()`, `buffsGone`) on top of its two-pass summary; the `Buff`/`Bufflist` coverage toll went into `specs/codebase/services.md` (`Widget.destroy` unlinks only — it clears neither `res` nor `info`, so a stashed removed `Buff` keeps reading, and `:exists()` is a bar-membership scan). The meter is **`:duration()`** here, not `:cooldown()` (renamed in this task, `buffEqual` diffs the new key), and the word **"debuff" is gone** from the whole area — the engine knows only `Buff`/`Bufflist` and publishes no positive/negative flag (**D-061**). No core edit in the whole feature. Prior: `024-audio-oop` **DONE** (024.1–024.4). Audio to OOP, and it is `hafen.sound` **alone**: `hafen.sound(name)` = an interned Sound (`:res/:play([volume])/:stop/:playing/:info`; no `:exists()` — a name-keyed handle has no lifetime to go stale), `hafen.sound()` = the addon's own still-playing set (auto-silenced on disable/reload); volume is `:play`'s FIRST arg. Flat `hafen.sound.play` hard-cut. **ZERO core edits in the whole feature.** **024.1 DONE** — `LuaSound` (userdata + per-addon weak intern cache keyed by resource NAME, the `LuaPagina` shape; no catalogue behind it — sound resources are not enumerable): `hafen.sound(name)` → `:res/:play([volume])/:info`, ungated (client-local); the play path is the old `AddonManager.playSound` folded in (`Resource.local()` + `loader.defer`, a bogus name is silent) plus an `Audio.VolAdjust(cs, vol)` wrap when volume ≠ 1; volume checking lives in one shared `LuaSound.volume(v, method)`, written to be shared with the Track section 024.3 cut (the method-name arg still buys the caller-accurate error). **024.2 DONE** — `:stop()` → self, `:playing()`, `hafen.sound()` = the addon's still-playing Sounds, and the teardown sweep. ZERO core edits: `ActAudio.RootChannel.remove(cs)` stops, `mixer().playing(cs)` tests, and since `Audio.Mixer.get` drains a finished clip LAZILY (the only end-of-clip signal) every read IS the prune. Playback state lives in the per-addon `LuaSound.Cache` keyed by resource NAME — not on the handle, which is weakly interned. `:play()` returns before the loader resolves, so `:stop()` carries a per-name generation stamp the deferred task re-checks, and that task registers the clip + calls `UI.sfx` under ONE monitor (lock order `Live` → mixer) or a stop in between "stops" a clip the mixer has not got yet. Swept by `AddonRegistry.teardown` (disable/`:reload`/session init) plus an explicit `reload()` sweep of the `:lua` REPL, which is not an addon and never gets teardown. Harness: `:hello sound` toggles a long clip. **024.3 = the CUT (D-058)** — the Track section was built to spec and then removed whole (`LuaMusic` deleted, `Addon.tracks` gone, `haven/Music.java` reverted to pristine): `haven.Music` is MIDI driven only by `RootWidget`'s `"bgm"` uimsg, which this server never sends (**0 `midi` layers in 132,777 cached resources** vs 36 `audio`, all sfx). The audible "music" is `ActAudio.Ambience` on the `amb` channel (a render-tree node with a lifetime, not a clip handle — its own feature if ever wanted). `hafen.music` is deliberately absent. **024.4 DONE** — the close: `docs/addons/api/audio.md` is a sound-only page ending in a "There is no `hafen.music`" section that states the reason (MIDI + no content; the audible "music" is `Ambience` on the ambientVolume slider) so nobody re-proposes it; the two index rows and `client.md`'s cross-ref drop `/ music`. `hello` (v0.52.0) checks the whole contract once per login in `readSound` — interning, `:info()`, the volume-range and number-key errors, a bogus name that stays silent to Lua (the client's own red "could not play" line IS the proof, expected each login), the live count, and `flatPlayGone`/`musicGone` — while `:hello ping`/`:hello sound` stay the audible demos. **D-059** (an interned entity: per-*use* parameters are call arguments, only identity is state — a stored volume would leak between unrelated uses of the same clip) and **D-060** (`:exists()` iff the entity has a lifetime; on a name it would silently become a typo check that races the loader) written; the learning that a weakly-interned handle cannot hold state — it lives in the per-addon cache under the intern key, which is also what the collection form and the teardown sweep walk. Prior: `023-menugrid-oop` **DONE** (023.1–023.3). The action menu (`MenuGrid` paginae) as OOP: `hafen.menugrid()` = the catalogue (interned, grid sort order, `:find/:roots/:list`), `hafen.menugrid(key)` = one Pagina — key by SHAPE (`/` ⇒ res name = identity, else display name = a search convenience, not unique; a number errors, a miss ⇒ `nil`); `:res/:name/:path/:parent/:children/:tooltip/:hotkey/:isnew/:exists/:info`. The catalogue is `paginae` **plus the parent closure** (categories live only in the private `pmap`); `Loading`-guarded ⇒ short right after login, fills in sub-second. The verb `:use()` → self, **no arguments**, UNGATED on purpose — drives `PagButton.use(new Interaction(1,0))` (the pure message half: `"act"`-by-path vs `"use"`-by-id, so id-only paginae fire), NOT `MenuGrid.use` (the click handler); a category errors and points at `:children()`; firing demo `:walker menugrid <name>` (`hello` stays read-only: summary + contract check each login, tree under `:hello actions`). Docs: `docs/addons/api/menugrid.md`. Prior: `022-actionbar-set` **DONE** (gated `slot:set(resourceName)`, async write), `021-actionbar-oop` **DONE** (LuaSlot + hard cut, `ActionbarChanged` = a Slot), `020-kin-oop` **DONE** (hard cut, Kin ↔ Gob, `KinChanged` = `Kin[]`), `019-profiling` **DONE**. **Next:** no feature in flight — ROADMAP's OOP migration continues (party/fight/… still flat).

## Engine & runtime
- **Engine**: LuaJ embedded (`src/io/brodgar/addon/`); per-addon sandboxed envs (D-017), instruction watchdog + soft
  per-tick CPU budget with auto-disable (D-018). **Loading/lifecycle**: `manifest.json` discovery from `addons/`;
  `:reload` rebuilds the addon layer only (D-005); enabled set persisted (D-006). Console `:lua` / `:addons` /
  `:reload`; AddOns panel (toggle/status).
- **Event bus** `hafen.events.on` (OnLoad/OnEnterWorld/OnUpdate/OnDisable, GobAdded/Removed (a Gob), MeterAdded/Removed/Changed (a Meter, 027.2 — `VitalsChanged` GONE),
  Buff*/Fep/Study/Actionbar/Equip Changed, GhostClicked, ChatMessage…) — error-isolated, UI-thread-marshalled;
  timers `hafen.timer.after/every`. **Store**: `hafen.store` JSON saved-vars, per-char + account (D-023).

## Read API (`hafen.*`), UI (`hafen.ui`), hooks, input
- **Gobs (OOP, D-044/045)**: `hafen.gob(id)` → an interned Gob
  (`:pos/name/health/facing/moving/speed/speech/icon/overlays/isplayer/distance/exists/id/info`); the flat
  `gob.*(ref)` table and the `"player"`/`"partyN"` tokens are GONE. `world.*` hands out Gobs. **Kin (OOP, D-056,
  020.1)**: `hafen.kin` is CALLABLE-ONLY — `hafen.kin()` = the roster of interned Kin (+`:find/:list/:add`),
  `hafen.kin(idOrName)` = one Kin (`:id/:name/:group/:color/:online/:exists/:info`; gated
  `:rename/:setGroup(0..254)/:endkin/:forget` chain on self); flat `kin.*` GONE; `KinChanged` = a per-addon `Kin[]`
  (`fireKin`, `hasSub`-gated, 020.3). **Kin ↔ Gob (020.2)**: `gob:kin()` = the `ui/obj/buddy` attrib (+ name fallback
  on a pin bump), `kin:gob()` = an `OCache` sweep **preferring the body** (hearth fires are marked too —
  `world.gobs`+`g:kin()` = the many-case). **Actionbar (OOP, D-057, 021.1)**: CALLABLE-ONLY — `hafen.actionbar(n)` =
  the Slot at the raw 0-based game index (0..143, OOB throws), `hafen.actionbar()` = the **1-based** array of all 144
  (same interned objects, `#`=144); `slot:index/:empty/:res/:name/:cooldown/:info` + gated `:use([mods])` → self; flat
  `.slot/.use` GONE; `ActionbarChanged` = the changed **Slot** (`fireSlot`, `hasSub`-gated, 021.2; `hello` re-checks the contract each login, 021.3). **Write (022)**: gated `slot:set(resourceName)` → self = the drag's own `wdgmsg("setbelt", n, "res", …)`; ASYNC (echoed back ⇒ `:set():use()` uses the OLD content — watch `ActionbarChanged`), unknown name silently dropped, no `"pag"`; demo `:walker setbar`. **Map** `map.*`: tile/height/grid/gridPos + conversions, grid ids = exact decimal strings.
  **Meters (OOP, 027.1–027.2)**: `hafen.meter()` = every HUD bar (1-based, HUD order) / `hafen.meter(needle)` = the first whose server-published `IMeter.bg` name contains it; `:res/:index/:value/:color/:segments/:exists/:info`; `MeterAdded`/`MeterRemoved`/`MeterChanged` carry the Meter (`fireMeter`, `hasSub`-gated). **Player/char**: `hafen.player()` → Player object (`:gob()/:name()/:worldToScreen()`, D-046; `:vitals()` CUT by 027.1); `char.*`
  (attrs/food/skills/lp/weight), `time/party/buffs/study.*`, `items.*`. **Gap subsystems** (A1–A11)
  `markers/radar/kin/speed/craft/quests/wounds/fight` — widget-tree adapters. Windows/widgets (`ui.window`/`widget`) +
  `LuaGOut` draw wrapper; overlays `ui.overlay`/`ui.gobOverlay`.
- **Client options** `hafen.client:options()` → `interface/video/audio/camera/client/keybindings`, one per OptWnd
  panel over the stores OptWnd writes; arity is the verb (`opt:name()` reads, `opt:name(v)` writes + chains).
  `keybindings` = `register(name, fn)` (UNBOUND, D-047) + `get/set/list/unregister`, keybind-panel integrated;
  `hafen.key` GONE (018.2); slash `hafen.slash.register`.
- **Profiling** (019, detail in `019-profiling/`): Options ▸ **Client** ▸ "Enable profiling" =
  `options():client():profiling()` = `:profile on` — ONE switch (`prof.Prof.arm`, D-049), next-frame; engine in
  `io.brodgar.prof`/`ui`, handles in `addon` (D-048). `hafen.client:profiling()` → `:frame()`/`:history(n)` (600-frame
  ring)/`:reset()`; snapshot tables, absent key = not measured, off ⇒ `{}` (D-050). **Pull-only counters**
  `:memory/:net/:loader/:render` (D-051) answer **off**, match `:stats on`; `:addons()`+`:scope`/ `:measure` = the
  D-018 watchdog's OWN measurement split by `callLua` category, never re-timed (D-052). `:widgets()` = ONE `// addon:`
  `long[] prof` on `Widget`, inclusive at the seams, self derived at snapshot time (D-053); `:passes()` = FIXED
  `shadow`/`scene`/`ui2d`, CPU+GPU, SELF time under the frame's `draw` (D-054; `:gl()` = ARMED-ONLY); `:overhead()` =
  five tiers, **0.54% of frame** (D-055).
- Hooks: `hook.input` (L1 pre-widget), `hook.action` (L2 outbound `UI.wdgmsg`), `hook.message` (L3 inbound).
  Interception/replacement: `ui.onWidgetCreate`, `ui.adopt` (model handle), `ui.replace` (bags replaces the native
  inventory, restores on disable), `widgetstack` = `/framestack`. Introspection: `ui.root()/node(id)` WidgetNode
  walk (D-041), `ui.at/mouse` hit-testing + `node:rootpos` (D-042).

## Actions (gated write tier), virtual entities, rendering, data, fonts, example addons
- `hafen.act.*`: moveTo/clickGob(gob)/useItemOn/place/select/raw, menu/flower, item verbs + per-subsystem verbs
  (`slot:use`, speed.set, `kin:rename/:setGroup/:endkin/:forget`, craft.make…; **`pag:use()`** 023, ungated — the
  first section owning its own verb). Per-addon permission + enable-time consent, NO global switch (D-027/D-028);
  write addons default-disabled. **Example addons (harness)**: `hello` (grows with every feature — one login
  re-checks everything), `hogtest`, `bags`, `planner`, `widgetstack`, `netdemo`, `walker` (writes), `optionstest`, `profiler`.
- **Ghosts** `hafen.ghost`: client-only virtual gobs (D-029..D-033) — clickable, oriented, grid-anchored layouts,
  transform gizmo. **Render** `hafen.render`: image (screen), sprite (fixed + billboard world quads), glTF 2.0
  static models — textures, materials, lighting (D-034/D-035). **Data/net**: `hafen.json` (D-036); `hafen.http`
  async get/post, manifest `network` allowlist (D-037). **Fonts** `hafen.font` (D-043): all 11 scopes + `node:setFont`.
