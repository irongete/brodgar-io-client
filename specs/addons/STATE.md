# STATE — what works right now

> Maintained by REPLACING (max 60 lines). Branch `feature/addons`; per-feature detail: its `NNN-` folder.

**ACTIVE:** `029-widget-oop` (**029.1-029.3 DONE**, 029.4 to go) - **feature B1**: collapse the THREE objects `hafen.ui` hands back for one widget (window/widget handle . `adopt`/`replace` model . transient `WidgetNode`) into ONE interned entity, `:info()` as the snapshot hatch. What you create and what you find become the same type. The consequence that matters: **reading stops being coupled to replacing** - today the only way to read a chest's items is `hafen.ui.adopt(id)`, which HIDES the native window. Planning decisions: items are a **relation on the container** (`widget:items()`) and **`hafen.items` is hard-cut** (`hafen.ui.inventory()/equipment()/hand()`) because in Hafen there is no inventory model outside the widget tree; geometry is **arity-as-verb** (`:pos()`/`:pos(x,y)`, the 018 shape) so `:move()` is cut; a geometry write on a NATIVE widget errors naming feature E; **`adopt` is DELETED, not renamed** while **`replace` is untouched**. Also closes the `LuaModel`/`LuaMesh` collision 028 deferred - by deletion. **029.1 DONE** - the entity + the reads. `LuaWidget` (the addon's own *drawn* widget) -> **`AddonWidget`** (package-private, zero contract change; it now sits beside the unrelated `haven.AddonWidgets` - different packages, but worth knowing), `LuaWidgetNode` **deleted** and its role grown into the new **`LuaWidget`**: userdata + per-addon metatable + intern cache, the `LuaMeter` shape with **one deliberate deviation** - the cache is `WeakHashMap<Widget, WeakReference<LuaValue>>`, weak on BOTH axes (**D-064**), because over a whole tree the strong-keyed `IdentityHashMap` would pin every destroyed widget and break D-041's no-pin rule; the inner `WeakReference` is load-bearing (a `WeakHashMap` whose value reaches its own key never expires an entry). `hafen.ui.root()/node(id)/at(x,y)` and `model:node()` all hand back that ONE type; the 268-line `nodeHandle` closure-table builder in `UiApi` is gone. Reads carried over unchanged (`:type/:id/:children/:parent/:pos/:size/:visible/:text/:walk/:at/:rootpos/:setFont/:resetFont`) **plus `:exists()` and `:info()`**; **`:same()` HARD CUT** - it only ever existed because nothing was interned, so `widgetstack` (v0.3.0) collapsed its per-frame hover guard to `if leaf == last then return end`, which also absorbs the separate "still hovering nothing" branch (`nil == nil`). Liveness stays `hasparent(ui.root)` and a stale entity still nulls its `wdg`. **The measurement gate passed the wrong way round**: on a 2000-widget tree, per node, the OLD `nodeHandle` cost **513 ns** (a `LuaTable` + *fifteen* closures per node), an interned cache hit **27 ns**, a cold insert **201 ns** - one full walk 1.03 ms -> 0.05 ms steady, 0.40 ms cold. Interning is ~19x **cheaper** than the handles it replaced; the plan's documented fallback is unused. Verified headlessly (22 asserts incl. a GC-based no-pin proof) - 029.1 also established that a real `haven.UI` **can** be built off-screen (`bin/builtin-res.jar` for `Text.<clinit>`, an `Unsafe`-allocated `Audio.Root` with a reflected `Mixer`), recorded in `specs/codebase/boot-and-loop.md` - and in-game (widgetstack, the `==` probe, `hello`'s F5 `:hello node`). **The docs still describe `WidgetNode`/`:same()`/`adopt` - 029.4 owns that sweep.** Zero core edits (one comment in `haven/Fonts.java`). **029.2 DONE** - **what you create and what you find are now the same value**: `hafen.ui.window{}`/`widget{}` return the entity, interned on the **ROOT** (the chrome, or the bare widget - `content != root` is the whole "is this a window?" test; the 38-line `uiHandle` closure table is gone), so `hafen.ui.at(x,y)` over your own window hands back that very value. Geometry is **arity-as-verb** (`:pos()`/`:size()` read on ANY widget, `:pos(x,y)`/`:size(w,h)` write + chain; `:size(w,h)` resizes the CONTENT then repacks the chrome); `:move()` HARD CUT. **Provenance (OWNED vs BORROWED) is DERIVED from the tree, never stored on the handle (D-065)** - a weak-on-both-axes cache re-mints handles, so a mint-time flag would silently go false under GC; an `AddonWidget` already records its owner + its `root`, so the test is two field reads and is per-addon for free. `:info()` gained **`owned`** (asking beats provoking the error). The write verbs (`:pos(x,y)`/`:size(w,h)`/`:pack()`/`:destroy()`) are OWNED-only with two distinct refusals - geometry names **layout, feature E**; `:pack()/:destroy()` names `hafen.ui.window{}`/`widget{}` - and a write on a STALE widget is a **silent chaining no-op** (a write is not a question; erroring would force an `:exists()` guard at every call site). **`adopt` is DELETED** (its whole job was a readable handle, which `hafen.ui.node(id)` now gives with nothing hidden), and with it `model:raw()` (it was `:id()`); what remains is `w:hide()`, which on a BORROWED widget records the new **`Addon.hiddenNative`** restore list - `UiApi.teardownHidden` replays the **ORIGINAL** visibility (hiding something already hidden must not reveal it later), `w:show()` drops its own record, and the relog-vs-`:reload` guard is **two-branch** (`getwidget(id) == wdg` when server-bound, else `hasparent(u.root)` - `w:hide()` can hit a client-only widget, which `replace`'s single id test could not). The restore list is a NEW field, **not** `Addon.models` as plan.md said: `LuaModel` is still `replace`'s bookkeeping until 029.3. `replace` itself is untouched except that its builder now returns the entity - the old `view.istable()` check silently yielded null and **leaked the custom view on every undo**, so `m.replaceView` holds the `AddonWidget` and is killed in Java. Verified headlessly (49 asserts; a bare `Widget` stands in for the GL-needing `Window` chrome) and in-game (`bags` replace/restore across toggle, `:reload`, disable and relog; the panel/`widgetstack`/`profiler` windows; the errors from `:lua`). Zero core edits. **029.3 DONE** - **reading stopped being coupled to replacing**: `widget:items()` answers on ANY container (`children(WItem.class)`, a DEEP traversal) with its window **visible and interactive** - proved in-game by one `root():walk()` returning the backpack, an open **Cupboard** (22 items), the `Belt` and the **study** inventory, three of which no adapter had ever been written for (**D-066**: a thing that lives inside another is a **relation** on it, not a section - so `hafen.items` is a HARD CUT, `inventory()`/`equipment()` became widget LOOKUPS under `hafen.ui`, `hand()` stays a snapshot because the cursor item is not a widget, and `find()` got no replacement at all: it is a Lua one-liner over `:items()`). Each entry is shaped by its container (an `Equipory` adds `slot` + the slot name, everything else the grid cell) and the Item SHAPE is unchanged - one `CharApi.itemSnapshot`, so `item.handle` still drives the gated `hafen.act.item` (D-022). The lifecycle moved onto the entity too (`:onItemAdded/:onItemRemoved/:onDestroy`, a per-tick `WItem` diff since an add is a create, not a uimsg) and is **hasSub-gated by construction**: the subscription IS the registration (`Addon.itemWatches`, born with the first callback, dropped with the last), so an unwatched widget costs one `isEmpty()` and there is no `:watch()`/`:unwatch()` verb; the death test is the **two-branch** guard (by id when server-bound, else `hasparent(root)`) because a container can be either; teardown drops the records **without firing** (a `:reload` is not a destroy). That is what let `replace`'s bespoke model handle be **deleted**: `fn(model)` now receives the ENTITY (every verb it had already lived there, and `:raw()` was `:id()`), `LuaModel` is a pure undo record and `pollModels` is destroy-detection only - while `replace`'s own rule is untouched, it hides the enclosing WINDOW and replays `hideTargetOrigVisible`, whereas `w:hide()` hides what you point at (`hafen.ui.inventory()` is the GRID; the window is `:parent()`, one hop up, same restore list). Verified headlessly (23 asserts: the hasSub gate, onDestroy-once, stale no-ops, teardown fires nothing) and in-game (the cupboard read with nothing hidden; `Inventory#9` still answering its 14 items while `visible=false`; `bags` replace/restore across four toggles, a `:reload` and two container windows; `hafen.items` = nil; no per-tick polling with nobody subscribed). `hello`/`bags`/`walker` carry INTERIM edits only (the 4 `adopt`/items sites; `walker` would have been broken by the cut). Zero core edits, no coverage toll. **The docs still describe `hafen.items`, `WidgetNode`, `:same()` and `adopt` - 029.4 owns that whole sweep**, plus `hello`'s contract check and version bump. **`028-asset-loader` DONE** — ONE loader for the files an addon ships: callable `hafen.asset(path)` (D-056) over ONE per-addon intern cache, with the D-017 sandbox resolver in its own home (`AssetApi`). Extension dispatch — `.png/.jpg/.jpeg/.gif/.bmp` image · `.ttf/.otf` font · `.glb/.gltf` mesh (`.jpeg` was ADDED; unknown ⇒ an error listing them all). `hafen.asset()` = this addon's **live** assets in load order; `:type()/:path()/:dispose()` on every asset, on top of each type's own verbs (`:size()` · `:derive/:family/:size` · `:bounds()/:info()`). **The loader takes a path and nothing else** (no per-type options: a font's size/style is `:derive{…}`, AWT's own split) — so the signature is uniform and `==` never depends on an options table. The intern key is the **RESOLVED** path (`"icon.png"`, `"./icon.png"`, `"img/../icon.png"` = one asset, one `TexI`); `:path()` answers the FIRST load's spelling; values are strong (an asset is an owned resource, bounded by the addon's own files). Identity is stable **while alive**: `:dispose()` drops the entry *before* freeing, so a re-load is a NEW object and a corpse is never served or listed. **The cache is unified; the teardown is not** — `AssetApi.teardownAssets` walks the typed lists in the R3b order (images, then meshes, after `teardownObjects`) then clears the map. A **built-in** font (`hafen.font("sans"|"serif"|"mono"|"fraktur")`, name-keyed, interned, engine-owned) and a `:derive`d variant carry NONE of the asset verbs (D-060: no file, no path, no lifetime). Hard cut of `hafen.font.load`/`hafen.render.image`/`hafen.render.model`; `render.sprite{image=}`/`object{model=}` are **handle-only** (D-012) with three distinguishable failures — a path string (quoted back, naming `hafen.asset`, *it is interned, so repeating the load is free*), a disposed handle, `got <typename>`; the path-string branch is guarded `isstring() && !isnumber()` (in LuaJ a number IS a string). `RenderApi` is **−347 lines** and a SCENE namespace; **zero core edits** in the whole feature; no coverage toll (no `haven` file read). Fonts finally have a cache (one `Font.createFont` + one `registerFont` per FILE, not per call — the feature's one real defect). `planner` DID pass path strings (the spec's "no addon uses it" was wrong): its records still store the PATH, `spawn()` turns it into a handle — the pattern feature **C**'s JSON themes will reuse. **The feature refuted a standing learning**: R3b claimed a manual `mesh:dispose()` under a live object "frees the textures out from under it" — it does NOT (`MeshSprite.texRender` captures `tex.st().data` ONCE at mill time), so the teardown order is load-bearing for **leak-freedom**, not visual correctness; corrected in `learnings/rendering.md`, the javadoc, the harness and `asset.md`. **028.3 DONE** — the close, docs + Lua only: new `docs/addons/api/asset.md` (the one flow *load → draw/decorate/stand*; the three types; the path-and-nothing-else rule; the sandbox; interning + the honest limit; the shared/typed verbs; the collection form; the error catalogue; why there are no URLs and why engine resources are *addressed*, not loaded — with the CORRECTED mesh-dispose wording), rows in both index tables, `render.md` reframed as a scene namespace (both loader sections deleted, a new **Handle-only** section, the glTF subset kept as its own heading), `fonts.md` rewritten around the **two doors** (`hafen.font(name)` built-ins / `hafen.asset(path)` files, `:derive` as the one way to size), plus `ui.md`, `conventions.md` (a new *Asset* reference) and `getting-started.md` (a new *Files your addon ships*); every internal link/anchor across `docs/addons/` verified. `hello` **v0.63.0**: `readAssets(tag)` is the once-per-login contract check (the live list, `==` interning incl. the resolved-path key, five error shapes, both handle-only refusals, the built-in-font D-060 boundary, `loadersGone`), `:hello assets` re-runs it and `:hello assets dispose` keeps the destructive half. Two learnings: an explicit `nil` to a D-056 callable namespace is the **collection form**, not an error (LuaJ cannot tell it from no-argument — document it, do not try to fix it); and a harness assertion whose precondition is not guaranteed must be **gated** (the handle-only probe only reaches its check in-world) — plus the version string `hello` logged itself had drifted 11 versions behind its manifest. **Prior feature:** `027-meters-oop` **DONE** (027.1–027.3) — the last flat widget-tree read became an OOP section: `hafen.meter()` = every HUD meter (the `place == "meter"` slot, not a hard-coded triple), `hafen.meter(needle)` = the first whose **server-published** `IMeter.bg` res name contains the needle; entity `:res/:index/:value/:color/:segments/:exists/:info` interned on widget identity; `VitalsChanged` → `MeterAdded`/`MeterRemoved`/`MeterChanged` carrying the object; hard cut of `hafen.player():vitals()` and the `Vitals` type. One `// addon:` core edit (`AddonWidgets.hudMeters` over the private `GameUI.meters`). **027.1 DONE** — `LuaMeter` (userdata + per-addon weak-valued identity `Cache`, the `LuaBuff` shape: `IdentityHashMap<IMeter,Ref>` + `ReferenceQueue` drained on every access) with `:res/:index/:value/:color/:segments/:exists/:info`, and the callable `hafen.meter` table (list / needle / number-key + empty-string errors / miss → nil). ONE shared scan `AddonWidgets.hudMeters(GameUI)` — the engine's OWN ordered `place == "meter"` list, not a `children(IMeter.class)` DFS whose order is a tree artefact — backs the list form, the lookup, `:index()`, `:exists()` and (027.2) the poll. That list was `private`, so the feature's single `// addon:` core edit is `private` → package-private on `GameUI.meters` (same-package `AddonWidgets` still cannot read a private field). Every read is `Loading`-guarded → nil (`bg.get()` throws until cached), only `:exists()` always answers, and a destroyed meter keeps reading with `:exists()` false. Colours are `AddonManager.color` → `{r,g,b,a}` 0..255, the `kin:color()` shape; `snapshot()` always sets `segments` (possibly empty), the rest unset when absent. Hard cut: `hafen.player():vitals()`, `readVitals`/`vitalsEqual`/`VITAL_KEYS`/`vitalsCache` all deleted; `VitalsAdapter` still compiles but fires NOTHING (`VitalsChanged` is dead until 027.2 renames it), `interested` narrowed to `IMeter` + `"set"`/`"col"`. `hello` carries an INTERIM edit only (`readVitals` → `readMeters`, the HUD overlay draws a bar per meter in `meter:color()`) — 027.3 owns the contract check, the handlers and the version bump, and the panel rows overlap the map-lock line while mounted. Verified in-game: the array matches the HUD bars in order with live values, and **mounting adds two bars the old positional triple could never see** (`häst`, `mount`) — the observed names are in `027-meters-oop/observed-res-names.md`, with the warning that `häst` is non-ASCII so the docs must advise an ASCII needle. **027.2 DONE** — `VitalsAdapter` → `MeterAdapter` (the `BuffsAdapter` split): **poll** diffs `LuaMeter.hud()` per tick for `MeterAdded`/`MeterRemoved` (the removal fires BEFORE the entry is dropped), **refresh** (`interested` = `IMeter` + `"set"`/`"col"`) re-reads the cached meters for `MeterChanged`; all three carry the **Meter object** through the new `AddonManager.fireMeter(String, IMeter)` beside `fireBuff` (`hasSub`-gated, per-addon interning, addons + the `:lua` owner), so an addon subscribing to none of them costs nothing. `VitalsChanged` is gone. The change key is the **segment array** (`LuaMeter.segments`), not `snapshot()` — `:value()`/`:color()` are its first entry, so one comparison covers both uimsgs and a multi-segment bar moving is a real change too; `:res()` is out of the key (leaving `Loading` renames nothing) and so is `:index()` (layout). The snapshot is a diff KEY only, never handed to Lua (`meter:info()` is that) — an `IdentityHashMap<IMeter, LuaValue>`, UI-thread-only, re-instantiated per session; membership is `==` everywhere. Engine order is refresh→poll and the poll seeds the cache at fire time, so a new meter surfaces as ONE `MeterAdded` with its values applied. Verified in-game: relog → one `MeterAdded` per bar (`hp`/`stam`/`nrj`, +2 on mounting), moving → `MeterChanged` on `stam`/`nrj` only with **no idle spam**, a stashed `MeterRemoved` payload still reads with `:exists()` false, `hello` regression clean. Known and owed to 027.3's docs: a payload can be younger than its resource — the bar added at mount fired while its `bg` was still `Loading`, so `:res()` was nil (`Meter(?)`) at fire time and answered a beat later. **027.3 DONE** — the close, docs + Lua only (no Java): new `docs/addons/api/meters.md` (the two call forms; the observed res-name table marked *observed, not contract* with the advice to needle on an ASCII substring — `häst` is non-ASCII; the read table; "no absolute numbers, no hunger → `hafen.char.food`" moved here from `player.md`; the three events; the `MeterAdded`-is-younger-than-its-resource gotcha — name-match on a later tick, never inside the handler; removed-but-still-readable; and why there is no write verb), its row in both index tables, and the vitals cut swept through `player.md` / `types.md` / `events.md` / `conventions.md` / `hooks.md` / `fonts.md` / `getting-started.md`. `hello` v0.62.0: `readMeters` is the once-per-login contract check (interning via a res-tail needle, miss → nil, the number-key and empty-string errors, `:index()`/`:exists()`/`:info()`, `vitalsGone`), the three handlers replace the `VitalsChanged` one, `vitalsFreeze` → `meterFreeze`, and the panel's hook lines are **bottom-anchored** (`h - 46/32/18`, window 190x166) because the bar count is genuinely variable (5 while mounted) — capping the meter block would have hidden real bars. **D-063** "an entity's key is what the engine publishes about it, not what the genre calls it". No new learnings (027.1/027.2 already wrote the three) and no coverage toll owed — no `haven` file was read this task. Prior, all DONE (detail in each `NNN-` folder): `026-text-cache` - a per-addon, content-keyed, bounded LRU of the rendered `Text` behind `g:text`/`g:atext` (130 -> 220-240 FPS on the harness; key = string + font handle + `Fonts.gen()`, colour is a blit tint so it stays OUT; caps 512 entries / 8 MiB, both live bounds, tuned against the reading; `hafen.client:profiling():textcache()` is the pull-only reader and `total`/`owners` the leak check; D-062, zero core edits). `025-buffs-oop` - `hafen.buff()`/`hafen.buff(needle)`, interned on the Buff WIDGET (the same res can be up twice), all three events carry the object, a removed Buff still reads with `:exists()` false; the word "debuff" is gone, the engine publishes no sign (D-061). `024-audio-oop` - `hafen.sound` ALONE, name-keyed + interned, volume is `:play`'s first arg, `hafen.sound()` = the addon's still-playing set (auto-silenced on teardown); the Track section was built to spec and then CUT - this server sends no MIDI (D-058/D-059/D-060). `023-menugrid-oop` - `hafen.menugrid()` catalogue + `hafen.menugrid(key)` Pagina, key by SHAPE (`/` => res name), `pag:use()` ungated. `022-actionbar-set` (gated async `slot:set(res)`), `021-actionbar-oop`, `020-kin-oop` (Kin <-> Gob), `019-profiling`. The flat widget-tree reads are done; ROADMAP's OOP migration continues (party/fight/... still flat).

## Engine & runtime
- **Engine**: LuaJ embedded (`src/io/brodgar/addon/`); per-addon sandboxed envs (D-017), instruction watchdog + soft
  per-tick CPU budget with auto-disable (D-018). **Loading/lifecycle**: `manifest.json` discovery from `addons/`;
  `:reload` rebuilds the addon layer only (D-005); enabled set persisted (D-006). Console `:lua` / `:addons` /
  `:reload`; AddOns panel (toggle/status).
- **Event bus** `hafen.events.on` (OnLoad/OnEnterWorld/OnUpdate/OnDisable, GobAdded/Removed (a Gob), MeterAdded/Removed/Changed (a Meter, 027.2 — `VitalsChanged` GONE),
  Buff*/Fep/Study/Actionbar/Equip Changed, GhostClicked, ChatMessage…) — error-isolated, UI-thread-marshalled;
  timers `hafen.timer.after/every`. **Store**: `hafen.store` JSON saved-vars, per-char + account (D-023).

## Read API (`hafen.*`), UI (`hafen.ui`), hooks, input
- **Gobs (OOP, D-044/045)**: `hafen.gob(id)` → an interned Gob
  (`:pos/name/health/facing/moving/speed/speech/icon/overlays/isplayer/distance/exists/id/info`); the flat
  `gob.*(ref)` table and the `"player"`/`"partyN"` tokens are GONE. `world.*` hands out Gobs. **Kin (OOP, D-056,
  020.1)**: `hafen.kin` is CALLABLE-ONLY — `hafen.kin()` = the roster of interned Kin (+`:find/:list/:add`),
  `hafen.kin(idOrName)` = one Kin (`:id/:name/:group/:color/:online/:exists/:info`; gated
  `:rename/:setGroup(0..254)/:endkin/:forget` chain on self); flat `kin.*` GONE; `KinChanged` = a per-addon `Kin[]`
  (`fireKin`, `hasSub`-gated, 020.3). **Kin ↔ Gob (020.2)**: `gob:kin()` = the `ui/obj/buddy` attrib (+ name fallback
  on a pin bump), `kin:gob()` = an `OCache` sweep **preferring the body** (hearth fires are marked too —
  `world.gobs`+`g:kin()` = the many-case). **Actionbar (OOP, D-057, 021.1)**: CALLABLE-ONLY — `hafen.actionbar(n)` =
  the Slot at the raw 0-based game index (0..143, OOB throws), `hafen.actionbar()` = the **1-based** array of all 144
  (same interned objects, `#`=144); `slot:index/:empty/:res/:name/:cooldown/:info` + gated `:use([mods])` → self; flat
  `.slot/.use` GONE; `ActionbarChanged` = the changed **Slot** (`fireSlot`, `hasSub`-gated, 021.2; `hello` re-checks the contract each login, 021.3). **Write (022)**: gated `slot:set(resourceName)` → self = the drag's own `wdgmsg("setbelt", n, "res", …)`; ASYNC (echoed back ⇒ `:set():use()` uses the OLD content — watch `ActionbarChanged`), unknown name silently dropped, no `"pag"`; demo `:walker setbar`. **Map** `map.*`: tile/height/grid/gridPos + conversions, grid ids = exact decimal strings.
  **Meters (OOP, 027.1–027.2)**: `hafen.meter()` = every HUD bar (1-based, HUD order) / `hafen.meter(needle)` = the first whose server-published `IMeter.bg` name contains it; `:res/:index/:value/:color/:segments/:exists/:info`; `MeterAdded`/`MeterRemoved`/`MeterChanged` carry the Meter (`fireMeter`, `hasSub`-gated). **Player/char**: `hafen.player()` → Player object (`:gob()/:name()/:worldToScreen()`, D-046; `:vitals()` CUT by 027.1); `char.*`
  (attrs/food/skills/lp/weight), `time/party/buffs/study.*` (`items.*` GONE, 029.3 — items are `widget:items()`). **Gap subsystems** (A1–A11) `markers/radar/kin/speed/craft/quests/wounds/fight` — widget-tree adapters. Windows/widgets (`ui.window`/`widget`) + `LuaGOut` draw wrapper; overlays `ui.overlay`/`ui.gobOverlay`.
- **Client options** `hafen.client:options()` → `interface/video/audio/camera/client/keybindings`, one per OptWnd
  panel over the stores OptWnd writes; arity is the verb (`opt:name()` reads, `opt:name(v)` writes + chains).
  `keybindings` = `register(name, fn)` (UNBOUND, D-047) + `get/set/list/unregister`, keybind-panel integrated;
  `hafen.key` GONE (018.2); slash `hafen.slash.register`.
- **Profiling** (019, detail in `019-profiling/`): Options ▸ **Client** ▸ "Enable profiling" =
  `options():client():profiling()` = `:profile on` — ONE switch (`prof.Prof.arm`, D-049), next-frame; engine in
  `io.brodgar.prof`/`ui`, handles in `addon` (D-048). `hafen.client:profiling()` → `:frame()`/`:history(n)` (600-frame
  ring)/`:reset()`; snapshot tables, absent key = not measured, off ⇒ `{}` (D-050). **Pull-only counters**
  `:memory/:net/:loader/:render` (D-051) answer **off**, match `:stats on`; `:addons()`+`:scope`/ `:measure` = the
  D-018 watchdog's OWN measurement split by `callLua` category, never re-timed (D-052). `:widgets()` = ONE `// addon:`
  `long[] prof` on `Widget`, inclusive at the seams, self derived at snapshot time (D-053); `:passes()` = FIXED
  `shadow`/`scene`/`ui2d`, CPU+GPU, SELF time under the frame's `draw` (D-054; `:gl()` = ARMED-ONLY); `:overhead()` =
  five tiers, **0.54% of frame** (D-055).
- Hooks: `hook.input` (L1 pre-widget), `hook.action` (L2 outbound `UI.wdgmsg`), `hook.message` (L3 inbound). Interception/replacement: `ui.onWidgetCreate`, `ui.adopt` (model handle — DELETED by 029.2), `ui.replace` (bags replaces the native inventory, restores on disable), `widgetstack` = `/framestack`. **Widget entity (OOP, 029.1, D-064)**: `ui.root()`/`node(id)`/`ui.at(x,y)` (+ `ui.mouse()`) all hand back ONE interned type — `:type/:id/:children/:parent/:pos/:size/:visible/:text/:exists/:info/:walk/:at/:rootpos/:setFont/:resetFont`
  + (029.3) `:items()` on any container and `:onItemAdded/:onItemRemoved/:onDestroy` (hasSub-gated), plus
  `ui.inventory()/equipment()` (widget lookups) and `ui.hand()` (a snapshot); `==` is the identity test (`:same()` GONE), liveness = `hasparent(ui.root)`, no pin (D-041), hit-testing mirrors the engine's own pointer dispatch (D-042).

## Actions (gated write tier), virtual entities, rendering, data, fonts, example addons
- `hafen.act.*`: moveTo/clickGob(gob)/useItemOn/place/select/raw, menu/flower, item verbs + per-subsystem verbs
  (`slot:use`, speed.set, `kin:rename/:setGroup/:endkin/:forget`, craft.make…; **`pag:use()`** 023, ungated — the
  first section owning its own verb). Per-addon permission + enable-time consent, NO global switch (D-027/D-028);
  write addons default-disabled. **Example addons (harness)**: `hello` (grows with every feature — one login
  re-checks everything), `hogtest`, `bags`, `planner`, `widgetstack`, `netdemo`, `walker` (writes), `optionstest`, `profiler`.
- **Assets (028)** `hafen.asset(path)` — docs `api/asset.md`: the ONE loader for an addon's own files — interned per resolved path, typed by extension (image/font/mesh), `:type/:path/:dispose` + the type's own verbs, `hafen.asset()` = its live ones, sandboxed (D-017); `font.load`/`render.image`/`render.model` GONE, and `render.sprite{image=}`/`object{model=}` take a **handle only** (a path string raises an error naming `hafen.asset`, D-012).
- **Ghosts** `hafen.ghost`: client-only virtual gobs (D-029..D-033) — clickable, oriented, grid-anchored layouts, transform gizmo.
  **Render** `hafen.render`: sprite (fixed + billboard world quads), glTF 2.0 static models — textures, materials, lighting
  (D-034/D-035); a SCENE namespace now, its files come from `hafen.asset`. **Data/net**: `hafen.json` (D-036); `hafen.http` async
  get/post, manifest `network` allowlist (D-037). **Fonts** `hafen.font(builtin)` (D-043): all 11 scopes + `node:setFont`; a `.ttf`
  is `hafen.asset`, its size is `:derive{size=…}`.
