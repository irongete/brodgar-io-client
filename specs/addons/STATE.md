# STATE — what works right now

> Maintained by REPLACING (max 60 lines). Branch `feature/addons`; per-feature detail: its `NNN-` folder.

**ACTIVE: `040-ui-controls`** (11 of 13 tasks done) — the client's own UI controls, reachable from Lua. 17 of them in two groups: the **direct** ones (`Button` `IButton` `TextEntry` `Label` `Img` `Progress` `HRuler` `CheckBox` `ICheckBox` `RadioButton` `HSlider` `Scrollport`+`Scrollbar`), concrete classes that construct and configure, and the **model-backed** five (`SListBox` `SDropBox` `SListMenu` `GridList` `TableBox`), abstract and generic over the item type, which all reduce to `SListWidget`'s two methods — `items()` and `makeitem()` — plus `GridList`'s `drawitem`. A control is a **Widget**, not a nineteenth entity, so every existing verb and selector works on one for free; `:value()` is the one verb for a control's value (its first implementor, 040.3) and `:onChange(fn)` the one notification (its first implementor, 040.4) — a programmatic `:value(v)` never re-enters it.

**040.11 DONE — `:grid()`, the one control that PAINTS its rows (D-163/D-164).** `hafen.ui():grid()` is a real `GridList`, the last of the model-backed five and the odd one out: `GridList` has no `items()`/`makeitem()`, only `drawitem(GOut, T)`, so `:rows(t)` here is a plain array of whatever the addon's own `:onCell(g, item, w, h)` wants to read, not the string-or-`{icon=,text=}` shape the other three share. **`:onCell` binds the SAME `LuaGOut` wrapper `:onDraw(fn)` already uses** (D-163) — one bind/unbind per cell rather than per widget-frame — and isolation is free: `AddonManager.callLua` already catches per-call, so one cell's handler throwing costs that cell's log line while `GridList.draw`'s loop keeps painting every other one, that same frame. **`:cell(w, h)` is building-only** (D-164), the same shape `:rowHeight(n)` already has: `Group.itemsz` is `final` and a `Group` self-registers into a private list with no removal, so a different box rebuilds the control under the same Lua handle, carrying `:rows`/`:onCell` across. One `Group` per control, `marg = (-1, 5)` — the engine's own even-spread-across-the-width shape (`SkillGrid`/`ExpGrid` use the same margin). `haven` core edits zero. **One round on the suite itself**: its isolation check's crashing cell first threw every frame for the whole verification wait, which was correct but read as noise in the pasted log; fixed to a one-shot guard, same assertion.

**040.10 DONE — `:dropdown()` and `:menu()` (D-161/D-162).** `hafen.ui():dropdown()` is a real `SDropBox` and `hafen.ui():menu()` a real `SListMenu`, both over 040.9's `LuaRows` bridge — split into `content()` (the bare row) and `makeitem()` (the `ItemWidget`-wrapped row only `CList` needs), since `SDropBox`/`SListMenu` each do their own click-wrapping through an inner list class (`SDropList`, `InnerList`) and wrapping again here would nest two click handlers over one row. **Neither adapter calls the engine's own `of(...)` factories** — subclassing `SDropBox`/`SListMenu` directly, exactly like `CList`, is what makes the task's own "anonymous subclass" ownership question not arise at all (D-161). A dropdown answers the list's `:value()`/`:onChange()` spine, written through `SDropBox.change()` directly rather than a bare `sel` field (unlike `CList`) because the closed box's own displayed widget is kept in sync only inside that method. A menu fires and holds nothing: `:onSelect(fn)` is a new capability, not `:onChange` with no value (D-162). **Two things found only in-game, both fixed in `io.brodgar.addon` with zero `haven` core edits.** `SListMenu.added()` grabs ALL mouse and keyboard input the instant it attaches — fixed with the engine's own `nograb()`, since a menu here is a placed control, not a modal popup. And `Window.mousedown` raises itself AFTER propagating a click to children, so a dropdown's popup — added to `ui.root` DURING that same click — always lands one step behind the very window that opened it; fixed by queuing the popup for a same-frame re-raise from `AddonManager.tick()`, in the same slot the arming tick already occupies, so it resolves before that frame's draw with no visible flicker.

**040.9 DONE — the row bridge, and `:list()` (D-159/D-160).** `hafen.ui():list()` is a real `SListBox` over `LuaRows`, the Lua-array→`items()`/`makeitem()` bridge the later model-backed controls (dropdown, menu, table) reuse (D-108): a plain string becomes a `TextItem` row, `{icon=, text=}` an `IconText` row, chosen PER ELEMENT so a mixed table works. **Every row is resolved WHOLE at `:rows(t)` time, not lazily per visible row** (D-159) — because `SListBox.update()` (where `makeitem` actually runs) is called from the ordinary per-frame tick with none of `AddonManager.callLua`'s error isolation; the resolved `Row` object, not the raw Lua value, is what `SListWidget`'s identity-keyed selection binds to, and `:value()` unwraps it back on the way out. `:value(v)` writes `sel` directly (D-153 again), so a programmatic write never re-enters `:onChange`; the real-click path is `change(I)`, overridden to notify — the one hook both a real click (`ItemWidget.mousedown`) and the click-away deselect reach. `:rowHeight(n)` (D-160) is building-only and rebuilds, the same shape a face setter has (D-113/D-148), since `SListBox.itemh` is `final`; it defaults to the client's own label height. `haven` core edits zero. **Found only in-game**: a suite's tree-size baseline must be measured on the same side of a `SListBox`'s first lazy row-build tick as its final count, or teardown reads a leak that never was one. Docs split: `docs/addons/api/ui/lists.md` is a new page for the model-backed family, since `controls.md` was already at the 300-line ceiling.

**040.8 DONE — `:scroll()`, composed rather than extended, and the `:parent(w)` trap actually fixed.** `hafen.ui():scroll()` does NOT subclass `haven.Scrollport` — its constructor seals `bar` as a fixed anonymous `Scrollbar` with no seam for a subclass to add its own notification, so the control rebuilds the same shape from `Scrollport`'s own public pieces (`Scrollbar`, the nested `Scrollcont`), with the bar as its own adapter (`Bar`, nested in `CScrollport`) carrying the usual `Owned`/`:range`/`:value`/`:onChange` contract. **The bar is Owned in its own right, found structurally** — `sp:children()` or `hafen.ui():all("@Scrollbar")`, never a dedicated verb — which is what lets it answer those verbs at all (an unowned widget refuses them by name, per 040.6); a programmatic write on it DOES move the actual scroll offset (unlike a bare `:scrollbar()`, which drives nothing), while `:onChange` still fires from a real drag only. `:type()` on the composite reads `"Widget"`, the same answer `:radio()` gives, for the same reason: two engine classes glued into one control. **The trap the task exists to not fall into, actually fixed**: `Widget.add` never routes through `addchild`, and this class (like the engine's own `Scrollport`) only overrides `addchild`, so `LuaWidget`'s `:parent(w)` write now special-cases a `Scrollport`-shaped target, adding into its inner container instead of dropping the child beside the bar. `haven` core edits zero.

**040.7 DONE — `:entry()`, and the first READ/WRITE-split retirement (D-158).** `TextEntry` answers `:value(s)` (the one door), `:onChange(fn)` per keystroke and `:onSubmit(fn)` once on Enter, via `activate(String)` overridden outright (never `wdgmsg`); a write goes through `rsettext` (replaces the `ReadLine` buffer, notifying nothing), D-153's rule reached through a buffer object rather than a field. **`entry:text(s)` retires naming `:value(s)`; `entry:text()` (the read) does NOT** — found in-game when retiring both arities crashed the shipped `widgetstack` example, whose inspector calls `:text()` generically on every widget it walks, exactly the never-throwing best-effort contract `docs/addons/api/ui/widget.md` already publishes; only the WRITE collided with the one-door rule. `haven` core edits zero.

**040.6 DONE — `:slider()` and `:scrollbar()`, and the value spine's first CLAMP (D-156/D-157).** `HSlider`/`Scrollbar` answer `:range(min, max)`/`:value(n)`/`:onChange`, `:range` the new name (`Controls.Range`, dispatched like `:rows`) whose write CLAMPS an out-of-bounds `:value` to the nearer bound rather than refusing it (unlike 040.3's `Progress`, D-156) — a slider's range is caller-owned and moving, not a fixed contract; narrowing `:range` re-clamps a value that no longer fits, silently, D-153's direct-field-write discipline again. **`:onChange(v, final)` is one name over the engine's two hooks** (`changed()`/`fchanged()`, D-157) — `final` false while dragging, true once on release, always, even for a click with no movement (`HSlider.mouseup` fires it unconditionally once a grab was active); the scrollbar's own `:onChange(fn)` carries no `final`, since a bare `Scrollbar` gives the bridge no release hook to report. **The one real gotcha**: `Scrollbar`'s OTHER constructor (`(h, Scrollable)`) makes `draw()` overwrite `min`/`max`/`val` from its `ctl` every frame — the adapter always takes the bare `(h, min, max)` form so an addon's own writes are the only thing touching those fields. `haven` core edits zero.

**040.5 DONE — `:radio()` as ONE control, and `:rows(t)`'s debut (D-155).** `hafen.ui():radio()` stacks `RadioGroup.RadioButton`s downward from its own `:position`, one row height apart — `RadioGroup`/`RadioButton` never surface, and `:type()` reads `"Widget"` since there is no single engine class this is an instance of. **No `haven` core edit**: `RadioGroup` is fully public, unlike 040.4's `CheckBox`. **The one real puzzle**: the engine gives no `ACheckBox`-style read/write split, so every selection path — a click and a script alike — funnels through one method, `RadioGroup.check(...)`, that always fires the notification hook; `:value(v)` instead calls the leaf `RadioButton.changed(boolean)` directly on the two affected buttons, one level below where the paths converge, which is D-153's rule generalised to a control with no seam to exploit for it. `:rows(t)` is the new sixth-ish name (a `Controls.Rows` capability, dispatched like `:value`/`:onChange`): re-`:rows{}` rebuilds a fresh `RadioGroup` (the engine has no way to remove a button once added), an empty one is an empty control, and a repeated or unknown label is refused by name. `:text()` is deliberately NOT shipped on radio, despite spec.md's summary table listing it — the per-builder table and the task's own acceptance criteria never asked for it.

**040.1 DONE — provenance is a CONTRACT, not a class (D-145), and `hafen.ui():button()` is its first consumer.** `ownedContent` now tests `instanceof Owned` rather than `instanceof AddonWidget`, so a `haven.Button` an addon builds reads as owned; adapters are `Ctl<Control>` in `io.brodgar.addon` (not a sub-package — the bridge's internals are package-private), each `implements Owned.Control` over one shared state object. `typeName` climbs past an adapter (D-146) so `:type()`/selectors/sheet keys still match the engine's class. New verbs: `:text(s)`'s write half and `:onPress(fn)`; `haven` core edits zero. **Open (D-147, 🔄)**: the eight paint/input callbacks refuse on a control, and decision B (`:onClick` on a button) is not yet wired. `specs/codebase/ui-controls.md` written and indexed.

**040.2 DONE — one builder, two engine classes, and the SETTER chooses (D-148).** `:image(up, down[, hover])` completes `hafen.ui():button()` as an `IButton` where `:text(s)` completes it as a `Button` — legal only while the control is pending (D-113 × D-119's arming window), refused once armed; the rebuild moves every map keyed on the old widget (parent/coord/visibility, owned registry, arming queue, Widget intern cache, `:rule()` cache, sheet style level) so the Lua handle stays `==` across the swap. **D-149**: a face is a `hafen.asset` handle or a client resource name (UI-scaled), with the near-miss (`"up.png"`) refused naming `hafen.asset`. A verb dispatches on a **capability** (`Controls.Press`), not a class. `haven` core edits again zero.

**040.3 DONE — the four display controls, and `:value()`'s debut (D-150/D-151/D-152).** `:label()`/`:image()`/`:separator()`/`:progress()` ship `Label`/`Img`/`HRuler`/`Progress` — plain `Widget` subclasses, none `SIWidget`, so none need 040.1's `redraw()`-on-resize fix. **`:value()`** is the new sixth-name verb (`Controls.Value`, dispatched on a capability like `:onPress`): `Progress`'s fraction is the first to answer it, `0..1`, a write outside the range refused rather than clamped; every other control reads `nil` on it. **`:source(h)`** is a picture's own content setter — unlike a button's face it is live at ANY time (`Img.setimg` is a public post-construction setter, nothing `final`), so it needs no D-113 rebuild. **The plan's assumption did not survive reading the source**: `ILabel` was expected to be `:label()`'s picture form, mirroring `IButton`; it is instead a label with a font baked once and never live-restyled — no picture at all — so `ILabel` is **not shipped** (D-151), decided with the maintainer mid-task. `haven` core edits zero.

**040.4 DONE — `:check()`, and `:onChange(fn)`'s debut (D-153/D-154).** `CheckBox`/`ICheckBox` (neither `SIWidget`-based) answer `:text`/`:value`/`:onChange`; **`:value(v)` writes the state field directly, never through the engine's own `set()`**, which is what keeps a programmatic write from re-entering `:onChange` (D-153) — the rule every later value-bearing control now follows. `:image(up, down, hoverUp, hoverDown)` completes the builder to `ICheckBox`, exactly four faces (D-154), through a new `Tex`-returning door (`Controls.faceTex`) beside the `BufferedImage` one a button's face uses. **One `haven` core edit** where the plan said zero: `CheckBox.lbl` was package-private with no live setter, so `// addon:` made it `public` and added `settext(String)`. **Found in-game, not by the suite**: a checkbox's click runs during `mousedown`, and the containing `Window` still touches itself afterward (`parent.setfocus`) — destroying that window synchronously from `:onChange` NPEs the UI thread, unlike a button's `:onPress` (safe only because `Button.mouseup` finishes with itself first). Fixed by deferring the destroy a tick.

**`039-uniform-api` DONE — one grammar for 33 sections, one `position()`, and the OOP migration finished.** Sixteen tasks, each its own login to verify. **The rule**: a section is CALLED and is a per-addon singleton, everything after it is a colon verb, **arity is the verb** (`:x()` reads, `:x(v)` writes and chains), a set is a **collection** (`:list/:count/:get/:find/:add/:remove`, singular name, not indexable), a builder is constructed **bare** and configured by chained setters, an explicit `nil` is an error except where a page documents *undo a layer* or *none*, every read hands back a live interned object with `:info()` as the only snapshot, and **every retired spelling throws naming its replacement**. Four mechanisms carry all of it — `Section`, `LuaCollection`, `LuaPosition`, `Retired` — and the last is pure data generated from the inventory, which is what made the close's completeness sweep possible rather than remembered. `hafen.gob` folded into `hafen.world():gob()` (34 sections → 33); three plural sections went singular (`quests`/`wounds`/`events`); five `get`/`set` pairs, four singular/plural dual pairs and eight config-table constructors collapsed; **14 new entities** finished the OOP half (char, study, party, fight, craft, quest, wound, and the Item), which restored `hafen.party():list()[1]:gob()` and `hafen.fight():target():gob()` — the regression 017 wrote down and accepted. **One `position()`, and it is a value that is computable AND durable**: it holds a session coordinate or a durable anchor and DERIVES the other, deleting `gridPos`/`fromGridPos`/`marker:anchor()` and moving 38 arithmetic sites into the engine, because a grid is 1100 world units and `p.y + 22` at `y = 1095` is a bug. Measured at **209.7 bytes a call against 419.4** for the read plus the `{x, y}` table it replaced — better than parity, not assumed. It marshals through `hafen.store` and `hafen.json` by its own three-key wire shape and comes back a Position; a non-durable one is refused rather than written as `null`; it deliberately has **no `__eq`**, because the store round trip is exact to the tile and not to the last bit of a double. **Decisions D-107..D-143** (`decisions/architecture-api.md`, plus `widgets-ui`, `fonts`, `virtual-entities`), the standing design is **`design/25-uniform-api.md`**, and `design/06-lua-api.md` is superseded in full while `design/22`'s selector grammar survives with new spellings. **What the build actually taught, in one line each.** 028.3's contract was **wrong** and the whole `nil` discipline rests on it being wrong — `narg()` does separate `f()` from `f(x)` when `x` is nil; the one hole is `f(g())` with a zero-return `g`, which the page states and the suite asserts. An entity's metatable pointing `__index` straight at its methods table makes a retired verb read as plain `nil`: found **three** times (`LuaGob`, `LuaOverlay`, `hafen.player()`), each only because something asserted the refusal rather than the dispatch. A scripted corpus port cannot see `pcall(hafen.json.parse, x)` — the field is fetched outside the guard — and it rewrites the very spelling a refusal test pins. `luac -p` **exits 0 on a syntax error**: read the output, not the status. A thing that cannot exist without a place takes it on the constructor (D-127, found in-game because `Placement()` resolves the tile under the gob and the world origin *raises*). An id the server **re-uses** cannot be an identity (D-138: an item handle keyed on the widget id retargets, and a gated write through it moves something the caller never named). And two filter paths over one collection disagreed on an unresolved name for thirteen tasks (D-141), because every consumer reached for the path that was right. **Ported and green**: 16 new suites, 21 older ones, 11 example addons and frozen `hello`, all verified in-game task by task; the docs tier is 75 pages with the link/anchor checker at 0 broken over ~1,243 links, every page ≤ 300 lines and the retired-name greps at zero. **The close (039.16)** swept all twelve example manifests — the `description` is the AddOns panel's **tooltip**, and `hello`'s had accreted to 26,086 characters still teaching `hafen.ghost.new{res, x, y}` (D-142) — settled the last open question, `g:text`'s trailing `{font, color}`, as the one named-argument table that stays, because R4 governs a builder and a draw call builds nothing (D-143), and shipped `addons/039-uniform-api.16/`: 33 sections callable and singleton, an unknown verb refused by name on every one, **98 retired section spellings and ~29 of 47 entity rows swept in one pass**, and the categorical cost claim measured beside a call that allocates, so the zero is the shape rather than the meter. Pre-checked headlessly 5/0/3 and 8/0/1 against a rule stub, with six falsifications biting 7 red lines — the last of them (the falsifier allocating nothing) reddening **both** zero claims. Clean `rm -rf build/classes` rebuild BUILD SUCCESSFUL; LuaJ parse sweep 50/50. `API.md` ends with **0 open questions and no unticked row**. **Left open and NOT done**: the five options subsystem handles and `ProfHandle` still read `nil` for an unknown verb where D-125 says a closed vocabulary throws (filed by 039.10, out of every task's rows since).

**`038-gob-overlays` DONE — `overlay` is the ENGINE's word for a thing attached to a gob, and the API finally spends it on one.** `gob:overlay()` is the one way to attach anything to a game object and the one way to read what is already attached — yours by key, the game's own as `native`, read-only — absorbing `hafen.ui.gobOverlay` and the `follow =` anchor of `hafen.render`/`hafen.ghost`. **D-100 is the rule that carries it — the state belongs on the THING, and a sweep is the shape of having put it elsewhere**: the store moved onto the gob's own `GAttrib`, which deleted the filter list, the throttled 5 Hz sweep, the per-frame re-match at the draw and the prune an addon-side map would have needed, and made *an overlay dies with its gob* cost no handler at all. **D-102 is the half that is not free**: a world-space overlay's visual is its own client gob and `OCache.remove` never calls `dispose()`, which is exactly the latent bug `follow=` had — a sprite anchored to a felled tree floating forever with no owner — so its end rides the `GobRemoved` the client already raises. D-101 was measured (13 of 33 decorated gobs carry two overlays of one resource, so a native overlay is a **union** and `ov:count()` publishes the multiplicity); D-104/D-105/D-106 are the event rules, the last being that a queue whose handlers write back into it is drained one frame's worth, never to empty. Three `// addon:` core edits where the plan predicted two, because `Gob.ctick`'s expiry of a self-ending sprite is how most of the game's overlays actually end. Example addon `tagger`; the standing design is `design/24-gob-overlays.md`.

**Before that**, all DONE (detail in each `NNN-` folder, one-line summaries in `FEATURES.md`): `037-map-database` (the RECORDED map on disk beside the live world — segments, grids, markers, masks, minimap drawings), `036-ui-layout` (position/size/anchor in the sheet, and a whole theme as a data file), `035-ui-chrome` (the sheet learns to DRAW), `034-ui-stylesheet-tree` (a tree key says WHICH widgets), `033-ui-stylesheet` (ONE table says what the client looks like), `032-replace-verb` (replacement is a verb on the entity; the `UI.NewWidget` core seam deleted), `031-window-lifecycle` (hiding a native window takes its toggle), `030-ui-selectors` (a tiny CSS-shaped grammar parsed once into a predicate), `029-widget-oop` (three objects for one widget became ONE interned entity), `028-asset-loader` (one loader for an addon's own files), `027-meters-oop`, `026-text-cache` (130 → 220-240 FPS on the harness), `025-buffs-oop`, `024-audio-oop` (the Track section was built to spec and then CUT — this server sends no MIDI), `023-menugrid-oop`, `022-actionbar-set`, `021-actionbar-oop`, `020-kin-oop`, `019-profiling`, `018-client-options`, `017-gob-oop`, and 001–016.

## Engine & runtime
- **Engine**: LuaJ embedded (`src/io/brodgar/addon/`); per-addon sandboxed envs (D-017), instruction watchdog + soft per-tick CPU budget with auto-disable (D-018). **Loading/lifecycle**: `manifest.json` discovery from `addons/`; `:reload` rebuilds the addon layer only (D-005); enabled set persisted (D-006). Console `:lua` / `:addons` / `:reload`; AddOns panel (toggle/status, and the manifest `description` as the row's tooltip). **Event bus** `hafen.event():on` (OnLoad/OnEnterWorld/OnUpdate/OnDisable, GobAdded/Removed (a Gob), MeterAdded/Removed/Changed (a Meter), Buff*/Fep/Study/Actionbar/Equip Changed, GhostClicked, ChatMessage…) — error-isolated, UI-thread-marshalled; timers `hafen.timer():after/:every`. Container item events are **not** on the bus — they are per-widget (`w:onItemAdded/:onItemRemoved/:onDestroy`). **Store**: `hafen.store():get(name)` JSON saved-vars, per-char + account (D-023), handing back the LIVE table.

## The `hafen.*` surface — the docs tier IS the contract

- **One grammar** (039 §2 · `docs/addons/api/conventions.md`): a section is CALLED and is a per-addon singleton, everything after it is a colon verb, **arity is the verb**, a set is a **collection**, every read hands back a live interned object with `:info()` as the only snapshot, an explicit `nil` raises except where a page documents a meaning for it, a builder is constructed bare and configured by chained setters, a place is a **Position**, and every retired spelling **throws naming its replacement**. **33 sections**; the per-verb surface is `docs/addons/api/` and is deliberately not copied here.
- **Gated tier**: `hafen.act()` and the per-subsystem write verbs, behind the per-addon `actions` permission with enable-time consent and no global switch (D-027/D-028); `hafen.http` carries its own `network` host allowlist. Everything else observes, or writes client-local only and says so on its page.
- **Example addons** (12, installed, dormant until asked): frozen `hello`, `bags`, `theme`, `atlas`, `planner`, `tagger`, `widgetstack`, `profiler`, `optionstest`, `walker`, `netdemo`, `hogtest` — each described in `docs/addons/examples.md`, and each carrying one accurate paragraph in its manifest rather than a changelog (D-142).
- **Testing**: one self-checking suite per task under `addons/<NNN>-<feature>.<X>/`, run by hand as `:t<NNN>-<X>` (`specs/addons/TESTING.md`). The full regression is running their commands, one at a time, in any order.
