# STATE — what works right now

> Maintained by REPLACING (max 60 lines). Branch `feature/addons`; per-feature detail: its `NNN-` folder.

**ACTIVE: `041-unified-events`** (planned 2026-08-06; **3 of 7 tasks done** — 041.1 verified in-game 16/0, one `[manual]` answered; 041.2 verified in-game 18/0, one `[manual]` answered; 041.3 verified in-game 13/0, one `[manual]` answered). One verb for every notification in the area. Before it, an addon installed a callback three unrelated ways: `hafen.event():on("GobAdded", fn)` (N subscribers, `sub:off()`, PascalCase), `hafen.hook():input/:action/:message` (N per message, `handle:remove()`, lowercase) and `btn:onPress(fn)`/`inv:onItemAdded(fn)`/`w:onDraw(fn)` (**one slot that silently replaces**, no unsubscribe, `onCamelCase`) — three cardinalities, three unsubscribe spellings and three naming conventions for one concept, the half 039 deliberately left alone. **The rule: `X:on(key, fn)` returns a subscription, `sub:off()` ends it, and one question picks the door — ¿tienes el objeto? `obj:on(...)`; ¿no? `hafen.event()`.** N subscribers everywhere (the single slot is exactly what forced WoW to bolt `HookScript` beside `SetScript` years later); cancelling is `ev:preventDefault()`, DOM-shaped and one moment — so **no handler's return value is ever read**, and any handler cancelling cancels while every handler still runs, which is why the outcome never depends on registration order; keys are PascalCase with no `On` prefix — unifying onto the bus's own spelling, so 44 of its 88 `hafen.event():on(...)` sites need no rename at all, only the four `On`-prefixed lifecycle ones; a CLOSED key set (widgets, the bus) refuses an unknown one while `action`/`message` names stay OPEN because a `wdgmsg` name is protocol, not a catalogue the client owns (D-129). **Four concrete defects, not just the inconsistency**: input reaches 3 widgets out of ~625 through magic string tokens (`Widget.listen` is a `Widget` method — an API limit, never an engine one); `w:onClick` and `hafen.hook():input(…, "mousedown")` are two spellings for one gesture whose name is also wrong (it binds `MouseDownEvent`); `ev.sender`/`ev.target` are class-name **strings**, so a handler cannot navigate to the widget the event is about; and a native widget found by selector has **no input surface at all**. Measured corpus: ~330 Lua call sites over 64 files, 33 of 76 doc pages, 30 Java files, 26 bus names. `EXAMPLES.md` carries the complete emitter × key catalogue, the before→after port map and the worked examples — validated before implementation, and read by every `/implement` session instead of guessing a spelling. **The maintainer's review of the draft corrected two things**, both the area's own rules catching a leftover: the event object was drafted with FIELDS (`ev.msg`, `ev.sender`, `ev.args`) because that is what `LuaActionHook` builds today — a plain Lua table written before 039 and never reached by it — which would have made `ev` the one object in the API mixing `.` and `:`, so it becomes `ev:msg()`/`ev:sender()`/`ev:args()` and the composite bus payloads (`GobOverlay*`, the three `*Clicked`) move with it; and `hafen.hook():grab{move, up}` was excused from R4 as per-call scope like `g:text`'s table (D-143) — **wrong**, because `g:text`'s table dies with its call while a grab outlives it, so the table is cut and a grab becomes an emitter like everything else. **Looking for its new address turned `hafen.ui():mouse()` into an entity**: `m:x()`/`m:y()`, `m:over()`, `m:shift()`/`m:ctrl()`/`m:alt()` (**new** — `UI.modflags()` is public and reached Lua nowhere) and `m:grab()`, the receiver saying WHAT is grabbed so the verb keeps the engine's own word (D-061). `hafen.ui():at(x, y)` is NOT absorbed — it takes any point. Modifiers are three flat verbs, never a `mods` table. Found while writing it: **`ChatMessage` does not exist** — this file lists it among the bus events and `design/09` has it as a planned one-liner, but nothing fires it and no addon uses it; corrected at the close (041.7).

**041.1 shipped the mechanism the other six are built on.** [`Subs`](../../src/io/brodgar/addon/Subs.java) is a keyed multimap + `fire` + OR-accumulated cancel + the `has(key)` gate, one per emitter, and [`LuaSub`](../../src/io/brodgar/addon/LuaSub.java) is both the Lua handle and the list entry — so `:off()` is idempotent **by construction** (removal by identity from a copy-on-write list) rather than by a flag. `AddonManager.Sub` is deleted, not left one character from `Subs` in the same package. The bus is the first emitter over it: 26 keys closed at load (an unknown one throws instead of being accepted and never firing, which was the most common silent addon bug there is), the four lifecycle keys without their redundant `On` and the old spellings throwing their replacement, `MarkersChanged` handing over the count itself. **D-167/D-168/D-169** carry what the build decided: the profiling category is DATA on the `Subs` per key — every callback in the client becomes a `Subs.fire`, so a single charge inside `fire` would have collapsed `Addon.CATS`' five columns and the profiler would have gone on reporting confidently (the suite asserts the `events` column on a live armed client); a retired event KEY is an ARGUMENT, not a name, so it cannot hang off an `__index` and gets its own `Retired` table consulted **before** the vocabulary check, or all four moved keys report as "unknown event" — true and useless; and a closed vocabulary LISTS what it answers when the list is short (a widget's five) and POINTS at the catalogue when it is not (the bus's 26). Corpus ported in the same task per the feature's hard-cut rule: 46 quoted call sites + 51 prose mentions across 15 addons, frozen `hello` included (the change genuinely broke it). **The docs tier is deliberately NOT updated per task** — 041.6 rewrites all 33 pages at once — so `docs/addons/` still says `OnLoad` until then, and that is the plan rather than a gap.

**041.2 retired `hafen.hook():action/:message`** onto the bus's other two doors, `hafen.event():action():on(msg, fn)` and `:message():on(msg, fn)`, over the same `Subs` — no global dispatch map, each owner's own `Subs` answers `has(key)` at the door (`AddonManager.dispatchAction`/`dispatchMessage`). New [`LuaEvent`](../../src/io/brodgar/addon/LuaEvent.java): one class, a per-shape methods table, an unknown verb throwing and listing what that shape answers; `ev:sender()`/`ev:target()` are now **lazily interned Widget handles** (D-064) rather than class-name strings, so `ev:sender():parent()` navigates. The `holdsLock` guard stayed on the action path, none on the message path, and the OR-accumulated cancel now spans every addon of one fire, not just one addon's handlers. **D-170**: a colon VERB retired off a section that keeps existing (`hafen.hook():action`) had no `Retired` door until now — `Section.meta`'s `__index` consults it before the generic "has no verb" refusal, which is the sibling of D-168's event-key case one level up. **D-171, found on the first login**: a chat `"msg"` uimsg arrives `(nil, line)` — a null sender marks the player's OWN line, not an edge case — and `LuaMarshal.luaToArgs` sized its output by Lua's `#t`, which reads 0 across that leading hole; `ev:rewrite(ev:args())`, the identity round trip, silently applied an empty argument list. Fixed by sizing on the highest table index instead (capped at 256); a *trailing* nil stays unrepresentable, which is a property of Lua tables and is documented as such. `specs/codebase/services.md`'s Chat row now carries the three channel uimsg shapes (`SimpleChat`/`MultiChat`/`PrivChat` do not share one arg layout for `"msg"`) — what found D-171. Corpus: `addons/hello/main.lua`'s 2d/2e demos, the only Lua sites these two levels had (the ~20 in tasks.md's estimate were mostly 041.3/041.5's input/grab sites). `TESTING.md` amended this task: past suites are assumed **never run**, so a task carries forward every old assertion its own claims rest on rather than naming another suite's command as part of its proof.

**041.3 is the feature's one new reach: input on ANY widget**, not just the three tokens `hafen.hook():input` used to accept. [`WidgetSubs`](../../src/io/brodgar/addon/WidgetSubs.java) is one per (addon, widget) — the same shape a bus emitter is one per addon — installing the ONE engine `EventHandler` a key needs via `Widget.listen`/`deafen` (a plain `Widget` method, so the reach was always an API limit, never an engine one) and deafening it ([`Subs.Idle`](../../src/io/brodgar/addon/Subs.java), a new opt-in hook) once the last Lua sub on that key goes; teardown walks every widget an addon has touched and deafens what is left. **D-172: an OWN widget's input rides the identical door a NATIVE one does** — `AddonWidget` drops its four `mousedown`/`mouseup`/`mousemove`/`mousewheel` overrides entirely rather than keeping a second dispatch path behind the one verb, which is what "the same keys work on an own widget with one vocabulary" (the task's own claim) actually requires. `hafen.hook():input` and the four `w:onClick`/`:onMouseUp`/`:onMouseMove`/`:onWheel` verbs throw naming `widget:on(key, fn)`; D-040's per-callback `mods` table goes with the slots it rode on — the unified `ev` for these four keys carries no modifiers, and nothing replaces that read until the mouse entity (041.5). **Found in-game on the very first login**: `widget:on(key, fn)` hands back a Sub, not the widget, so — unlike every OTHER builder setter — it cannot sit mid-chain or be a chain's last, assigned call; the corpus port had done exactly that in five places across four addons (`bags`, `hello` ×2, `profiler`, `widgetstack` ×2), and one of them crashed `EnterWorld` outright. Fixed by moving every `:on(...)` to its own statement after the builder chain finishes; a headless dry run would not have caught it (a suite that builds windows cannot be dry-run) — this is `learnings/hooks-hotkeys.md`'s newest entry. Corpus ported: `hello` (frozen, genuinely broken), `bags`, `planner/gizmo.lua`, `profiler`, `widgetstack`, one doc-snippet block in `039-uniform-api.15`, one now-obsolete premise in `041-unified-events.2`'s own suite.

**`040-ui-controls` DONE — 18 of the client's own controls reach Lua, in two groups.** The **direct** ones (`Button`/`IButton`, `TextEntry`, `Label`, `ILabel`, `Img`, `Progress`, `HRuler`, `CheckBox`/`ICheckBox`, `RadioButton`, `HSlider`, `Scrollport`+`Scrollbar`) are concrete classes that construct and configure; the **model-backed** five (`SListBox`, `SDropBox`, `SListMenu`, `GridList`, `TableBox`) are abstract and generic over the item type, and four of the five reduce to `SListWidget`'s `items()`/`makeitem()` pair (`LuaRows`, D-108), while `GridList` alone draws its rows through the `LuaGOut` wrapper `:onDraw` already uses. **A control is a Widget, not a nineteenth entity** — every existing verb and selector works on one for free — with `:value()` the one verb for a control's value (`Controls.Value`, first on `:progress()`, 040.3) and `:onChange(fn)` the one notification (first on `:check()`, 040.4); a programmatic `:value(v)` never re-enters its own handler, the rule every later value-bearing control repeats (D-153). **The one mechanism that moved**: provenance is a CONTRACT, not a class (D-145) — `ownedContent` tests `instanceof Owned` rather than `instanceof AddonWidget`, so a `haven.Button`/`IButton`/… an addon builds reads as owned, and `typeName` climbs past the adapter (D-146) so `:type()`/selectors/sheet keys still match the engine's class. **One builder per role, not per class**: the setter chooses which engine class results — `:text(s)` completes `:button()`/`:check()` as `Button`/`CheckBox`, `:image(...)` as `IButton`/`ICheckBox` — legal only while the control is pending, refused once armed (D-113×D-119, D-148/D-154); `ILabel` looked like the same shape but carries no picture at all, so it is **not shipped** (D-151). **Per-control gotchas, each found only in-game**: a checkbox's click runs during `mousedown`, so destroying its own window from `:onChange` needs a deferred tick (040.4); a slider CLAMPS an out-of-range `:value` instead of refusing, and re-clamps silently when `:range` narrows (D-156, 040.6); an entry's `:value(v)` writes through `rsettext`, never `settext`, to avoid re-entering `:onChange` (040.7), and `entry:text(s)` is the feature's one retired spelling, throwing and naming `:value(s)` (D-158); `:scroll()` is COMPOSED from `Scrollport`'s own public pieces rather than subclassed, because its own bar is sealed at construction, and `:parent(w)` on any control now special-cases a `Scrollport`-shaped target so a child lands inside `cont`, not beside the bar (040.8); the row bridge (`LuaRows`, 040.9) resolves every row EAGERLY at `:rows(t)` time, never lazily, because `SListBox.update()` has no error isolation; `SListMenu.added()` grabs all input unconditionally (fixed with `nograb()`) and a dropdown's popup needs a queued same-frame re-raise to land above the window that opened it (040.10); `GridList.drawitem` isolates a throwing cell to just that cell's log line, one frame (040.11); and `TableBox`'s own constructor calls `spec()`/`itemh()` before an adapter's field write would be visible, so `:table()`'s factory hands back an ANONYMOUS subclass capturing them as locals instead (040.12). **`haven` core edits: one** — `CheckBox.lbl` made `public` + a `settext(String)` (040.4) — every other task needed zero. Decisions D-145..D-166 (`decisions/widgets-ui.md`). **The close (040.13)**: a roster-wide sweep proving all 16 builders exist, build bare (R4) and answer the client class the roster promises, that 12 shared verbs answer non-nil exactly where `docs/addons/api/ui/controls.md`/`lists.md` say and `nil` elsewhere (192 combinations), and that one destroy leaves no orphan in the tree — plus the example addon `stockfilter`, the api-sketch.md worked panel wired to real backpack/equipment items rather than an invented `query{}`. `specs/codebase/ui-controls.md` written and indexed across the feature.

**`039-uniform-api` DONE — one grammar for 33 sections, one `position()`, and the OOP migration finished.** Sixteen tasks, each its own login to verify. **The rule**: a section is CALLED and is a per-addon singleton, everything after it is a colon verb, **arity is the verb** (`:x()` reads, `:x(v)` writes and chains), a set is a **collection** (`:list/:count/:get/:find/:add/:remove`, singular name, not indexable), a builder is constructed **bare** and configured by chained setters, an explicit `nil` is an error except where a page documents *undo a layer* or *none*, every read hands back a live interned object with `:info()` as the only snapshot, and **every retired spelling throws naming its replacement**. Four mechanisms carry all of it — `Section`, `LuaCollection`, `LuaPosition`, `Retired` — and the last is pure data generated from the inventory, which is what made the close's completeness sweep possible rather than remembered. `hafen.gob` folded into `hafen.world():gob()` (34 sections → 33); three plural sections went singular (`quests`/`wounds`/`events`); five `get`/`set` pairs, four singular/plural dual pairs and eight config-table constructors collapsed; **14 new entities** finished the OOP half (char, study, party, fight, craft, quest, wound, and the Item), which restored `hafen.party():list()[1]:gob()` and `hafen.fight():target():gob()` — the regression 017 wrote down and accepted. **One `position()`, and it is a value that is computable AND durable**: it holds a session coordinate or a durable anchor and DERIVES the other, deleting `gridPos`/`fromGridPos`/`marker:anchor()` and moving 38 arithmetic sites into the engine, because a grid is 1100 world units and `p.y + 22` at `y = 1095` is a bug. Measured at **209.7 bytes a call against 419.4** for the read plus the `{x, y}` table it replaced — better than parity, not assumed. It marshals through `hafen.store` and `hafen.json` by its own three-key wire shape and comes back a Position; a non-durable one is refused rather than written as `null`; it deliberately has **no `__eq`**, because the store round trip is exact to the tile and not to the last bit of a double. **Decisions D-107..D-143** (`decisions/architecture-api.md`, plus `widgets-ui`, `fonts`, `virtual-entities`), the standing design is **`design/25-uniform-api.md`**, and `design/06-lua-api.md` is superseded in full while `design/22`'s selector grammar survives with new spellings. **What the build actually taught, in one line each.** 028.3's contract was **wrong** and the whole `nil` discipline rests on it being wrong — `narg()` does separate `f()` from `f(x)` when `x` is nil; the one hole is `f(g())` with a zero-return `g`, which the page states and the suite asserts. An entity's metatable pointing `__index` straight at its methods table makes a retired verb read as plain `nil`: found **three** times (`LuaGob`, `LuaOverlay`, `hafen.player()`), each only because something asserted the refusal rather than the dispatch. A scripted corpus port cannot see `pcall(hafen.json.parse, x)` — the field is fetched outside the guard — and it rewrites the very spelling a refusal test pins. `luac -p` **exits 0 on a syntax error**: read the output, not the status. A thing that cannot exist without a place takes it on the constructor (D-127, found in-game because `Placement()` resolves the tile under the gob and the world origin *raises*). An id the server **re-uses** cannot be an identity (D-138: an item handle keyed on the widget id retargets, and a gated write through it moves something the caller never named). And two filter paths over one collection disagreed on an unresolved name for thirteen tasks (D-141), because every consumer reached for the path that was right. **Ported and green**: 16 new suites, 21 older ones, 11 example addons and frozen `hello`, all verified in-game task by task; the docs tier is 75 pages with the link/anchor checker at 0 broken over ~1,243 links, every page ≤ 300 lines and the retired-name greps at zero. **The close (039.16)** swept all twelve example manifests — the `description` is the AddOns panel's **tooltip**, and `hello`'s had accreted to 26,086 characters still teaching `hafen.ghost.new{res, x, y}` (D-142) — settled the last open question, `g:text`'s trailing `{font, color}`, as the one named-argument table that stays, because R4 governs a builder and a draw call builds nothing (D-143), and shipped `addons/039-uniform-api.16/`: 33 sections callable and singleton, an unknown verb refused by name on every one, **98 retired section spellings and ~29 of 47 entity rows swept in one pass**, and the categorical cost claim measured beside a call that allocates, so the zero is the shape rather than the meter. Pre-checked headlessly 5/0/3 and 8/0/1 against a rule stub, with six falsifications biting 7 red lines — the last of them (the falsifier allocating nothing) reddening **both** zero claims. Clean `rm -rf build/classes` rebuild BUILD SUCCESSFUL; LuaJ parse sweep 50/50. `API.md` ends with **0 open questions and no unticked row**. **Left open and NOT done**: the five options subsystem handles and `ProfHandle` still read `nil` for an unknown verb where D-125 says a closed vocabulary throws (filed by 039.10, out of every task's rows since).

**`038-gob-overlays` DONE — `overlay` is the ENGINE's word for a thing attached to a gob, and the API finally spends it on one.** `gob:overlay()` is the one way to attach anything to a game object and the one way to read what is already attached — yours by key, the game's own as `native`, read-only — absorbing `hafen.ui.gobOverlay` and the `follow =` anchor of `hafen.render`/`hafen.ghost`. **D-100 is the rule that carries it — the state belongs on the THING, and a sweep is the shape of having put it elsewhere**: the store moved onto the gob's own `GAttrib`, which deleted the filter list, the throttled 5 Hz sweep, the per-frame re-match at the draw and the prune an addon-side map would have needed, and made *an overlay dies with its gob* cost no handler at all. **D-102 is the half that is not free**: a world-space overlay's visual is its own client gob and `OCache.remove` never calls `dispose()`, which is exactly the latent bug `follow=` had — a sprite anchored to a felled tree floating forever with no owner — so its end rides the `GobRemoved` the client already raises. D-101 was measured (13 of 33 decorated gobs carry two overlays of one resource, so a native overlay is a **union** and `ov:count()` publishes the multiplicity); D-104/D-105/D-106 are the event rules, the last being that a queue whose handlers write back into it is drained one frame's worth, never to empty. Three `// addon:` core edits where the plan predicted two, because `Gob.ctick`'s expiry of a self-ending sprite is how most of the game's overlays actually end. Example addon `tagger`; the standing design is `design/24-gob-overlays.md`.

**Before that**, all DONE (detail in each `NNN-` folder, one-line summaries in `FEATURES.md`): `037-map-database` (the RECORDED map on disk beside the live world — segments, grids, markers, masks, minimap drawings), `036-ui-layout` (position/size/anchor in the sheet, and a whole theme as a data file), `035-ui-chrome` (the sheet learns to DRAW), `034-ui-stylesheet-tree` (a tree key says WHICH widgets), `033-ui-stylesheet` (ONE table says what the client looks like), `032-replace-verb` (replacement is a verb on the entity; the `UI.NewWidget` core seam deleted), `031-window-lifecycle` (hiding a native window takes its toggle), `030-ui-selectors` (a tiny CSS-shaped grammar parsed once into a predicate), `029-widget-oop` (three objects for one widget became ONE interned entity), `028-asset-loader` (one loader for an addon's own files), `027-meters-oop`, `026-text-cache` (130 → 220-240 FPS on the harness), `025-buffs-oop`, `024-audio-oop` (the Track section was built to spec and then CUT — this server sends no MIDI), `023-menugrid-oop`, `022-actionbar-set`, `021-actionbar-oop`, `020-kin-oop`, `019-profiling`, `018-client-options`, `017-gob-oop`, and 001–016.

## Engine & runtime
- **Engine**: LuaJ embedded (`src/io/brodgar/addon/`); per-addon sandboxed envs (D-017), instruction watchdog + soft per-tick CPU budget with auto-disable (D-018). **Loading/lifecycle**: `manifest.json` discovery from `addons/`; `:reload` rebuilds the addon layer only (D-005); enabled set persisted (D-006). Console `:lua` / `:addons` / `:reload`; AddOns panel (toggle/status, and the manifest `description` as the row's tooltip). **Event bus** `hafen.event():on` (OnLoad/OnEnterWorld/OnUpdate/OnDisable, GobAdded/Removed (a Gob), MeterAdded/Removed/Changed (a Meter), Buff*/Fep/Study/Actionbar/Equip Changed, GhostClicked, ChatMessage…) — error-isolated, UI-thread-marshalled; timers `hafen.timer():after/:every`. Container item events are **not** on the bus — they are per-widget (`w:onItemAdded/:onItemRemoved/:onDestroy`). **Store**: `hafen.store():get(name)` JSON saved-vars, per-char + account (D-023), handing back the LIVE table.

## The `hafen.*` surface — the docs tier IS the contract

- **One grammar** (039 §2 · `docs/addons/api/conventions.md`): a section is CALLED and is a per-addon singleton, everything after it is a colon verb, **arity is the verb**, a set is a **collection**, every read hands back a live interned object with `:info()` as the only snapshot, an explicit `nil` raises except where a page documents a meaning for it, a builder is constructed bare and configured by chained setters, a place is a **Position**, and every retired spelling **throws naming its replacement**. **33 sections**; the per-verb surface is `docs/addons/api/` and is deliberately not copied here.
- **Gated tier**: `hafen.act()` and the per-subsystem write verbs, behind the per-addon `actions` permission with enable-time consent and no global switch (D-027/D-028); `hafen.http` carries its own `network` host allowlist. Everything else observes, or writes client-local only and says so on its page.
- **Example addons** (13, installed, dormant until asked): frozen `hello`, `bags`, `theme`, `atlas`, `stockfilter`, `planner`, `tagger`, `widgetstack`, `profiler`, `optionstest`, `walker`, `netdemo`, `hogtest` — each described in `docs/addons/examples.md`, and each carrying one accurate paragraph in its manifest rather than a changelog (D-142).
- **Testing**: one self-checking suite per task under `addons/<NNN>-<feature>.<X>/`, run by hand as `:t<NNN>-<X>` (`specs/addons/TESTING.md`). The full regression is running their commands, one at a time, in any order.
