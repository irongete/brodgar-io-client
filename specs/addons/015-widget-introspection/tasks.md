# 015-widget-introspection — Tasks

- [x] 015.1 — `hafen.ui.root()/node(id)` + the `WidgetNode` handle (type/id/children/
      parent/pos/size/visible/text/walk/same; transient, liveness-checked).
- [x] 015.2 — Hit-testing: `hafen.ui.at/mouse` + `node:at/:rootpos` (engine-mirroring
      hitTest) + the `widgetstack` framestack/inspector addon.
