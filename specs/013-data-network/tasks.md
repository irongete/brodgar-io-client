# 013-data-network — Tasks

- [x] 013.1 — `hafen.json` parse/encode: the unified `Json.write` serializer (strict public
      mode), `LuaMarshal.jsonToLua`, depth/input caps.
- [x] 013.2 — `hafen.http.get` + the async/security substrate: pool + queue + tick drain,
      `network` manifest allowlist, private-IP block, caps, `[net]` panel badge, `netdemo`.
- [x] 013.3 — `hafen.http.post` + redirect-follow: body encoding (string/table→JSON),
      manual ≤5-hop follow with per-hop allowlist + IP re-validation, method demotion.
