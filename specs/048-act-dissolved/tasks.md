# 048-act-dissolved — Tasks

- [x] 048.1 — `hafen.player():move(p)` and `gob:click(button, mods)`
- [x] 048.2 — the Hand: `hafen.player():hand()`, `:item()`, `:use(target, mods)`
- [x] 048.3 — the Item verbs: `item:use(mods)`, `:take()`, `:drop(n)`, `:transfer(n)`
- [x] 048.4 — `hafen.world():place(p, angle, button, mods)` and `:select(p1, p2, mods)`
- [x] 048.5 — `pag:use()` becomes protected, and `act():menu` is deleted
- [x] 048.6 — `widget:send(msg, ...)`, and the death of the target tokens
- [x] 048.7 — the two deletions, and the end of the section
- [x] 048.8 — the docs tier
