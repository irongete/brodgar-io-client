# NNN-<feature> — Spec

<!-- MAX 80 lines (a ceiling, not a target). If it doesn't fit, split the feature. -->

## What & why
<!-- 3-6 lines: the capability, who uses it, why now. If a design/ doc covers it, reference
     it and state only the delta this feature ships. -->

## Acceptance criteria
<!-- Each one verifiable by the maintainer the way this area's AREA.md defines Verification.
     Concrete: what to run/type/click, what appears. -->
- [ ] ...
- [ ] The area's test harness (see AREA.md) exercises the feature; regression still passes.

## Out of scope
- ...

## Context files
<!-- The feature's context budget: the EXACT files to read to work on it. Nothing else is
     loaded by /implement. Include: design/ docs, sources, doc pages, related NNN- folders.
     One line each, with why. -->
- `design/XX-....md` — the design this implements
- `src/...` — the seam being touched / where the code lands
- `<docs tier path>/....md` — the shipped surface this extends
- `NNN-<related-feature>/` — prior art / mechanism reused
