# Learnings — index

> Hard-won, non-obvious facts, split by category under `learnings/`. **Never read the whole
> set** — pick the 1-2 relevant files below and `grep` them (e.g.
> `grep -ri <term> learnings/`). **Append-only:** new learnings go to the matching category
> file; add an index line here only when a new category file is created.

- `learnings/<category>.md` — <what this category covers>
