# ROADMAP — future work for this area

> Candidate features, not yet planned. One line each. `/plan` removes the line it picks up.

*(empty — `049-css-selectors` was picked up as `007-css-selectors-review`.)*

The area's standing pattern: a feature closes in `addons`, and its pages are read against `src/` here.
The next entry is whichever feature `addons` closes next.
