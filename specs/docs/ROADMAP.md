# ROADMAP — future work for this area

> Candidate features, not yet planned. One line each. `/plan` removes the line it picks up.

Empty — both standing entries (the ceiling with no headroom, and the standard still spelling the tier
`gated`/`ungated` with `hafen.act` missing from §7's list) were picked up by
`006-act-dissolved-review`.
