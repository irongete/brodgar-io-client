# ROADMAP — future work for this area

> Candidate features, not yet planned. One line each. `/plan` removes the line it picks up.

- **Review `049-css-selectors` once area `addons` closes it.** Held out of `006` deliberately: its docs
  land after its implementation, so every selector page `006` read describes what shipped *before* it. This
  is the area's standing pattern — a feature closes in `addons`, its pages are read against `src/` here.
