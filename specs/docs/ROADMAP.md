# ROADMAP — future work for this area

> Candidate features, not yet planned. One line each. `/plan` removes the line it picks up.

- **Tree-wide wrap and wording sweep** — the six lines still over 110 columns on pages no feature has
  touched (`act.md:130` `font.md:132` `player.md:29` `ui/native.md:47` `ui/selectors.md:137`
  `ui/style/geometry.md:77`), plus the three places outside the two `AREA.md` files
  that still spell the addons docs tier `docs/addons/api/*.md`: `CLAUDE.md:16`,
  `specs/addons/_template/spec.md:20`, and IA §5.3's `api/markers.md` / `api/radar.md` rows.
