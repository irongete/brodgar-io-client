# NNN-<feature> — Tasks

<!-- MAX 60 lines (a ceiling, not a target). One task = one session: self-contained,
     builds, and verifiable on its own (see AREA.md → Verification). If it doesn't fit,
     split the feature. A task may carry its own "extra context:" line (files beyond the
     spec's Context files). -->

- [ ] NNN.1 — ...
      <!-- extra context: `src/...` (only this task needs it) -->
- [ ] NNN.2 — ...
